# 🚀 Инструкция: Создание Release v7.1 на GitHub

## Шаг 1: Создать Git Tag

```bash
cd "/Users/kotovod/Documents/GitHub/Orbita DS Tools"

# Создать аннотированный тег
git tag -a v7.1 -m "Release v7.1: Исправление проверки переменных"

# Отправить тег на GitHub
git push origin v7.1
```

## Шаг 2: Создать Release на GitHub

### Вариант A: Через веб-интерфейс

1. Перейдите в репозиторий на GitHub
2. Нажмите **"Releases"** → **"Draft a new release"**
3. Заполните форму:

**Tag version:**
```
v7.1
```

**Release title:**
```
v7.1 — Исправление проверки переменных
```

**Description:**
Скопируйте содержимое файла `releases/GITHUB_RELEASE_v7.1.md`

4. Нажмите **"Publish release"**

---

### Вариант B: Через GitHub CLI

```bash
# Установите GitHub CLI, если ещё не установлен
# brew install gh

# Авторизуйтесь
gh auth login

# Создайте release
gh release create v7.1 \
  --title "v7.1 — Исправление проверки переменных" \
  --notes-file releases/GITHUB_RELEASE_v7.1.md
```

---

## Шаг 3: (Опционально) Прикрепить файлы к релизу

Если хотите добавить ZIP-архив с файлами плагина:

```bash
# Создать ZIP с основными файлами
zip -r releases/orbita-ds-tools-v7.1.zip \
  code.js \
  ui.html \
  manifest.json \
  README.md \
  CHANGELOG.md \
  docs/

# Добавить к релизу (через GitHub CLI)
gh release upload v7.1 releases/orbita-ds-tools-v7.1.zip

# Или загрузите через веб-интерфейс:
# GitHub → Releases → v7.1 → Edit → Attach binaries
```

---

## Шаг 4: Обновить README в релизах

```bash
# Обновить список релизов
echo "- v7.1 (29.12.2025) - Исправление проверки переменных" >> releases/README.md
```

---

## 📋 Checklist

Перед созданием релиза убедитесь:

- [x] ✅ Все изменения закоммичены
- [x] ✅ CHANGELOG.md обновлен
- [x] ✅ README.md обновлен с новой версией
- [x] ✅ Плагин протестирован
- [x] ✅ Release notes подготовлены
- [ ] ⏳ Git tag создан
- [ ] ⏳ Release опубликован на GitHub
- [ ] ⏳ (Опционально) ZIP-архив прикреплен

---

## 🔧 Полезные команды

### Проверить статус git
```bash
git status
git log --oneline -5
```

### Просмотреть теги
```bash
git tag -l
```

### Удалить тег (если нужно)
```bash
# Локально
git tag -d v7.1

# Удаленно
git push origin :refs/tags/v7.1
```

### Проверить текущую ветку
```bash
git branch
```

---

## 📝 Текст для коммита

Если ещё не закоммитили изменения:

```bash
git add .
git commit -m "Release v7.1: Исправление проверки переменных для числовых свойств

- Корректная проверка наличия переменных через id
- Добавлена поддержка Individual Strokes
- Улучшены проверки для padding, corner radius, gap, stroke weight
- Обновлена документация

Fixes: Ложные срабатывания для элементов с привязанными переменными"

git push origin main
```

---

## 📞 Помощь

Если возникли проблемы:

1. **Проблема с правами:** Убедитесь, что у вас есть права на создание релизов
2. **Тег уже существует:** Удалите старый тег (см. команды выше)
3. **GitHub CLI не работает:** Используйте веб-интерфейс

---

**Готово!** После выполнения всех шагов release v7.1 будет доступен на GitHub.

