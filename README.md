# Orbita Icon Checker

Плагин для Figma, который проверяет реализацию иконок на соответствие требованиям дизайн-системы Orbita DS.

## Описание

Orbita Icon Checker автоматически проверяет иконки на соответствие техническим требованиям дизайн-системы Orbita DS. Плагин анализирует структуру компонентов, именование, размеры, варианты и другие параметры иконок, выводя список ошибок, которые нужно исправить.

## Как работает плагин

### Архитектура

Плагин состоит из двух основных частей:

1. **Backend (code.js)** - код, который выполняется в песочнице Figma и имеет доступ к API Figma для работы с документом
2. **Frontend (ui.html)** - пользовательский интерфейс на HTML/CSS/JS, который отображается пользователю

Части взаимодействуют между собой через систему сообщений (`postMessage`).

### Основной функционал

#### 1. Проверка иконок (Check Icons)

**Процесс работы:**

1. **Выбор компонентов**: Плагин сканирует выделенные объекты в Figma и рекурсивно находит все Component Set (наборы компонентов)
2. **Пакетная обработка**: Найденные компоненты разбиваются на пакеты по 30 штук для оптимальной производительности
3. **Проверка каждого компонента**: Для каждого Component Set и его вариантов выполняются проверки:
   - **Именование**: Соответствие схеме `orb-icon-[название]`
   - **Варианты**: Наличие свойств Variant (outline/solid) и Size (lg/md/sm/xs/xxs)
   - **Размеры**: Соответствие фиксированным размерам (32x32, 24x24, 16x16, 12x12, 8x8 px)
   - **Структура**: Наличие слоя Color-layer с вложенным Vector
   - **Constraints**: Выравнивание Color-layer по центру
   - **Vector**: Проверка блокировки, отсутствия обводки, использования кривых
   - **Группа Edit**: Наличие скрытой группы Edit с исходниками
   - **Description**: Наличие описания у вариантов
   - **Цветовые переменные**: Использование токена orb-icon для цвета

4. **Вывод результатов**: Обнаруженные ошибки группируются по компонентам и выводятся в UI с возможностью перехода к проблемному элементу

#### 2. AI Design Lint

**Процесс работы:**

1. **Экспорт дизайна**: Выделенные объекты экспортируются в PNG с разрешением 2x
2. **Сбор метаданных**: Собирается структура документа (слои, стили, типы объектов, ID элементов)
3. **Отправка в AI**: Изображение и метаданные отправляются в Yandex Eliza API с моделью GPT-4o Mini
4. **Анализ**: AI анализирует дизайн на основе кастомного промпта и правил дизайн-системы
5. **Получение рекомендаций**: AI возвращает детальные рекомендации с указанием ID проблемных элементов
6. **Интерактивный результат**: В UI отображаются рекомендации с возможностью перехода к элементам по клику на их ID

#### 3. Design System Validator ⭐ NEW

**Процесс работы:**

1. **Выбор режима**: Выбор типа variables для проверки (Local/Remote/All)
2. **Определение области**: Автоматическое определение — выделенные объекты или вся страница
3. **Загрузка Variables**: Получение списка Figma Variables через `figma.variables.getLocalVariablesAsync()`
4. **Батчинг**: Обработка нод пакетами по 50 штук для оптимальной производительности
5. **Проверка каждой ноды**: Для каждого элемента проверяются:
   - **Fill/Stroke** — использование цветовых токенов
   - **Effects** — тени и эффекты через variables
   - **Opacity** — прозрачность через токены
   - **Corner Radius** — скругления через variables
   - **Paddings** — внутренние отступы (Left/Right/Top/Bottom)
   - **Gap (itemSpacing)** — расстояние между элементами
   - **Width/Height** — размеры через токены
6. **Обнаружение проблем**:
   - **No Variable** — hardcoded значения (не использует токен)
   - **Missing Variable** — удалённые или недоступные токены
   - **Invalid Source** — несоответствие режиму проверки
   - **Deprecated Token** — устаревшие токены (`_deprecated`, `old-`)
7. **Детальный отчёт**: Сводка с статистикой, список проблем с навигацией, экспорт в JSON

**Особенности:**
- Асинхронная проверка с прогрессом в реальном времени
- Клик по проблеме → переход к элементу в Figma
- Стабильная работа на страницах с 1000+ элементами
- Экспорт отчёта в JSON для анализа

📚 **Полная документация:** [DESIGN_SYSTEM_VALIDATOR.md](./DESIGN_SYSTEM_VALIDATOR.md)  
🚀 **Быстрый старт:** [QUICK_START_DSV.md](./QUICK_START_DSV.md)

**Особенности:**
- Используется модель `gpt-4o-mini` через Yandex Eliza API
- Поддержка vision (анализ изображений)
- Настраиваемый промпт для разных типов анализа
- Автоматическая навигация по проблемным элементам

#### 3. Node ID Inspector

**Процесс работы:**

1. Пользователь выделяет объект в Figma
2. По нажатию кнопки плагин получает ID, имя и тип выделенного объекта
3. ID добавляется в текстовое поле для накопления
4. Функция "Сравнить ID" позволяет проверить, одинаковые ли ID у нескольких объектов

**Назначение**: Отладка и проверка идентичности объектов в Figma

#### 4. Экспорт в SVG

**Процесс работы:**

1. **Сканирование**: Находит все Component Set иконок в выделенных объектах
2. **Выбор параметров**: Пользователь выбирает варианты для экспорта (Variant, Size) и настройки цвета
3. **Экспорт**: Для каждого варианта:
   - Находит слой Color-layer или Vector
   - Экспортирует в SVG
   - Применяет цвет (черный или через токен)
   - Сохраняет с правильным именованием
4. **Упаковка**: Все SVG упаковываются в ZIP-архив
5. **Скачивание**: ZIP предлагается для скачивания через браузер

**Настройки экспорта:**
- Выбор вариантов (outline/solid)
- Выбор размеров (lg/md/sm/xs/xxs)
- Цвет: черный (#000000) или токен (currentColor)

### Автоматическое исправление ошибок

Плагин умеет автоматически исправлять некоторые типы ошибок:

- **Именование**: Исправление имен компонентов и слоев
- **Размеры**: Установка правильных размеров фреймов
- **Constraints**: Установка центрирования для Color-layer
- **Блокировка Vector**: Блокировка векторных слоев
- **Видимость Edit**: Скрытие группы Edit
- **Варианты**: Добавление/переименование свойств вариантов

Исправления можно применять:
- По одному (кнопка "Исправить" рядом с каждой ошибкой)
- Все сразу (кнопка "Исправить все ошибки")

### Технические особенности

- **Асинхронная обработка**: Использование `async/await` и `getNodeByIdAsync` для работы с большими документами
- **Прогресс-индикация**: Детальное отображение текущего этапа и процента выполнения
- **Пакетная обработка**: Разбиение на пакеты для предотвращения зависания UI
- **Обработка ошибок**: Подробные сообщения об ошибках с контекстом
- **Настраиваемость**: Возможность включать/выключать отдельные проверки
- **Фильтрация**: Автоматическое исключение компонентов с именами, начинающимися с точки

## Установка

1. Скачайте или клонируйте репозиторий
2. В Figma выберите **Plugins → Development → Import plugin from manifest...**
3. Выберите файл `manifest.json` из скачанного репозитория

## Использование

1. Откройте файл Figma с иконками
2. Запустите плагин **Plugins → Development → Orbita Icon Checker**
3. Нажмите кнопку **Проверить иконки**
4. Плагин отобразит индикатор прогресса с информацией о текущем этапе проверки
5. После завершения проверки плагин выведет список ошибок
6. Для перехода к компоненту с ошибкой, кликните на его название в списке

## Функции плагина

- **Индикатор прогресса**: Отображает текущий этап проверки и процент выполнения
- **Отладочная информация**: В случае ошибок плагин выведет подробную информацию о проблеме
- **Переход к компоненту**: Клик на название компонента в списке ошибок переместит фокус на этот компонент в Figma
- **Автоматическое исправление ошибок**: Кнопки "Исправить" для каждой ошибки и общая кнопка "Исправить все ошибки"
- **Настройка проверок**: Фильтр с чекбоксами для выбора, какие аспекты иконок проверять
- **Node ID Inspector**: Инструмент для получения и сравнения ID выделенных объектов в Figma

## Использование

1. Откройте файл Figma с иконками
2. Запустите плагин **Plugins → Development → Orbita Icon Checker**
3. Настройте параметры проверки, нажав на кнопку **Настройки проверки**
4. Нажмите кнопку **Проверить иконки**
5. Плагин отобразит индикатор прогресса с информацией о текущем этапе проверки
6. После завершения проверки плагин выведет список ошибок
7. Для перехода к компоненту с ошибкой, кликните на его название в списке
8. Для исправления конкретной ошибки, нажмите кнопку **Исправить** рядом с ошибкой
9. Для исправления всех ошибок сразу, нажмите кнопку **Исправить все ошибки**

### AI Design Lint

Для использования AI Design Lint:

1. Запустите плагин через **Plugins → Development → AI Design Lint**
2. При первом запуске:
   - Перейдите в настройки (кнопка шестерёнки)
   - Введите ваш Yandex OAuth Token
   - Сохраните настройки
3. Выделите компоненты в Figma для анализа
4. Опционально: добавьте дополнительные требования к анализу
5. Нажмите **Анализировать дизайн**
6. AI проанализирует дизайн и выдаст рекомендации с указанием ID проблемных элементов
7. Кликните на ID элемента в результатах для навигации к нему в Figma

### Node ID Inspector

Node ID Inspector позволяет получать и сравнивать идентификаторы объектов в Figma:

1. Выделите объект в Figma
2. Нажмите кнопку **Получить ID выделенного** - ID объекта будет отображен и автоматически добавлен в текстовое поле
3. Повторите процесс для других объектов
4. Нажмите кнопку **Сравнить ID** для проверки, одинаковые ли ID у выделенных объектов
5. Результат сравнения будет показан с цветовой индикацией (зеленый - одинаковые, красный - разные)

## Настройки проверок

Плагин позволяет настроить, какие аспекты иконок проверять:

- **Именование**: Проверка правильности именования иконок (orb-icon-[название_иконки])
- **Варианты**: Проверка свойств Variant (outline, solid) и Size (lg, md, sm, xs, xxs)
- **Размеры**: Проверка размеров иконок (32x32, 24x24, 16x16, 12x12, 8x8 px)
- **Структура**: Проверка наличия слоя Color-layer
- **Выравнивание**: Проверка выравнивания Color-layer по центру
- **Vector**: Проверка слоя Vector (блокировка, тип, цвет)
- **Группа Edit**: Проверка наличия и видимости группы Edit
- **Толщина линий**: Проверка толщины контура

## Проверяемые аспекты иконок

### 1. Структура
- Иконка должна быть Component Set
- Должен быть слой Color-layer, содержащий Vector
- Vector должен быть в кривых (не Stroke) и с Flatten

### 2. Размеры
- Размер холста иконки должен быть фиксированным (32x32, 24x24, 16x16, 12x12, 8x8 px)
- Слой Color-layer должен быть выровнен по центру (Constraints: Center Center)

### 3. Варианты (Variants)
- Должны быть реализованы через properties: Variant (outline, solid) и Size (lg, md, sm, xs, xxs)

### 4. Именование
- Название иконки должно соответствовать схеме: orb-icon-[название_иконки]
- Слои должны именоваться правильно: Color-layer, Vector

### 5. Цвет и стили
- Цвет иконки должен назначаться через Color-layer с токеном orb-icon
- Не должно быть цвета для слоя Vector

### 6. Lock Vector
- Векторные слои Vector должны быть заблокированы (Lock)

### 7. Исходник Edit
- Должна быть группа Edit, которая скрыта

### 8. Дополнительно
- Проверка толщины линий: для иконок 32, 24 px - 2 px, для 16, 12, 8 px - 1,5 px
- Отсутствие пустых и неиспользуемых слоев

## Требования к структуре иконок

```
orb-icon-name (Component Set)
 └─ Variant=outline, Size=md (Variant)
     └─ Color-layer (Union)
         └─ Vector (Icon Path) (Lock)
 └─ Edit (Group) (Invisible)
```

## Разработка

Для разработки плагина:

1. Клонируйте репозиторий
2. Внесите необходимые изменения в код
3. Обновите плагин в Figma, выбрав **Plugins → Development → Orbita Icon Checker → Update**

## Лицензия

Внутренний проект Яндекс 360