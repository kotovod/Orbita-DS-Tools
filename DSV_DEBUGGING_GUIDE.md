# Design System Validator - Debugging Guide

## 🐛 Отладка ложных ошибок "No Variable"

Если плагин сообщает, что токен не используется, хотя на самом деле он установлен, следуйте этому руководству.

---

## ✅ Что исправлено в v3.0.2

### 1. Улучшена логика проверки boundVariables

**Проблема:** Плагин неправильно проверял наличие токенов для массивов (fills, strokes, effects).

**Исправление:** Полностью переписана логика проверки:
- Каждое свойство проверяется отдельно
- Для массивов проверяется наличие видимых и непустых значений
- Для fills/strokes учитываются только SOLID типы (IMAGE/GRADIENT не поддерживают токены)

### 2. Добавлена проверка типов fills/strokes

**Теперь игнорируются:**
- IMAGE fills/strokes (изображения)
- GRADIENT fills/strokes (градиенты)  
- VIDEO fills (видео)

**Проверяются только:**
- SOLID fills/strokes (сплошные цвета)

### 3. Улучшено логирование

Добавлено детальное логирование для отладки:
- Примеры загруженных variables
- Информация о boundVariables для элементов с "DEBUG" в имени

---

## 🔍 Как проверить что видит плагин

### Способ 1: Проверка в Console

1. **Откройте Figma Dev Console:**
   - **Mac:** `Cmd + Option + I`
   - **Windows:** `Ctrl + Shift + I`

2. **Запустите проверку** в плагине

3. **Смотрите в Console:**

```javascript
// Вы увидите:
DSV: Начало проверки, режим: local, опции: {...}
DSV: Найдено нод для проверки: 156
DSV: Получено локальных variables: 45
DSV: Загружено variables: 45 шт.
DSV: Примеры загруженных variables: [
  { id: 'VariableID:123:456...', name: 'color/bg/primary', remote: false },
  { id: 'VariableID:789:012...', name: 'spacing/md', remote: false },
  ...
]
```

### Способ 2: DEBUG режим

1. **Переименуйте проблемный элемент** добавив `DEBUG` в название:
   ```
   Например: "Button/Primary" → "DEBUG Button/Primary"
   ```

2. **Запустите проверку**

3. **В Console увидите детальную информацию:**

```javascript
DSV: Проверка DEBUG Button/Primary -> fills: {
  hasBoundVariables: true,
  boundVar: [{id: 'VariableID:...', type: 'VARIABLE_ALIAS'}],
  actualValue: [{type: 'SOLID', color: {...}, visible: true}]
}

DSV: Проверка DEBUG Button/Primary -> strokes: {
  hasBoundVariables: true,
  boundVar: [{id: 'VariableID:...', type: 'VARIABLE_ALIAS'}],
  actualValue: [{type: 'SOLID', color: {...}, visible: true}]
}
```

---

## 🧪 Тестовые сценарии

### Тест 1: Stroke с токеном

**Создайте элемент:**
1. Добавьте Stroke
2. Привяжите к color variable
3. Назовите элемент "DEBUG Stroke Test"

**Запустите проверку → Откройте Console**

**Ожидаемый результат:**
```javascript
// В Console:
DSV: Проверка DEBUG Stroke Test -> strokes: {
  hasBoundVariables: true,
  boundVar: [{id: 'VariableID:...'}],  // ← Токен НАЙДЕН
  actualValue: [{type: 'SOLID', ...}]
}

// В отчёте: НЕТ ошибки для Stroke
```

### Тест 2: Stroke с GRADIENT (не поддерживает токены)

**Создайте элемент:**
1. Добавьте Stroke с GRADIENT
2. Назовите "Gradient Stroke"

**Запустите проверку**

**Ожидаемый результат:**
```
✅ НЕТ ошибки для Stroke (GRADIENT не поддерживает токены)
```

### Тест 3: Stroke с SOLID без токена

**Создайте элемент:**
1. Добавьте Stroke с SOLID цветом
2. НЕ привязывайте к variable
3. Назовите "DEBUG Hardcoded Stroke"

**Запустите проверку**

**Ожидаемый результат:**
```javascript
// В Console:
DSV: Проверка DEBUG Hardcoded Stroke -> strokes: {
  hasBoundVariables: true,
  boundVar: undefined,  // ← Токена НЕТ
  actualValue: [{type: 'SOLID', color: {r: 1, g: 0, b: 0}}]
}

// В отчёте:
❌ "Property Stroke не использует Design Token"
```

---

## 🔧 Частые проблемы

### Проблема 1: Ошибка для элемента с токеном

**Симптомы:**
```
❌ "Property Stroke не использует Design Token"
```
Но в Figma вы видите иконку токена 🔗

**Возможные причины:**

1. **Токен из неправильного режима:**
   - Проверьте режим проверки (Local/Remote/All)
   - Если токен remote, а режим Local → будет ошибка "Invalid Source"

2. **Токен удалён:**
   - Токен привязан, но сама variable удалена
   - Проверьте в Console: ищите "Missing Variable"

3. **Множественные fills/strokes:**
   - Если несколько fills, и один без токена → ошибка
   - Проверьте все fills в панели Design

**Решение:**
```javascript
// 1. Откройте Console
// 2. Найдите этот элемент в логах
// 3. Проверьте boundVar и actualValue
// 4. Сравните с ожидаемым
```

### Проблема 2: Remote variables не загружаются

**Симптомы:**
```
DSV: Загружено variables: 0 шт.
```
В режиме "Remote"

**Решение:**

1. **Проверьте подключение библиотеки:**
   - Assets → Libraries
   - Библиотека должна быть включена

2. **Проверьте Console:**
   ```javascript
   DSV: Найдена remote коллекция: LibraryName
   ```
   Если НЕТ таких сообщений → библиотека не подключена или нет remote variables

3. **Попробуйте режим "All":**
   - Запустите проверку в режиме "All"
   - Проверьте, загружаются ли variables

### Проблема 3: Слишком много ошибок

**Если плагин находит сотни ошибок:**

1. **Проверьте настройку Instance:**
   ```
   ☑ Пропускать вложенные Instance компоненты
   ```
   Включите, чтобы уменьшить количество проверок

2. **Проверьте режим:**
   - Если используете "All", попробуйте "Local" или "Remote"
   - Возможно, часть ошибок - это "Invalid Source"

3. **Используйте выделение:**
   - Выделите конкретный компонент
   - Запустите проверку только для него

---

## 📋 Чеклист отладки

Если плагин показывает ложную ошибку, пройдите по списку:

- [ ] **Открыл Console** (Cmd/Ctrl + Shift + I)
- [ ] **Добавил DEBUG** в название элемента
- [ ] **Запустил проверку**
- [ ] **Проверил в Console:**
  - [ ] Variables загружены (`DSV: Загружено variables: N`)
  - [ ] boundVar присутствует для проблемного свойства
  - [ ] actualValue не пустой
- [ ] **Проверил тип:**
  - [ ] Для fills/strokes: тип SOLID (не IMAGE/GRADIENT)
  - [ ] Для effects: visible !== false
- [ ] **Проверил режим:**
  - [ ] Local токен в Local режиме
  - [ ] Remote токен в Remote режиме
  - [ ] Или используется All режим
- [ ] **Проверил видимость:**
  - [ ] Элемент не скрыт
  - [ ] Fill/Stroke visible = true

---

## 📊 Интерпретация Console логов

### Правильный вывод (токен есть):

```javascript
DSV: Проверка Button -> strokes: {
  hasBoundVariables: true,           // ✅ boundVariables существует
  boundVar: [{                        // ✅ Привязка есть
    id: 'VariableID:1234:5678',      
    type: 'VARIABLE_ALIAS'
  }],
  actualValue: [{                     // ✅ Значение установлено
    type: 'SOLID',
    color: {r: 1, g: 0, b: 0},
    visible: true
  }]
}

// Результат: ✅ Ошибки НЕТ
```

### Проблемный вывод (токена нет):

```javascript
DSV: Проверка Button -> strokes: {
  hasBoundVariables: false,           // ❌ boundVariables нет
  boundVar: undefined,                // ❌ Привязки нет
  actualValue: [{                     // ⚠️ Значение есть, но без токена
    type: 'SOLID',
    color: {r: 1, g: 0, b: 0},
    visible: true
  }]
}

// Результат: ❌ Ошибка "No Variable"
```

### Градиент (нормально):

```javascript
DSV: Проверка Card -> fills: {
  hasBoundVariables: false,
  boundVar: undefined,
  actualValue: [{
    type: 'GRADIENT_LINEAR',          // ℹ️ GRADIENT не поддерживает токены
    // ...
  }]
}

// Результат: ✅ Ошибки НЕТ (GRADIENT игнорируется)
```

---

## 🚀 Следующие шаги

Если после всех проверок проблема осталась:

1. **Сделайте скриншот Console** с логами
2. **Сделайте скриншот элемента** в Figma
3. **Создайте issue** с деталями:
   - Что ожидали
   - Что получили
   - Console логи
   - Скриншоты

---

## 📚 Связанные документы

- [DSV_IMPROVEMENTS_v3.0.1.md](./DSV_IMPROVEMENTS_v3.0.1.md) — Предыдущие улучшения
- [DESIGN_SYSTEM_VALIDATOR.md](./DESIGN_SYSTEM_VALIDATOR.md) — Полная документация

---

**© 2025 Orbita Design System Team**

