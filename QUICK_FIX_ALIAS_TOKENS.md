# 🔧 Быстрое исправление: Кнопка "Привязать токен" не появляется

## Проблема

У вас токены-алиасы (один токен ссылается на другой), и кнопка "🔗 Привязать токен" не появляется.

## Решение за 3 шага

### ✅ Шаг 1: Переэкспортируйте токены

1. Откройте ваш Figma файл с токенами
2. Запустите **любой плагин** (например, Orbita Icon Checker)
3. Откройте **консоль разработчика**:
   - Mac: `Cmd + Option + I`
   - Windows: `Ctrl + Shift + I`
4. Откройте файл `export-tokens-script.js` (в папке плагина)
5. Скопируйте **весь код** из файла
6. Вставьте в консоль и нажмите `Enter`
7. Скопируйте полученный JSON
8. Сохраните в файл (например, `tokens.json`)

### ✅ Шаг 2: Загрузите новые токены

1. Откройте **Design System Validator**
2. Если токены уже загружены - нажмите **"🔄 Заменить"**
3. Если токенов нет - нажмите **"📁 Загрузить JSON"**
4. Выберите новый файл `tokens.json`

### ✅ Шаг 3: Проверьте

1. Запустите проверку DSV
2. Теперь кнопки **"🔗 Привязать токен"** должны появиться! ✨

---

## Что изменилось?

**Старый экспорт:**
```json
{
  "name": "color/primary",
  "value": "{alias: VariableID:...}"  ❌ Строка, не цвет!
}
```

**Новый экспорт:**
```json
{
  "name": "color/primary",
  "value": "{alias: VariableID:...}",
  "resolvedValue": {                   ✅ Реальный цвет!
    "r": 0.97,
    "g": 0.38,
    "b": 0.15
  }
}
```

---

## 🔍 Как проверить что работает?

Откройте консоль (`Cmd+Option+I`) и найдите логи:

✅ **Хороший лог:**
```
DSV: Алиас разрешён для "color/primary": {r: 0.97, g: 0.38, b: 0.15}
DSV findTokenByValue: ✅ Найден цветовой токен: color/primary
```

❌ **Плохой лог:**
```
DSV findTokenByValue: ❌ Токен не найден
```

Если видите плохой лог - проверьте что:
1. Новый JSON правильно загружен
2. В JSON есть `resolvedValue` для токенов-алиасов
3. Значения точно совпадают (например, цвет в элементе = цвет в токене)

---

## ⚡ Быстрый тест

1. Создайте прямоугольник с любым цветом
2. Создайте токен с таким же цветом
3. Экспортируйте токены новым скриптом
4. Загрузите в DSV
5. Запустите проверку
6. Должна появиться кнопка "🔗 Привязать токен" ✅

---

## 🆘 Если всё равно не работает

1. Откройте консоль
2. Скопируйте все логи начинающиеся с `DSV:`
3. Покажите разработчику

---

**Время на исправление:** ~2 минуты  
**Нужно делать один раз**  
**После этого кнопки будут работать всегда!** ✨

