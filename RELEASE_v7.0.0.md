# 🎉 Release v7.0.0 - Skip Hidden Objects

**Дата релиза:** 25 декабря 2025  
**Git tag:** `v7.0.0`  
**Commit:** `a2c36af`  

## ✨ Что нового

### Главная функция: Пропуск скрытых объектов

Добавлена настройка **"Пропускать скрытые объекты"** в Design System Validator:

✅ **Включена по умолчанию** - скрытые элементы автоматически не проверяются  
✅ **Рекурсивная проверка** - проверяется весь путь от объекта до PAGE  
✅ **Умная логика** - если родитель скрыт, все дети тоже пропускаются  
✅ **Работает с Instance** - Instance внутри скрытых Frame корректно пропускаются  

### Как использовать

1. **Автоматически работает** - просто запустите проверку как обычно
2. **Отключить** - снимите галочку "Пропускать скрытые объекты" в настройках
3. **Отладка** - откройте консоль DevTools для детальных логов

## 🐛 Исправленные проблемы

### До версии v7:
```
Frame (visible = false) ← скрыт
  └─ Instance (visible = true) ← ПРОВЕРЯЛСЯ ❌
```

### После версии v7:
```
Frame (visible = false) ← скрыт
  └─ Instance (visible = true) ← ПРОПУЩЕН ✅
```

## 🔧 Технические детали

### Изменения в коде

**code.js:**
- `isNodeOrParentHidden()` - новая функция проверки (строка 7124)
- Проверка в `checkNumericVariables()` (строка 916) 
- Проверка в `checkNodeVariables()` (строка 7169)
- Детальное диагностическое логирование

**ui.html:**
- Новый чекбокс `dsv-skip-hidden-objects` (строка 2660)
- Чтение настройки и передача в плагин (строка 4973)
- Диагностическое логирование

### Функция проверки иерархии

```javascript
function isNodeOrParentHidden(node) {
  let currentNode = node;
  while (currentNode) {
    if ('visible' in currentNode && currentNode.visible === false) {
      return true; // Найден скрытый родитель
    }
    if (currentNode.type === 'PAGE' || !currentNode.parent) {
      break;
    }
    currentNode = currentNode.parent; // Идём вверх по дереву
  }
  return false;
}
```

## 📚 Документация

Созданы новые документы:
- `docs/DSV_SKIP_HIDDEN_OBJECTS.md` - полное описание функции
- `docs/FIX_HIDDEN_OBJECTS_HIERARCHY.md` - рекурсивная проверка
- `docs/DEBUG_HIDDEN_OBJECTS.md` - инструкция по диагностике
- `CHANGELOG.md` - полная история изменений

## 📦 Установка и обновление

### Для новых пользователей:
```bash
git clone https://github.com/kotovod/Orbita-DS-Tools.git
# В Figma: Plugins → Development → Import plugin from manifest...
```

### Для существующих пользователей:
```bash
cd "Orbita DS Tools"
git pull origin main
git checkout v7.0.0
# В Figma: Plugins → Development → [Your Plugin] → Update
```

### Или просто обновите через git:
```bash
git pull origin main
```

## 🧪 Протестировано

✅ Скрытые Frame  
✅ Скрытые Group  
✅ Instance внутри скрытых объектов  
✅ Глубокая вложенность (5+ уровней)  
✅ Отключение настройки  
✅ Различные типы нод  

## ⚠️ Важные примечания

1. **Перезагрузка плагина обязательна** после обновления
2. **Обратная совместимость** полностью сохранена
3. **Диагностическое логирование** можно отключить после тестирования для производительности

## 📊 Статистика релиза

- **Изменено файлов:** 14
- **Добавлено строк:** 18,842+
- **Новых функций:** 1 главная + детальное логирование
- **Документации:** 4 новых файла
- **Исправлено багов:** 2 критичных

## 🔗 Ссылки

- **GitHub:** https://github.com/kotovod/Orbita-DS-Tools
- **Коммит:** https://github.com/kotovod/Orbita-DS-Tools/commit/a2c36af
- **Tag:** https://github.com/kotovod/Orbita-DS-Tools/releases/tag/v7.0.0
- **Изменения:** [CHANGELOG.md](CHANGELOG.md)

## 🎯 Следующие шаги

После установки v7.0.0:
1. Закройте и переоткройте плагин в Figma
2. Откройте Design System Validator
3. Проверьте что настройка "Пропускать скрытые объекты" включена (✓)
4. Запустите проверку - скрытые объекты будут пропущены!

---

**Спасибо за использование Orbita DS Tools!** 🎨

© 2025 Yandex · Orbita Design System

