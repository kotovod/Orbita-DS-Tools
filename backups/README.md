# 💾 Автоматические бэкапы

Эта папка содержит автоматические бэкапы, создаваемые перед откатом на другую версию.

## 📦 Структура

```
backups/
├── backup-20251015-143025/   ← Дата и время создания
│   ├── code.js
│   ├── ui.html
│   └── manifest.json
│
├── backup-20251015-150512/
│   └── ...
│
└── ...
```

## 🔄 Откат на бэкап

```bash
# Вернуться к бэкапу
cd "/Users/kotovod/Desktop/Figma Plugins/Orbita Icon Checker"
cp backups/backup-20251015-143025/* ./
```

## 🧹 Очистка старых бэкапов

```bash
# Удалить бэкапы старше 30 дней
find backups/ -type d -mtime +30 -exec rm -rf {} +

# Удалить все бэкапы кроме последних 10
ls -t backups/ | tail -n +11 | xargs -I {} rm -rf backups/{}
```

## 📊 Размер бэкапов

```bash
# Проверить размер всех бэкапов
du -sh backups/

# Показать размер каждого бэкапа
du -sh backups/*/
```

## ⚠️ Важно

- Бэкапы создаются автоматически при использовании `version-manager.sh restore`
- Рекомендуется периодически очищать старые бэкапы
- Бэкапы не версионируются в Git (см. `.gitignore`)

