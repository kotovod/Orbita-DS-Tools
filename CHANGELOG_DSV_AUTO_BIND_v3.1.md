# Changelog: DSV Auto-Bind Tokens v3.1

**Дата:** 16 октября 2025  
**Версия:** 3.1

## 🎉 Новая функциональность

### Автоматическое определение и привязка токенов

DSV теперь автоматически определяет подходящие токены для элементов без привязки и позволяет привязывать их одним кликом!

## ✨ Что добавлено

### 1. Умный поиск токенов по значению
- Система автоматически ищет токены, значения которых совпадают с текущими значениями элементов
- Работает для всех типов свойств: радиусы, отступы, цвета, размеры
- Высокая точность сравнения (±0.01 для чисел)
- Проверка релевантности токена по имени

### 2. Визуальные подсказки
- **💡 Найден подходящий токен** - зелёная панель с информацией о токене
- Отображение названия и значения токена
- Кнопка **🔗 Привязать токен** для быстрой привязки

### 3. Автоматическая привязка
- Привязка токена одним кликом
- Мгновенное обновление элемента в Figma
- Автоматическое удаление issue из списка проблем
- Уведомления об успехе/ошибке

## 📝 Изменённые файлы

### code.js
- **Модифицировано:** `checkNodeVariables()` - добавлен вызов `findTokenByValue()` для всех issues типа "No Variable"
- **Добавлено:** `bindTokenToProperty(nodeId, property, tokenId)` - функция привязки токена
- **Добавлено:** Обработчик сообщения `dsv-bind-token` для обработки запросов на привязку
- **Изменено:** Структура issue - добавлено поле `suggestedToken`

### ui.html
- **Модифицировано:** `createDSVIssueCard()` - добавлен параметр `issueIndex`, визуализация предложенного токена
- **Добавлено:** `formatTokenValue()` - форматирование значений токенов для отображения
- **Добавлено:** `bindTokenToIssue()` - отправка запроса на привязку токена
- **Добавлено:** `mapPropertyDisplayToKey()` - маппинг имён свойств UI → API
- **Добавлено:** Обработчик сообщения `dsv-bind-token-result` для обработки результатов

## 🔧 Технические улучшения

1. **Оптимизация поиска токенов:**
   - Использование существующей функции `findTokenByValue()`
   - Фильтрация по типу токена
   - Проверка релевантности по имени

2. **Улучшенная обработка ошибок:**
   - Проверка существования токена в Figma
   - Валидация доступности свойства
   - Понятные сообщения об ошибках

3. **UX улучшения:**
   - Визуальный feedback при привязке (кнопка меняет состояние)
   - Автоматическое обновление списка issues
   - Предотвращение двойных кликов

## 📊 Примеры использования

### До v3.1:
```
❌ Property "Corner Radius" не использует Design Token
→ Button
Property: Corner Radius
```

### После v3.1:
```
❌ Property "Corner Radius" не использует Design Token
→ Button
Property: Corner Radius

💡 Найден подходящий токен
radius/small = 8
[🔗 Привязать токен]  ← Новая кнопка!
```

## 🚀 Производительность

- Поиск токена выполняется во время валидации (не добавляет задержки)
- Привязка токена занимает < 100ms
- Batch-обработка не затронута

## 🐛 Известные ограничения

1. Токен должен существовать в Figma (не только в JSON)
2. Для cornerRadius токен должен иметь релевантное имя (содержать "radius", "corner" или "rounded")
3. Точность сравнения ±0.01 может не находить очень близкие значения

## 📦 Версионирование

- **v3.0-dsv-stable** - сохранена в `versions/v3.0-dsv-stable/`
- **v3.1** - текущая версия с auto-bind функциональностью

## ⚡ Быстрый старт

1. Загрузите токены через JSON import
2. Запустите DSV проверку
3. Если система найдёт подходящий токен - увидите зелёную панель
4. Нажмите "🔗 Привязать токен" - готово!

## 🎯 Следующие шаги

В будущих версиях планируется:
- Пакетная привязка всех найденных токенов
- Создание токенов из JSON прямо в Figma
- Настройка точности сравнения
- История привязок с отменой

## 📚 Документация

Полная документация: `DSV_AUTO_BIND_TOKENS.md`

---

**Разработано:** 16 октября 2025  
**Тестирование:** Рекомендуется протестировать на реальных проектах

