# Design System Validator

## 🎯 Описание

**Design System Validator** — это инструмент для проверки корректного использования Figma Variables (дизайн-токенов) в ваших компонентах и макетах.

## ✨ Возможности

### Основные функции

1. **Проверка Variables**
   - Автоматическая проверка всех визуальных свойств на использование дизайн-токенов
   - Выявление hardcoded значений (цвета, отступы, радиусы, размеры)
   - Обнаружение удалённых или недоступных variables
   - Определение устаревших токенов (deprecated)

2. **Гибкие режимы проверки**
   - **Local** — проверка только локальных variables
   - **Remote** — проверка только библиотечных variables
   - **All** — проверка всех variables (local + remote)

3. **Умное определение области**
   - Если объекты выделены → проверяются только выделенные элементы и их дети
   - Если ничего не выделено → проверяется вся текущая страница

4. **Производительность**
   - Асинхронная проверка с батчингом (пакеты по 50 нод)
   - Стабильная работа на больших страницах (1000+ элементов)
   - Отображение прогресса в реальном времени

5. **Детальные отчёты**
   - Сводная статистика проверки
   - Список всех найденных проблем
   - Классификация по типу и серьёзности
   - Экспорт отчёта в JSON

6. **Навигация**
   - Клик по проблеме → автоматический переход к элементу в Figma
   - Подсветка проблемного элемента
   - Визуальная обратная связь

---

## 🚀 Как использовать

### Запуск плагина

1. В Figma: **Plugins** → **Orbita Icons Checker** → **Design System Validator**
2. Откроется панель с интерфейсом валидатора

### Выбор режима проверки

В верхней части панели выберите режим:
- **Local** — для проверки локальных токенов (созданных в текущем файле)
- **Remote** — для проверки библиотечных токенов (из внешних библиотек)
- **All** — для проверки всех токенов

### Запуск проверки

1. **(Опционально)** Выделите элементы, которые хотите проверить
   - Если ничего не выделено — будет проверена вся страница
2. Нажмите кнопку **🔍 Проверить Variables**
3. Дождитесь завершения проверки (прогресс отображается внизу)

### Просмотр результатов

После завершения проверки отобразится:

#### Сводка
- **Проверено элементов** — общее количество проверенных нод
- **Найдено проблем** — количество обнаруженных issues
- **С токенами** — элементы, использующие variables
- **Без токенов** — элементы с hardcoded значениями

#### Список проблем

Каждая проблема содержит:
- **Тип** — категория проблемы (No Variable, Missing Variable, Invalid Source, Deprecated Token)
- **Элемент** — имя элемента с проблемой
- **Описание** — детальное описание проблемы
- **Property** — свойство, которое вызвало проблему

**Навигация:** Кликните на карточку проблемы, чтобы перейти к элементу в Figma.

### Экспорт отчёта

Нажмите **📄 Экспортировать отчёт (JSON)** для сохранения полного отчёта в формате JSON.

---

## 🔍 Какие свойства проверяются

Плагин проверяет следующие визуальные свойства на наличие привязки к Variables:

### Цвета и эффекты
- `fills` — заливка (Fill)
- `strokes` — обводка (Stroke)
- `effects` — эффекты (тени, размытие)
- `opacity` — прозрачность

### Размеры
- `width` — ширина
- `height` — высота

### Отступы и скругления
- `cornerRadius` — радиус скругления
- `paddingLeft` — внутренний отступ слева
- `paddingRight` — внутренний отступ справа
- `paddingTop` — внутренний отступ сверху
- `paddingBottom` — внутренний отступ снизу
- `itemSpacing` — расстояние между элементами (Gap)

---

## ⚠️ Типы проблем

### 1. **No Variable** (Средняя серьёзность)
- **Описание:** Свойство установлено, но не использует дизайн-токен
- **Пример:** У элемента задан `fill` цветом `#FF0000`, но он не привязан к variable
- **Рекомендация:** Привяжите свойство к соответствующему токену из дизайн-системы

### 2. **Missing Variable** (Высокая серьёзность)
- **Описание:** Variable, к которой привязано свойство, удалена или недоступна
- **Пример:** Элемент ссылается на variable с ID `123:456`, но она не существует
- **Рекомендация:** Обновите привязку на существующий токен или восстановите удалённый

### 3. **Invalid Source** (Средняя серьёзность)
- **Описание:** Variable существует, но не соответствует выбранному режиму проверки
- **Пример:** При режиме "Local" обнаружена привязка к remote variable
- **Рекомендация:** Убедитесь, что используются токены из правильного источника

### 4. **Deprecated Token** (Низкая серьёзность)
- **Описание:** Используется устаревший токен (содержит `_deprecated` или `old-`)
- **Пример:** Variable с именем `color-bg_deprecated` или `old-spacing-md`
- **Рекомендация:** Замените на актуальный токен из дизайн-системы

---

## 📊 Пример отчёта

### JSON структура
```json
{
  "mode": "local",
  "totalNodes": 156,
  "nodesChecked": 156,
  "nodesWithVariables": 98,
  "nodesWithoutVariables": 58,
  "totalIssues": 23,
  "issues": [
    {
      "type": "No Variable",
      "severity": "medium",
      "property": "Fill",
      "description": "Property \"Fill\" не использует Design Token",
      "nodeId": "123:456",
      "nodeName": "Button/Primary",
      "value": "{\"r\":1,\"g\":0,\"b\":0,\"a\":1}"
    },
    {
      "type": "Deprecated Token",
      "severity": "low",
      "property": "Padding Left",
      "description": "Используется устаревший токен \"spacing-old-lg\"",
      "nodeId": "789:012",
      "nodeName": "Card/Content",
      "variableId": "VariableID:345:678",
      "variableName": "spacing-old-lg"
    }
  ],
  "timestamp": "2025-10-16T12:34:56.789Z"
}
```

---

## 🛠️ Технические детали

### Архитектура
- **Frontend:** HTML/CSS/JavaScript в `ui.html`
- **Backend:** Figma Plugin API в `code.js`
- **Коммуникация:** `postMessage` между UI и plugin

### Производительность
- **Батчинг:** Обработка нод пакетами по 50 штук
- **Асинхронность:** Использование `async/await` для неблокирующих операций
- **Прогресс:** Обновление UI каждые 10 обработанных нод
- **Оптимизация:** Пропуск скрытых элементов и нулевых значений

### Алгоритм проверки

1. **Определение области:**
   - Проверка `figma.currentPage.selection`
   - Рекурсивный сбор дочерних нод

2. **Загрузка Variables:**
   - `figma.variables.getLocalVariablesAsync()`
   - Фильтрация по режиму (local/remote/all)

3. **Проверка нод:**
   - Итерация по всем нодам батчами
   - Проверка `boundVariables` для каждого свойства
   - Валидация привязок к variables

4. **Формирование отчёта:**
   - Сбор статистики
   - Классификация проблем по типу и severity
   - Экспорт в JSON

---

## 📝 Best Practices

### Для дизайнеров

1. **Регулярная проверка**
   - Запускайте валидатор перед передачей макетов в разработку
   - Проверяйте компоненты после обновления дизайн-системы

2. **Режим проверки**
   - Используйте **Local** для проверки локальных файлов
   - Используйте **Remote** для библиотек компонентов
   - Используйте **All** для полной проверки

3. **Приоритизация**
   - Сначала исправляйте **High** severity (удалённые variables)
   - Затем **Medium** severity (hardcoded значения, invalid sources)
   - В последнюю очередь **Low** severity (deprecated токены)

4. **Документирование**
   - Экспортируйте отчёт для анализа и отслеживания прогресса
   - Сохраняйте отчёты для сравнения до/после исправлений

### Для разработчиков дизайн-систем

1. **Именование variables**
   - Избегайте использования `_deprecated` и `old-` в активных токенах
   - Используйте понятные префиксы: `color-`, `spacing-`, `radius-`

2. **Миграция**
   - При обновлении токенов создавайте migration guide
   - Используйте deprecated-маркеры для старых токенов

3. **Документация**
   - Документируйте все токены дизайн-системы
   - Указывайте правильные режимы использования (local/remote)

---

## 🐛 Troubleshooting

### Проблема: "Проверено 0 элементов"
**Решение:** 
- Убедитесь, что на странице есть объекты
- Если используете выделение — проверьте, что объекты действительно выделены

### Проблема: "Плагин зависает на больших страницах"
**Решение:**
- Плагин использует батчинг, но для очень больших страниц (5000+ элементов) может потребоваться время
- Попробуйте выделить конкретную область для проверки
- Дождитесь завершения текущей проверки

### Проблема: "Ложные срабатывания для текстовых стилей"
**Решение:**
- Плагин не проверяет `fontSize` у текстовых нод, т.к. размер шрифта может быть частью текстового стиля
- Это ожидаемое поведение

### Проблема: "Не отображаются remote variables"
**Решение:**
- Убедитесь, что библиотека с remote variables подключена к файлу
- Проверьте доступ к библиотеке
- Используйте режим "All" для проверки всех variables

---

## 📦 Версия

**Текущая версия:** 3.0.0  
**Дата релиза:** 16 октября 2025

### История изменений

#### v3.0.0 (16.10.2025)
- ✨ Добавлен Design System Validator
- ✅ Поддержка проверки Figma Variables
- ✅ Три режима проверки (Local/Remote/All)
- ✅ Автоматическое определение области проверки
- ✅ Асинхронный батчинг для больших страниц
- ✅ Детальные отчёты с навигацией
- ✅ Экспорт отчётов в JSON
- ✅ Обнаружение deprecated токенов
- ✅ Проверка удалённых variables

---

## 🔗 Связанные документы

- [README.md](./README.md) — Основная документация плагина
- [DEVELOPMENT.md](./DEVELOPMENT.md) — Руководство для разработчиков
- [AI_DESIGN_LINT_IMPROVEMENTS.md](./AI_DESIGN_LINT_IMPROVEMENTS.md) — AI Design Lint функциональность

---

## 💬 Обратная связь

Если вы нашли баг или хотите предложить улучшение, пожалуйста, создайте issue в репозитории проекта.

---

**© 2025 Orbita Design System Team**

