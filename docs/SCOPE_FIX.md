# 🔧 Исправление: Фильтрация токенов по Scope в зависимости от типа элемента

**Дата:** 2 декабря 2024  
**Проблема:** Плагин предлагал токены `orb-text/*` для фреймов, хотя они применимы только к тексту  
**Причина:** Неправильная логика определения required scopes для fills

---

## 🐛 Проблема

### Что происходило:

При проверке заливки (fills) фрейма плагин предлагал токены с scope `TEXT_FILL`:

```
❌ Фрейм с цветом #F3F4F5
   Предложенный токен: orb-text/primary (TEXT_FILL)
   Результат: ❌ Нельзя применить к фрейму!
```

### Почему это неправильно:

В Figma переменные с разными scope применяются к разным типам элементов:

- **TEXT_FILL** → только TEXT
- **FRAME_FILL** → FRAME, COMPONENT, INSTANCE, SECTION
- **SHAPE_FILL** → RECTANGLE, ELLIPSE, POLYGON, VECTOR и т.д.
- **ALL_SCOPES** → любые элементы

**До исправления:** Для fills использовались ВСЕ scopes одновременно:
```javascript
'fills': ['FRAME_FILL', 'SHAPE_FILL', 'TEXT_FILL', 'ALL_SCOPES']
```

Это означало, что плагин искал токены со ВСЕМИ этими scope, включая TEXT_FILL для фреймов.

---

## ✅ Решение

### Динамическое определение scope по типу элемента

Теперь `requiredScopes` определяются в зависимости от типа node:

```javascript
// ВАЖНО: Корректируем scopes для fills в зависимости от типа элемента
if (propertyType === 'fills' && node) {
  const nodeType = node.type;
  
  // Для TEXT - только TEXT_FILL
  if (nodeType === 'TEXT') {
    requiredScopes = ['TEXT_FILL', 'ALL_SCOPES'];
    console.log(`✓ Тип элемента: TEXT → scope: TEXT_FILL`);
  }
  // Для FRAME, COMPONENT, INSTANCE, SECTION - только FRAME_FILL
  else if (['FRAME', 'COMPONENT', 'INSTANCE', 'SECTION', 'COMPONENT_SET'].includes(nodeType)) {
    requiredScopes = ['FRAME_FILL', 'ALL_SCOPES'];
    console.log(`✓ Тип элемента: ${nodeType} → scope: FRAME_FILL`);
  }
  // Для остальных (shapes: RECTANGLE, ELLIPSE, etc.) - только SHAPE_FILL
  else {
    requiredScopes = ['SHAPE_FILL', 'ALL_SCOPES'];
    console.log(`✓ Тип элемента: ${nodeType} → scope: SHAPE_FILL`);
  }
}
```

---

## 🎯 Результат

### До исправления:

**Тест-кейс 3:** Фрейм с цветом `#F86025`

```
❌ Найдено: orb-text/brand (TEXT_FILL)
   Нельзя применить к фрейму!
```

### После исправления:

**Тест-кейс 3:** Фрейм с цветом `#F86025`

```
✅ Найдено: orb-surface/brand/brand-hovered (FRAME_FILL)
   Можно применить к фрейму!
```

---

## 📋 Таблица соответствия типов элементов и scope

| Тип элемента | Required Scope | Примеры токенов |
|-------------|----------------|-----------------|
| **TEXT** | `TEXT_FILL` | `orb-text/primary`, `orb-text/secondary`, `orb-text/brand` |
| **FRAME** | `FRAME_FILL` | `orb-surface/generic`, `orb-elevation/base`, `orb-background/primary` |
| **COMPONENT** | `FRAME_FILL` | То же, что для FRAME |
| **INSTANCE** | `FRAME_FILL` | То же, что для FRAME |
| **SECTION** | `FRAME_FILL` | То же, что для FRAME |
| **RECTANGLE** | `SHAPE_FILL` | `orb-surface/accent/*`, shapes-токены |
| **ELLIPSE** | `SHAPE_FILL` | То же, что для RECTANGLE |
| **POLYGON** | `SHAPE_FILL` | То же, что для RECTANGLE |
| **VECTOR** | `SHAPE_FILL` | То же, что для RECTANGLE |
| **ALL** | `ALL_SCOPES` | Универсальные токены (если есть) |

---

## 🧪 Тестирование

### Test Case 3: Brand Frame Color

**Шаги:**
1. Создайте фрейм
2. Установите fill `#F86025` (без переменной)
3. Запустите проверку

**Ожидаемый результат:**
```
✓ Тип элемента: FRAME → scope: FRAME_FILL
✓ Требуемые scopes: [FRAME_FILL, ALL_SCOPES]
✅ Предложен токен: orb-surface/brand/brand-hovered
```

**НЕ должно быть:**
```
❌ orb-text/brand (TEXT_FILL)
```

### Test Case 3a: Brand Text Color

**Шаги:**
1. Создайте текстовый элемент
2. Установите fill `#F86025` (без переменной)
3. Запустите проверку

**Ожидаемый результат:**
```
✓ Тип элемента: TEXT → scope: TEXT_FILL
✓ Требуемые scopes: [TEXT_FILL, ALL_SCOPES]
✅ Предложен токен: orb-text/brand
```

**НЕ должно быть:**
```
❌ orb-surface/brand/brand-hovered (FRAME_FILL)
```

---

## 📝 Изменённые файлы

- **`code.js`** (строки ~1721-1750):
  - Добавлена логика динамического определения `requiredScopes`
  - Фильтрация по типу элемента для fills
  - Добавлены логи для отладки

---

## ⚠️ Важные замечания

### 1. ALL_SCOPES всегда включён

`ALL_SCOPES` всегда добавляется к `requiredScopes`, потому что токены с этим scope можно применить к любому элементу.

### 2. Приоритет scope

Если найдено несколько токенов с одинаковым цветом:
1. Сначала выбираются токены с точным scope (например, `FRAME_FILL` для фрейма)
2. Затем токены с `ALL_SCOPES`

### 3. Обратная совместимость

Для других свойств (padding, gap, cornerRadius и т.д.) логика не изменилась - они используют стандартные scope из `PROPERTY_TO_SCOPES`.

---

## 🚀 Следующие улучшения

### Потенциальные доработки:

1. **Strokes:** Добавить аналогичную логику для strokes (если нужно различать типы)
2. **Эффекты:** Расширить на effects (shadows, blurs)
3. **Кэширование:** Кэшировать определение типа элемента для оптимизации

---

*Исправление критического бага с scope фильтрацией*  
*Orbita DS Tools v3.x*  
*Декабрь 2024*

