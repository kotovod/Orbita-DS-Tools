# Dev vs Production Build

## 📦 Два манифеста

Проект использует два манифеста для разных окружений:

### `manifest.json` - Production (Community)
- **Назначение:** Публикация в Figma Community
- **Функции:** Только стабильные, готовые к production
- **Network Access:** Минимальный список доменов
- **Доступные команды:**
  - Проверить иконки
  - Экспорт иконок в SVG
  - Design System Validator β
  - Node ID Inspector
  - Экспорт свойств компонентов
  - Получить текст со страницы

### `manifest.dev.json` - Development (Локальная разработка)
- **Назначение:** Локальная разработка и тестирование
- **Функции:** Все функции, включая экспериментальные
- **Network Access:** Полный список доменов (включая AI API)
- **Дополнительные команды:**
  - ✨ **AI Design Lint** (в разработке)

---

## 🔧 Как работать

### Локальная разработка

1. **Используйте `manifest.dev.json`:**
   ```bash
   # В Figma: Plugins → Development → Import plugin from manifest
   # Выберите manifest.dev.json
   ```

2. **Все функции доступны**, включая AI Design Lint

3. **Имеет доступ к AI API:**
   - `https://api.eliza.yandex.net`
   - `https://events.statsigapi.net`

### Публикация в Community

1. **Используйте `manifest.json`:**
   ```bash
   # При публикации Figma использует manifest.json по умолчанию
   ```

2. **AI Design Lint скрыт** из меню

3. **Код сохранён** в `code.js` и `ui.html`

4. **Network Access ограничен** только необходимыми доменами

---

## 💡 Почему так?

### Преимущества подхода:

✅ **Безопасность**
- Не раскрываем API ключи и экспериментальные функции

✅ **Гибкость**
- Можно дорабатывать AI Design Lint локально
- Не нужно удалять/восстанавливать код

✅ **Простота**
- Один код, два манифеста
- Нет необходимости в build системе

✅ **Тестирование**
- Можно тестировать обе версии параллельно

---

## 🚀 Workflow

### Разработка новой функции

1. Добавляете функцию в код
2. Добавляете команду **только в `manifest.dev.json`**
3. Тестируете локально
4. Когда готова → переносите в `manifest.json`

### Публикация обновления

1. Проверяете, что `manifest.json` актуален
2. Убеждаетесь, что экспериментальные функции не в меню
3. Публикуете плагин в Community
4. Продолжаете разработку с `manifest.dev.json`

---

## 📝 Текущий статус

### Production (manifest.json)
- ✅ 6 стабильных функций
- ✅ Design System Validator β (в бета-тестировании)
- ❌ AI Design Lint (скрыт)

### Development (manifest.dev.json)
- ✅ Все production функции
- ✅ AI Design Lint (в активной разработке)

---

## 🔍 Технические детали

### Как это работает?

1. **Figma читает манифест** при загрузке плагина
2. **Формирует меню** на основе `menu` массива
3. **Запускает команды** через `figma.command`
4. **Код в `code.js`** содержит обработчики для ВСЕХ команд
5. **Если команды нет в манифесте** → она просто не показывается в меню

### Пример:

```javascript
// code.js - обработчик ВСЕГДА присутствует
} else if (figma.command === 'ai-design-lint') {
  // Этот код будет работать, если команда вызвана
  figma.showUI(__html__, { width: 400, height: 550 });
  // ...
}
```

```json
// manifest.json - команды НЕТ в меню (скрыта)
{
  "menu": [
    // "ai-design-lint" отсутствует
  ]
}
```

```json
// manifest.dev.json - команда ЕСТЬ в меню
{
  "menu": [
    {
      "name": "AI Design Lint",
      "command": "ai-design-lint"
    }
  ]
}
```

---

## ⚠️ Важно помнить

1. **Не коммитить API ключи** в production манифест
2. **Проверять networkAccess** перед публикацией
3. **Код можно оставлять** - он не будет доступен без команды в меню
4. **Тестировать обе версии** перед публикацией

---

## 🎯 Лучшие практики

### ✅ DO:
- Храните оба манифеста в git
- Документируйте различия
- Используйте комментарии в коде для dev-only функций
- Проверяйте размер bundle перед публикацией

### ❌ DON'T:
- Не удаляйте код dev-функций (просто скрывайте через манифест)
- Не забывайте переключаться между манифестами
- Не публикуйте dev манифест в Community
- Не оставляйте debug логи в production коде (это ок)

---

**Обновлено:** 30 ноября 2025

