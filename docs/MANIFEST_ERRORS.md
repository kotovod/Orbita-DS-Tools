# ⚠️ Figma Manifest - Типичные ошибки

## Проблема: "View Manifest Error"

Если Figma показывает ошибку при импорте манифеста, вот частые причины:

---

## ❌ Что НЕ работает

### 1. Произвольные поля

**НЕ работает:**
```json
{
  "networkAccess": {
    "allowedDomains": ["..."],
    "note": "Это комментарий"  ❌ Figma не поддерживает
  }
}
```

**Работает:**
```json
{
  "networkAccess": {
    "allowedDomains": ["..."]
  }
}
```

### 2. Комментарии в JSON

**НЕ работает:**
```json
{
  "name": "My Plugin",  // Это комментарий ❌
  "id": "123"
}
```

**Работает:**
```json
{
  "name": "My Plugin",
  "id": "123"
}
```

### 3. Trailing commas

**НЕ работает:**
```json
{
  "menu": [
    { "name": "Item 1" },
    { "name": "Item 2" },  ❌ Запятая в конце
  ]
}
```

**Работает:**
```json
{
  "menu": [
    { "name": "Item 1" },
    { "name": "Item 2" }
  ]
}
```

---

## ✅ Допустимые поля манифеста

### Обязательные:
- `name` - Название плагина
- `id` - ID плагина (строка)
- `api` - Версия API (обычно "1.0.0")
- `main` - Файл с кодом (обычно "code.js")

### Опциональные:
- `ui` - HTML файл для UI
- `editorType` - Массив редакторов ["figma", "figjam"]
- `networkAccess` - Настройки сетевого доступа
- `documentAccess` - Уровень доступа к документу
- `menu` - Массив команд для меню
- `relaunchButtons` - Кнопки для быстрого запуска
- `capabilities` - Дополнительные возможности

---

## 🔍 Как проверить манифест

### 1. Валидация JSON:

```bash
python3 -m json.tool manifest.json
```

Если нет ошибок - JSON синтаксически правильный.

### 2. Проверка структуры:

```bash
# Убедитесь что есть все обязательные поля
cat manifest.json | grep -E '"name"|"id"|"api"|"main"'
```

### 3. Проверка в Figma:

```
Figma → Plugins → Development → Import plugin from manifest
```

Если ошибка - смотрите console в Figma Desktop.

---

## 🛠️ Быстрое исправление

Если Figma выдает ошибку:

1. **Удалите все нестандартные поля** (note, comment, description и т.д.)
2. **Проверьте trailing commas** (запятые в конце массивов/объектов)
3. **Убедитесь что JSON валидный** (`python3 -m json.tool`)
4. **Проверьте что все строки в UTF-8** (особенно emoji)

---

## 📝 Правильная структура манифеста

```json
{
  "name": "Plugin Name",
  "id": "1234567890",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "networkAccess": {
    "allowedDomains": [
      "https://example.com"
    ]
  },
  "documentAccess": "dynamic-page",
  "menu": [
    {
      "name": "Command Name",
      "command": "command-id"
    }
  ]
}
```

---

## 💡 Где можно оставлять заметки?

Если нужны комментарии о манифесте:

1. **В README.md**
2. **В отдельном docs/MANIFEST.md**
3. **В коде (code.js) как комментарии**

**НЕ в самом manifest.json!**

---

## ✅ Исправлено в нашем проекте

**Было:**
```json
"networkAccess": {
  "allowedDomains": [...],
  "note": "AI Design Lint доступен только в dev версии"  ❌
}
```

**Стало:**
```json
"networkAccess": {
  "allowedDomains": [...]
}
```

**Заметка перенесена в:** `docs/DEV_VS_PRODUCTION.md`

---

**Дата:** 30 ноября 2025  
**Статус:** ✅ Исправлено

