# Структура дизайн-системы и алгоритм привязки токенов

## 📚 Структура дизайн-системы в variables.json

Дизайн-система Orbita построена по **трёхуровневой иерархической системе токенов**.

### 1️⃣ Коллекция "1. Theme" (базовые семантические токены)

**Назначение**: Основные цветовые токены с говорящими семантическими названиями

**Характеристики:**
- Два режима: **Light** и **Dark**
- Публичные переменные (`hiddenFromPublishing: false`)
- Это финальные токены, которые должны применяться к элементам дизайна

**Примеры токенов:**
- `orb-text/primary` - основной цвет текста
- `orb-text/secondary` - второстепенный цвет текста
- `orb-background/primary` - основной фон
- `orb-brand` - брендовый цвет
- `orb-icon/primary` - цвет иконок
- `orb-feedback/warning` - предупреждение
- `orb-feedback/success` - успех
- `orb-feedback/danger` - ошибка

**Структура данных:**
```json
{
  "orb-text/primary": {
    "key": "564e7843ef292ec39ec49a69a3839f2c9dc68b69",
    "id": "VariableID:36:14807",
    "scopes": ["SHAPE_FILL", "TEXT_FILL"],
    "hiddenFromPublishing": false,
    "description": "",
    "Light": "#34343C",
    "Dark": "#F3F4F5"
  }
}
```

### 2️⃣ Коллекция "2. Product" (промежуточный слой)

**Назначение**: Связующее звено между контекстными токенами и базовыми

**Характеристики:**
- Скрытые переменные (`hiddenFromPublishing: true`)
- Режимы продуктов: `Mail, Calendar, Disc, Yango`, `Docs`, и др.
- Служит для маппинга между продуктовыми контекстами и темой

**Роль в системе:**
Промежуточный слой, который связывает специфичные для продуктов токены из коллекций 2.X с базовыми семантическими токенами из 1.Theme.

### 3️⃣ Коллекции "2.1", "2.2", "2.3"... (контекстные токены)

**Назначение**: Токены для конкретных продуктовых контекстов

**Характеристики:**
- Скрытые переменные (`hiddenFromPublishing: true`)
- Режимы типа: `Mail`, `Docs`, `Admin`, `Messenger`
- Специализированные токены для разных продуктов Orbita

**Примеры коллекций:**
- `2.1` - для Mail
- `2.2` - для Docs
- `2.3` - для Admin
- И т.д.

### 📊 Другие коллекции (числовые токены)

**3. Spacing** - отступы и gap
- Значения: 0, 1, 2, 4, 6, 8, 10, 12, 16, 20, 24, 32, 40, 48, 56, 64, 72, 80...
- Именование: `0m`, `05m`, `1m`, `2m`, `3m`, `4m`, `5m`, `6m`, `8m`...

**4. Rounding** - скругления углов
- Значения: 0, 2, 4, 6, 8, 12, 16, 24
- Именование: `none`, `2xs`, `xs`, `sm`, `md`, `lg`, `xl`, `full`

**5. Sizing** - размеры
- Значения для ширины/высоты элементов

**6. Line** - толщина линий
- Значения для strokeWeight

---

## 🔍 Алгоритм поиска и привязки токенов

Когда плагин находит hardcoded значение (например, цвет `#34343C` или отступ `16px`), он применяет **многоступенчатый алгоритм** выбора подходящего токена.

### Для цветовых токенов

#### ШАГ 1 (ПРИОРИТЕТ): Поиск через скрытые переменные 2.X → 1.Theme

Это основной и наиболее точный метод, который учитывает продуктовый контекст.

**1. Определение контекста объекта:**
```
Объект находится в режиме Mail → используем коллекцию 2.1
Объект находится в режиме Docs → используем коллекцию 2.2
Объект находится в режиме Admin → используем коллекцию 2.3
```

**2. Поиск в скрытых переменных 2.X:**
- Ищет совпадение HEX-кода в **активном режиме объекта** (например, Mail)
- Если нашёл совпадение - берёт цвет **первого режима** этой переменной
- Проверяет правила применения (текст/линия/заливка)

**3. Промежуточный шаг через 2. Product:**
- Берёт полученный цвет из первого режима 2.X
- Ищет его в коллекции "2. Product"
- Если нашёл - берёт первый режим из 2. Product
- Если не нашёл - использует цвет из 2.X

**4. Финальный поиск в 1.Theme:**
- Ищет полученный цвет в коллекции "1.Theme"
- Ищет в режиме объекта (Light или Dark)
- Возвращает публичный токен из 1.Theme с семантическим названием

**Пример цепочки:**
```
Hardcoded #34343C в режиме Mail + Light
  ↓
Найден в 2.1/hidden-token (режим Mail) = #34343C
  ↓
Первый режим 2.1/hidden-token = #34343C
  ↓
Найден в 2. Product/intermediate (первый режим) = #34343C
  ↓
Первый режим 2. Product/intermediate = #34343C
  ↓
Найден в 1.Theme/orb-text/primary (режим Light) = #34343C
  ↓
РЕЗУЛЬТАТ: Применяем токен "orb-text/primary" из 1.Theme
```

#### ШАГ 1.5: Прямой поиск в 2. Product

Если ШАГ 1 не дал результата:

**1. Поиск в скрытых переменных 2. Product:**
- Ищет совпадение HEX в **первом режиме** коллекции 2. Product
- Проверяет правила применения

**2. Затем поиск в 1.Theme:**
- По той же логике, что и в ШАГ 1
- Ищет в режиме объекта (Light/Dark)

#### ШАГ 2 (ЗАПАСНОЙ): Прямой поиск в 1.Theme

Если предыдущие шаги не дали результата:

**1. Прямой поиск в 1.Theme:**
- Ищет совпадение **напрямую** в коллекции 1.Theme
- Только в **видимых** переменных (`hiddenFromPublishing: false`)
- Только в **режиме объекта** (Light или Dark)
- Применяет фильтрацию по scopes и правилам

**Этот шаг используется когда:**
- Hardcoded цвет не проходит через продуктовые слои
- Это прямое использование базового семантического цвета
- Например, статичный белый/чёрный цвет

### Для числовых токенов (spacing, sizing, rounding, line)

#### Алгоритм поиска числовых значений:

**1. Определение целевой группы:**
```javascript
PROPERTY_TO_GROUP = {
  'paddingLeft': 'Spacing',
  'paddingRight': 'Spacing',
  'paddingTop': 'Spacing',
  'paddingBottom': 'Spacing',
  'itemSpacing': 'Spacing',
  'cornerRadius': 'Rounding',
  'topLeftRadius': 'Rounding',
  'strokeWeight': 'Line',
  'width': 'Sizing',
  'height': 'Sizing'
}
```

**2. Фильтрация по scopes:**
```javascript
PROPERTY_TO_SCOPES = {
  'paddingLeft': ['ALL_SCOPES', 'GAP'],
  'cornerRadius': ['ALL_SCOPES', 'CORNER_RADIUS'],
  'strokeWeight': ['ALL_SCOPES', 'STROKE_FLOAT'],
  'width': ['ALL_SCOPES', 'WIDTH_HEIGHT']
}
```

**3. Поиск ближайшего значения:**

**а) Точное совпадение (приоритет #1):**
```
Hardcoded: 16px
Найден токен: 4m = 16
РЕЗУЛЬТАТ: Применяем "4m"
```

**б) Ближайшее большее (приоритет #2):**
```
Hardcoded: 15px
Нет точного совпадения
Ближайшее большее: 4m = 16
РЕЗУЛЬТАТ: Применяем "4m"
```

**в) Ближайшее меньшее (приоритет #3):**
```
Hardcoded: 17px
Нет точного совпадения
Нет ближайшего большего
Ближайшее меньшее: 4m = 16
РЕЗУЛЬТАТ: Применяем "4m"
```

---

## 🎯 Как выбирается токен из нескольких с одинаковым HEX?

Когда найдено **несколько токенов с одинаковым HEX-кодом**, система применяет **многоуровневую фильтрацию**:

### 1. Фильтрация по Scopes (область применения)

**Типы scopes:**
- `SHAPE_FILL` - заливка фигур
- `TEXT_FILL` - заливка текста
- `STROKE_COLOR` - цвет обводки
- `FRAME_FILL` - заливка фреймов
- `CORNER_RADIUS` - скругления
- `GAP` - отступы и gap
- `STROKE_FLOAT` - толщина обводки
- `WIDTH_HEIGHT` - размеры
- `ALL_SCOPES` - универсальная переменная

**Логика проверки:**
```javascript
function isScopeCompatible(variableScopes, requiredScopes) {
  // Если у переменной нет scopes или ALL_SCOPES - она универсальная
  if (!variableScopes || variableScopes.length === 0) return true;
  if (variableScopes.includes('ALL_SCOPES')) return true;
  
  // Проверяем пересечение scopes
  return requiredScopes.some(reqScope => variableScopes.includes(reqScope));
}
```

**Пример:**
```
Проверяем fills текстового элемента
Требуемые scopes: ['TEXT_FILL', 'ALL_SCOPES']

Токен A: scopes = ['SHAPE_FILL'] → ❌ Не подходит
Токен B: scopes = ['TEXT_FILL'] → ✅ Подходит
Токен C: scopes = ['ALL_SCOPES'] → ✅ Подходит
Токен D: scopes = [] → ✅ Подходит (универсальная)
```

### 2. Правила применения по имени токена

**Проверка имени переменной:**
```javascript
function canApplyColorVariableToNode(variableName, node) {
  // Переменные с "-line-" применяются только к линиям/обводкам
  if (variableName.includes('-line-')) {
    return node.type === 'LINE' || isStrokeProperty;
  }
  
  // Переменные с "-text-" применяются к текстам
  if (variableName.includes('-text-')) {
    return node.type === 'TEXT' || isTextProperty;
  }
  
  // Остальные - универсальные
  return true;
}
```

**Примеры:**
- `orb-line-primary` → применяется только к линиям и обводкам
- `orb-text-primary` → применяется только к текстам
- `orb-background-primary` → универсальная переменная

### 3. Приоритет коллекций

Если после всех фильтров осталось несколько токенов, выбирается по приоритету коллекции:

```javascript
function getCollectionPriority(collectionName) {
  if (collectionName.startsWith('1.')) return 1; // Высший приоритет
  if (collectionName.startsWith('2.') && !collectionName.match(/^2\.\d+/)) return 2;
  if (collectionName.match(/^2\.\d+/)) return 3; // Низший приоритет
  return 999;
}
```

**Приоритет:**
1. **1. Theme** - высший (публичные семантические токены)
2. **2. Product** - средний (промежуточный слой)
3. **2.X** - низший (контекстные токены)

### 4. Учёт режима объекта

Плагин определяет режим, в котором находится объект:

**Для коллекции 1.Theme:**
- `Light` - светлая тема
- `Dark` - тёмная тема

**Для коллекций 2.X:**
- `Mail` - режим почты
- `Docs` - режим документов
- `Admin` - режим администратора
- `Messenger` - режим мессенджера
- И т.д.

**Алгоритм определения режима:**
```javascript
// 1. Проверяем boundVariables объекта
// 2. Проходим вверх по дереву до родителя
// 3. Ищем применённые переменные из нужной коллекции
// 4. Определяем активный режим
```

**Поиск токена всегда происходит в активном режиме объекта.**

---

## 📋 Резюме: Полный порядок приоритетов

### Для цветовых токенов:

```
1. ШАГ 1 (ПРИОРИТЕТ)
   ├─ Определить режим 2.X объекта (Mail/Docs/Admin...)
   ├─ Найти совпадение в скрытых токенах 2.X (активный режим)
   ├─ Взять первый режим найденной переменной 2.X
   ├─ Найти это значение в 2. Product (первый режим)
   ├─ Взять первый режим из 2. Product
   └─ Найти в 1.Theme (режим Light/Dark объекта) → РЕЗУЛЬТАТ

2. ШАГ 1.5 (ПРОМЕЖУТОЧНЫЙ)
   ├─ Найти совпадение в скрытых токенах 2. Product (первый режим)
   └─ Найти в 1.Theme (режим Light/Dark объекта) → РЕЗУЛЬТАТ

3. ШАГ 2 (ЗАПАСНОЙ)
   └─ Найти напрямую в 1.Theme (режим Light/Dark объекта) → РЕЗУЛЬТАТ

4. Если ничего не найдено
   └─ Вернуть null (токен не найден)
```

### Для числовых токенов:

```
1. Определить целевую группу (Spacing/Rounding/Sizing/Line)
2. Собрать все токены из этой группы
3. Фильтровать по scopes
4. Найти:
   ├─ Точное совпадение (приоритет #1)
   ├─ Ближайшее большее (приоритет #2)
   └─ Ближайшее меньшее (приоритет #3)
```

---

## 🎯 Преимущества такой системы

### 1. Контекстность
Токены выбираются с учётом продуктового контекста (Mail, Docs, Admin), что обеспечивает правильное применение цветов для каждого продукта.

### 2. Семантичность
Финальные токены всегда из коллекции 1.Theme с понятными семантическими именами (`orb-text/primary`, `orb-background/primary`), что упрощает поддержку.

### 3. Гибкость
Если нет контекстного токена в 2.X, система использует базовый токен из 1.Theme, обеспечивая fallback.

### 4. Масштабируемость
Легко добавлять новые продукты (новые коллекции 2.X) без изменения базовой структуры.

### 5. Единообразие
Все элементы используют токены из единой системы, что гарантирует визуальное единообразие.

---

## 🔧 Технические детали

### Структура данных токена в JSON:

```json
{
  "key": "564e7843ef292ec39ec49a69a3839f2c9dc68b69",      // Уникальный ключ
  "id": "VariableID:36:14807",                            // ID переменной в Figma
  "scopes": ["SHAPE_FILL", "TEXT_FILL"],                  // Области применения
  "hiddenFromPublishing": false,                          // Скрыта ли переменная
  "description": "",                                      // Описание
  "Light": "#34343C",                                     // Значение для Light режима
  "Dark": "#F3F4F5"                                       // Значение для Dark режима
}
```

### Формат значений цвета:

Цвета могут быть представлены в нескольких форматах:
- `#FFFFFF` - HEX без прозрачности
- `#FFFFFF (80%)` - HEX с прозрачностью в процентах
- `#B5B7C0 (80%)` - HEX с прозрачностью

### Парсинг цвета:

```javascript
function parseColorString(colorString) {
  // Извлекает HEX и opacity из строки
  // Возвращает: { hex: "#FFFFFF", opacity: 0.8 }
}
```

### Сравнение цветов:

```javascript
function colorsMatch(hex1, opacity1, hex2, opacity2) {
  // Сравнивает с учётом допуска в 1% для opacity
  return hex1.toUpperCase() === hex2.toUpperCase() && 
         Math.abs(opacity1 - opacity2) < 0.01;
}
```

---

## 📖 Примеры использования

### Пример 1: Цвет текста в режиме Mail

```
Дано:
- Элемент TEXT с hardcoded цветом #34343C
- Объект находится в режиме Mail (коллекция 2.1)
- Тема Light

Алгоритм:
1. Определяем режим: Mail → коллекция 2.1
2. Ищем #34343C в скрытых токенах 2.1 (режим Mail)
3. Находим: 2.1/hidden-mail-text-primary = #34343C (Mail режим)
4. Берём первый режим: Mail = #34343C
5. Ищем в 2. Product (первый режим): находим intermediate-text
6. Берём первый режим 2. Product: #34343C
7. Ищем в 1.Theme (Light режим): находим orb-text/primary = #34343C
8. РЕЗУЛЬТАТ: Применяем "orb-text/primary"
```

### Пример 2: Отступ padding

```
Дано:
- Элемент FRAME с paddingLeft = 16px
- Без привязки к переменной

Алгоритм:
1. Определяем группу: paddingLeft → Spacing
2. Собираем все токены из группы Spacing
3. Фильтруем по scopes: ['ALL_SCOPES', 'GAP']
4. Ищем точное совпадение: 4m = 16 ✅
5. РЕЗУЛЬТАТ: Применяем "4m" (16px)
```

### Пример 3: Скругление углов

```
Дано:
- Элемент FRAME с cornerRadius = 8px
- Без привязки к переменной

Алгоритм:
1. Определяем группу: cornerRadius → Rounding
2. Собираем все токены из группы Rounding
3. Фильтруем по scopes: ['ALL_SCOPES', 'CORNER_RADIUS']
4. Ищем точное совпадение: md = 8 ✅
5. РЕЗУЛЬТАТ: Применяем "md" (8px)
```

---

## 🐛 Отладка

Плагин выводит подробные логи в консоль Figma:

```javascript
console.log(`🎨 Ищем цветовую переменную для fills: #34343C (opacity: 100%)`);
console.log(`✓ Требуемые scopes: [TEXT_FILL, ALL_SCOPES]`);
console.log(`✓ Режим 1.Theme для объекта: Light`);
console.log(`✓ Найдено 817 цветовых переменных`);
console.log(`📍 ШАГ 1 (ПРИОРИТЕТ): Ищем скрытые переменные в 2.X по hex`);
console.log(`✓ Найдено 3 коллекций 2.X: 2.1, 2.2, 2.3`);
console.log(`✅ Применена коллекция: "2.1", режим: "Mail"`);
console.log(`✅ Найдено совпадение: hidden-mail-text-primary`);
console.log(`✅ Найдена переменная в 1.Theme: orb-text/primary`);
console.log(`✅ ШАГ 1: Применяем токен orb-text/primary`);
```

Для просмотра логов: `Figma → Plugins → Development → Open Console`

---

*Документация актуальна для версии плагина 3.x*
*Последнее обновление: Декабрь 2024*

