# Design System Validator - Improvements v3.0.1

## 📦 Улучшения функциональности

**Дата:** 16 октября 2025  
**Версия:** 3.0.1

---

## ✨ Что исправлено

### 1. ✅ Исключена проверка COMPONENT_SET

**Проблема:** COMPONENT_SET (родительский фрейм компонента) проверялся на наличие токенов, что генерировало ложные ошибки.

**Решение:**
```javascript
// В функции checkNodeVariables
if (node.type === 'COMPONENT_SET') {
  return; // Пропускаем проверку
}
```

**Эффект:** COMPONENT_SET теперь игнорируется при проверке, проверяются только вложенные варианты компонента.

---

### 2. ✅ Улучшена логика проверки опциональных свойств

**Проблема:** Свойства `effects`, `opacity`, `width`, `height` генерировали ошибки даже когда они не были установлены.

**Решение:** Разделены свойства на **обязательные** и **опциональные**:

#### Обязательные свойства (должны иметь токен если заданы):
- `fills` — Fill
- `strokes` — Stroke
- `cornerRadius` — Corner Radius
- `paddingLeft/Right/Top/Bottom` — Paddings
- `itemSpacing` — Gap

#### Опциональные свойства (ОК если отсутствуют, но если заданы — нужен токен):
- `effects` — Effects
- `opacity` — Opacity (дефолт = 1)
- `width` — Width
- `height` — Height

**Реализация:**
```javascript
const propertiesToCheck = [
  // Обязательные
  { key: 'fills', displayName: 'Fill', optional: false },
  { key: 'strokes', displayName: 'Stroke', optional: false },
  // ...
  
  // Опциональные
  { key: 'effects', displayName: 'Effect', optional: true },
  { key: 'opacity', displayName: 'Opacity', optional: true },
  { key: 'width', displayName: 'Width', optional: true },
  { key: 'height', displayName: 'Height', optional: true }
];
```

**Функция проверки значений:**
```javascript
function hasPropertyValue(node, property) {
  // Для effects - проверяем массив
  if (property === 'effects') {
    return Array.isArray(value) && value.length > 0 && value.some(e => e.visible !== false);
  }
  
  // Для opacity - проверяем что не равно 1 (дефолт)
  if (property === 'opacity') {
    return value !== undefined && value !== 1;
  }
  
  // Для width/height - проверяем что установлены явно
  if (property === 'width' || property === 'height') {
    return typeof value === 'number' && value > 0;
  }
  
  return value !== undefined && value !== null && value !== 0;
}
```

**Эффект:** 
- ❌ **Было:** "Effect не использует токен" даже если effects нет
- ✅ **Стало:** Ошибка только если effect **есть**, но без токена

---

### 3. ✅ Добавлена настройка для проверки Instance

**Проблема:** Instance компонентов внутри других компонентов всегда проверялись, что могло быть избыточным.

**Решение:** Добавлен чекбокс в UI:

```html
<label>
  <input type="checkbox" id="dsv-skip-instances">
  <span>Пропускать вложенные Instance компоненты</span>
</label>
```

**Логика:**
```javascript
// В функции checkNodeVariables
if (node.type === 'INSTANCE' && options.skipInstances) {
  return; // Пропускаем проверку Instance
}
```

**Передача в плагин:**
```javascript
parent.postMessage({
  pluginMessage: {
    type: 'dsv-validate',
    mode: dsvValidationMode,
    options: {
      skipInstances: skipInstances // true/false
    }
  }
}, '*');
```

**Эффект:** Пользователь может контролировать, нужно ли проверять Instance внутри компонентов.

---

### 4. ✅ Исправлена работа с Remote Variables

**Проблема:** Remote variables из подключённых библиотек не загружались корректно.

**Старая логика:**
```javascript
const allVariables = await figma.variables.getLocalVariablesAsync();
// Попытка определить remote по resolvedType (неверно)
const isLocal = variable.resolvedType !== undefined;
```

**Новая логика:**
```javascript
async function getVariablesByMode(mode) {
  const variablesMap = new Map();
  
  // 1. Получаем локальные variables
  const localVariables = await figma.variables.getLocalVariablesAsync();
  for (const variable of localVariables) {
    if (mode === 'all' || mode === 'local') {
      variablesMap.set(variable.id, variable);
    }
  }
  
  // 2. Получаем remote variables из коллекций
  if (mode === 'remote' || mode === 'all') {
    const collections = await figma.variables.getLocalVariableCollectionsAsync();
    
    for (const collection of collections) {
      // Проверяем флаг remote
      if (collection.remote === true) {
        console.log('Найдена remote коллекция:', collection.name);
        
        // Получаем variables из коллекции
        for (const varId of collection.variableIds) {
          const variable = await figma.variables.getVariableByIdAsync(varId);
          if (variable && variable.remote === true) {
            variablesMap.set(variable.id, variable);
          }
        }
      }
    }
  }
  
  return variablesMap;
}
```

**Эффект:**
- ✅ Корректное определение remote variables через флаг `collection.remote`
- ✅ Загрузка variables из библиотек
- ✅ Логирование для отладки

---

## 📊 Итоговые изменения

### code.js

**Изменено функций:** 3
- `validateDesignSystem()` — добавлен параметр `options`
- `getVariablesByMode()` — полностью переписана логика
- `checkNodeVariables()` — добавлена фильтрация COMPONENT_SET и INSTANCE

**Добавлено функций:** 1
- `hasPropertyValue()` — проверка значений опциональных свойств

**Изменено строк:** ~80

### ui.html

**Добавлено элементов:** 1
- Чекбокс "Пропускать вложенные Instance"

**Изменено функций:** 1
- `initDesignSystemValidator()` — добавлена передача настроек

**Изменено строк:** ~30

---

## 🧪 Тестирование

### Что нужно проверить

1. **COMPONENT_SET не проверяется**
   - Создайте компонент с вариантами
   - Запустите проверку
   - ✅ COMPONENT_SET не должен генерировать ошибки

2. **Опциональные свойства**
   - Создайте элемент **без** effects
   - ✅ Ошибка "No Variable" для effects **не должна** появиться
   - Добавьте effect без токена
   - ❌ Ошибка "No Variable" **должна** появиться

3. **Настройка Instance**
   - Включите чекбокс "Пропускать Instance"
   - Запустите проверку на компоненте с вложенными Instance
   - ✅ Instance не должны генерировать ошибки

4. **Remote Variables**
   - Подключите библиотеку с remote variables
   - Используйте remote variable в элементе
   - Запустите проверку в режиме "Remote"
   - ✅ Remote variable должна валидироваться
   - ✅ Не должно быть ошибки "Invalid Source"

---

## 📝 Использование

### Новая настройка

В UI появился новый чекбокс в секции **Настройки**:

```
☐ Пропускать вложенные Instance компоненты
```

**Когда включать:**
- Если вы используете много Instance внутри компонентов
- Если Instance наследуют токены от родительского компонента
- Если хотите уменьшить количество проверяемых элементов

**Когда выключать:**
- Если хотите полную проверку всех элементов
- Если Instance могут иметь собственные overrides
- Для максимально строгой проверки

---

## 🐛 Исправленные баги

1. ❌ **Было:** COMPONENT_SET генерирует ошибки  
   ✅ **Стало:** COMPONENT_SET пропускается

2. ❌ **Было:** "Effect не использует токен" когда effects нет  
   ✅ **Стало:** Ошибка только если effect есть, но без токена

3. ❌ **Было:** "Opacity не использует токен" при opacity=1  
   ✅ **Стало:** Ошибка только если opacity ≠ 1 и без токена

4. ❌ **Было:** Remote variables не загружаются  
   ✅ **Стало:** Remote variables загружаются из коллекций

5. ❌ **Было:** Instance всегда проверяются  
   ✅ **Стало:** Можно пропустить через настройку

---

## 📄 API Changes

### validateDesignSystem

**Было:**
```javascript
async function validateDesignSystem(mode)
```

**Стало:**
```javascript
async function validateDesignSystem(mode, options = {})

// options: {
//   skipInstances: boolean  // Пропускать Instance
// }
```

### checkNodeVariables

**Было:**
```javascript
async function checkNodeVariables(node, variables, mode, report)
```

**Стало:**
```javascript
async function checkNodeVariables(node, variables, mode, report, options = {})

// options: {
//   skipInstances: boolean  // Пропускать Instance
// }
```

---

## 🎯 Совместимость

- ✅ **Обратная совместимость:** Полная
- ✅ **Breaking changes:** Нет
- ✅ **Требуется миграция:** Нет

Все изменения опциональны и не ломают существующую функциональность.

---

## 🚀 Обновление

Для использования новых возможностей:

1. Обновите код плагина (уже обновлён)
2. Перезапустите плагин в Figma
3. Используйте новую настройку в UI

---

## 📚 Связанные документы

- [DESIGN_SYSTEM_VALIDATOR.md](./DESIGN_SYSTEM_VALIDATOR.md) — Полная документация
- [TESTING_DSV.md](./TESTING_DSV.md) — Руководство по тестированию
- [DSV_IMPLEMENTATION_SUMMARY.md](./DSV_IMPLEMENTATION_SUMMARY.md) — Техническая сводка

---

**© 2025 Orbita Design System Team**

