# Design System Validator - Отладка через консоль

## 🔍 Детальное логирование

Добавлено подробное логирование для **всех** случаев когда находятся проблемы с токенами.

---

## 📝 Как использовать

### Шаг 1: Запустите проверку
```
DSV → Проверить Variables
```

### Шаг 2: Откройте консоль
```
Mac: Cmd + Option + I
Windows: Ctrl + Shift + I
```

### Шаг 3: Найдите логи
Ищите строки начинающиеся с:
- `DSV: 🔍 Свойство без токена:` - для обычных свойств
- `DSV: 🔍 Опциональное свойство без токена:` - для опциональных
- `DSV: 🔍 Массив свойств без токена:` - для fills/strokes/effects
- `DSV: ❌ Токен не найден` - когда токен привязан, но не в списке

---

## 📊 Что показывают логи

### Пример 1: Свойство без токена (ваш случай)

```
DSV: 🔍 Свойство без токена:
  Node: "Button/Primary"
  Property: Corner Radius (cornerRadius)
  Value: 8
  Has boundVariables: true
  boundVariables для cornerRadius: undefined
  Все boundVariables: ["fills", "strokes"]
  Node type: FRAME
  ---
```

**Что это значит:**
- ✅ У ноды ЕСТЬ boundVariables (для других свойств)
- ❌ Но для cornerRadius boundVariable = undefined
- 💡 **Вывод**: cornerRadius действительно НЕ привязан к токену!

---

### Пример 2: Токен привязан, но не найден

```
DSV: ❌ Токен не найден. 
  Node: "Button/Secondary", 
  Property: Fill, 
  Token: "color/primary/500", 
  isRemote: true, 
  Mode: local
DSV: Всего токенов в списке: 32
```

**Что это значит:**
- ✅ Токен ПРИВЯЗАН к элементу
- ❌ Но токен remote, а проверяем в режиме "local"
- 💡 **Решение**: Использовать режим "All" или "Remote"

---

### Пример 3: Токен привязан через Instance

```
DSV: 🔍 Свойство без токена:
  Node: "Button/Primary → Instance"
  Property: Corner Radius (cornerRadius)
  Value: 8
  Has boundVariables: false
  Node type: INSTANCE
  ---
```

**Что это значит:**
- Instance наследует свойства от главного компонента
- У Instance может не быть собственных boundVariables
- 💡 **Решение**: Включить "Пропускать вложенные Instance"

---

## 🎯 Как читать логи для Corner Radius

### Сценарий A: boundVariables вообще нет
```
Has boundVariables: false
```
**Причина**: У элемента вообще нет привязанных токенов  
**Решение**: 
1. Отключить проверку Corner Radius
2. Или привязать токен в Figma

---

### Сценарий Б: boundVariables есть, но не для cornerRadius
```
Has boundVariables: true
boundVariables для cornerRadius: undefined
Все boundVariables: ["fills"]
```
**Причина**: Токен привязан к fills, но не к cornerRadius  
**Решение**: 
1. Отключить проверку Corner Radius
2. Или привязать токен к cornerRadius в Figma

---

### Сценарий В: boundVariables для cornerRadius есть
```
Has boundVariables: true
boundVariables для cornerRadius: { id: "VariableID:123:456" }
```
**Причина**: Токен ЕСТЬ, но не в списке проверяемых  
**Ищите**: `DSV: ❌ Токен не найден` выше в логах  
**Решение**: Загрузить правильные токены из JSON

---

## 🔧 Что делать с информацией

### 1. Проверьте Has boundVariables

#### Если `false`:
```
Элемент не имеет привязанных токенов вообще
→ Это нормально для некоторых элементов
→ Отключите проверку этого свойства
```

#### Если `true`:
```
Элемент имеет привязанные токены
→ Проверьте какие именно
→ Смотрите "Все boundVariables"
```

---

### 2. Проверьте boundVariables для cornerRadius

#### Если `undefined`:
```
cornerRadius НЕ привязан к токену
→ Это реальная проблема (если нужно)
→ Или отключите проверку если не критично
```

#### Если есть объект с `id`:
```
cornerRadius ПРИВЯЗАН к токену!
→ Ищите "❌ Токен не найден" выше
→ Проблема в том что токен не в списке
```

---

### 3. Проверьте список всех boundVariables

```
Все boundVariables: ["fills", "strokes", "cornerRadius"]
```

Это покажет **какие именно** свойства имеют токены.

**Если cornerRadius в списке** → токен привязан, но не найден  
**Если cornerRadius НЕ в списке** → токен не привязан

---

## 💡 Практические примеры

### Проблема: "Corner Radius без токена"

#### Шаг 1: Смотрим лог
```
DSV: 🔍 Свойство без токена:
  Node: "ya-360/Button"
  Property: Corner Radius (cornerRadius)
  Value: 8
  Has boundVariables: true
  boundVariables для cornerRadius: undefined
  Все boundVariables: ["fills"]
```

#### Шаг 2: Анализируем
- Node: "ya-360/Button" ← это ваш элемент
- Has boundVariables: true ← есть токены для других свойств
- boundVariables для cornerRadius: undefined ← НО НЕТ для cornerRadius
- Все boundVariables: ["fills"] ← привязан только fills

#### Шаг 3: Вывод
**cornerRadius действительно НЕ привязан к токену!**

#### Шаг 4: Решения
1. **Отключить проверку** (быстро):
   ```
   DSV → Проверяемые свойства → ☐ Corner Radius
   ```

2. **Привязать токен в Figma** (правильно):
   ```
   1. Выбрать элемент в Figma
   2. Corner Radius → Открыть переменные
   3. Выбрать нужный токен (например border-radius/md)
   4. Запустить проверку снова
   ```

3. **Проверить Instance** (если это Instance):
   ```
   DSV → ☑ Пропускать вложенные Instance
   ```

---

## 🚨 Частые случаи

### Case 1: "Много элементов без Corner Radius токена"

**Если видите 10+ таких логов:**
```
DSV: 🔍 Свойство без токена:
  Property: Corner Radius
  boundVariables для cornerRadius: undefined
```

**Вывод**: В вашем файле много элементов реально не используют токены для радиусов

**Решение**: 
- Отключите проверку Corner Radius
- Или массово привяжите токены в Figma

---

### Case 2: "Instance компоненты"

**Если видите:**
```
Node type: INSTANCE
Has boundVariables: false
```

**Вывод**: Instance не имеет собственных boundVariables (наследует от мастера)

**Решение**: 
```
DSV → ☑ Пропускать вложенные Instance компоненты
```

---

### Case 3: "Variant внутри Component Set"

**Если видите:**
```
Node: "Button → Variant=primary, State=default"
Property: Corner Radius
boundVariables для cornerRadius: undefined
```

**Вывод**: Variant может не иметь токенов если они на уровне Component Set

**Решение**: Проверьте главный компонент, не variant

---

## 🎓 Как понять логи

### Формат лога
```
DSV: 🔍 Свойство без токена:     ← Тип проблемы
  Node: "..."                     ← Имя элемента
  Property: ... (...)             ← Свойство (DisplayName и key)
  Value: ...                      ← Текущее значение
  Has boundVariables: ...         ← Есть ли токены вообще
  boundVariables для ...: ...     ← Токен для этого свойства
  Все boundVariables: [...]       ← Список всех токенов
  Node type: ...                  ← Тип элемента
  ---
```

### Ключевые поля

| Поле | Значение | Что означает |
|------|----------|--------------|
| **Has boundVariables** | `true` | У элемента ЕСТЬ токены (для каких-то свойств) |
| | `false` | У элемента НЕТ токенов вообще |
| **boundVariables для cornerRadius** | `undefined` | cornerRadius НЕ привязан |
| | `{ id: "..." }` | cornerRadius ПРИВЯЗАН (но не найден) |
| **Все boundVariables** | `["fills"]` | Только fills имеет токен |
| | `["fills", "cornerRadius"]` | И fills и cornerRadius имеют токены |
| **Node type** | `INSTANCE` | Это Instance компонента |
| | `FRAME` | Это Frame/обычный элемент |
| | `COMPONENT` | Это главный компонент |

---

## 📋 Чеклист отладки

### Шаг 1: Запустить проверку и открыть консоль
- [ ] Проверка запущена
- [ ] Консоль открыта (Cmd+Option+I)

### Шаг 2: Найти логи с Corner Radius
- [ ] Нашёл строки "🔍 Свойство без токена"
- [ ] Нашёл проблемный Corner Radius

### Шаг 3: Проверить Has boundVariables
- [ ] Если `false` → элемент без токенов вообще
- [ ] Если `true` → есть токены для других свойств

### Шаг 4: Проверить boundVariables для cornerRadius
- [ ] Если `undefined` → cornerRadius НЕ привязан к токену
- [ ] Если есть объект → токен привязан, но не найден

### Шаг 5: Принять решение
- [ ] Отключить проверку Corner Radius
- [ ] Или привязать токены в Figma
- [ ] Или пропускать Instance компоненты

---

## 🎉 Результат

Теперь вы можете:
- ✅ Точно определить есть ли токен привязан
- ✅ Понять почему возникает ошибка
- ✅ Принять правильное решение
- ✅ Настроить проверку под свои нужды

**Используйте логи для отладки!** 🔍

---

**Версия**: 3.0.3  
**Дата**: Октябрь 16, 2025  
**Статус**: ✅ Готово к использованию

