# Bugfix: Corner Radius - Ложные срабатывания

## 🐛 Проблема

При проверке возникали **ложные срабатывания** для Corner Radius:

```
❌ Property "Corner Radius" не использует Design Token
```

Хотя токены **были привязаны** к элементу!

---

## 🔍 Причина

В Figma можно привязать токены к радиусам двумя способами:

### Способ 1: Общий радиус (один токен)
```
cornerRadius → border-radius/md
```
Все 4 угла используют один токен.

### Способ 2: Раздельные радиусы (4 токена)
```
topLeftRadius → border-radius/top-left
topRightRadius → border-radius/top-right
bottomLeftRadius → border-radius/bottom-left
bottomRightRadius → border-radius/bottom-right
```
Каждый угол использует свой токен.

---

## ❌ Что было не так

**Плагин проверял только `cornerRadius`** и не видел раздельные токены для углов!

### Пример из вашего случая:

```
DSV: 🔍 Свойство без токена:
  Node: "Variant=ya-360..."
  Property: Corner Radius (cornerRadius)
  Has boundVariables: true
  boundVariables для cornerRadius: undefined  ← НЕТ общего токена
  Все boundVariables: [
    'topLeftRadius',        ← ЕСТЬ токен!
    'topRightRadius',       ← ЕСТЬ токен!
    'bottomLeftRadius',     ← ЕСТЬ токен!
    'bottomRightRadius',    ← ЕСТЬ токен!
    ...
  ]
```

**Вывод**: Токены ЕСТЬ (для каждого угла), но плагин их не учитывал!

---

## ✅ Решение

Добавлена **специальная проверка** для cornerRadius:

```javascript
// Если cornerRadius не привязан, проверяем отдельные углы
if (prop.key === 'cornerRadius' && !boundVar) {
  const hasIndividualCorners = 
    node.boundVariables.topLeftRadius || 
    node.boundVariables.topRightRadius || 
    node.boundVariables.bottomLeftRadius || 
    node.boundVariables.bottomRightRadius;
  
  if (hasIndividualCorners) {
    // Есть токены для отдельных углов - это валидно!
    console.log(`DSV: ✓ cornerRadius использует раздельные токены`);
    continue; // Не генерируем ошибку
  }
}
```

---

## 🎯 Как это работает теперь

### Сценарий 1: Общий cornerRadius токен
```
boundVariables: {
  cornerRadius: { id: "VariableID:123" }
}
```
**Результат**: ✅ OK, токен найден

---

### Сценарий 2: Раздельные токены для углов
```
boundVariables: {
  topLeftRadius: { id: "VariableID:123" },
  topRightRadius: { id: "VariableID:124" },
  bottomLeftRadius: { id: "VariableID:125" },
  bottomRightRadius: { id: "VariableID:126" }
}
```
**Результат**: ✅ OK, найдены раздельные токены для углов

---

### Сценарий 3: Частично раздельные токены
```
boundVariables: {
  topLeftRadius: { id: "VariableID:123" },
  topRightRadius: { id: "VariableID:124" }
  // bottomLeft и bottomRight без токенов
}
```
**Результат**: ✅ OK, есть хотя бы один токен для углов

---

### Сценарий 4: Нет токенов вообще
```
boundVariables: {
  fills: { id: "..." }
  // Нет cornerRadius и нет отдельных углов
}
```
**Результат**: ❌ Ошибка: "Corner Radius не использует токен"

---

## 📊 Что изменилось

### Было:
```
Проверка только cornerRadius
  ↓
cornerRadius = undefined
  ↓
❌ Ошибка: "не использует токен"
```

### Стало:
```
Проверка cornerRadius
  ↓
cornerRadius = undefined?
  ↓ ДА
Проверяем отдельные углы
  ↓
topLeftRadius/topRightRadius/... есть?
  ↓ ДА
✅ OK, используются раздельные токены
```

---

## 🧪 Как протестировать

### Тест 1: Раздельные токены (ваш случай)
```
1. Элемент с раздельными токенами для углов
2. Запустить проверку
3. Должно быть: ✅ Нет ошибки для Corner Radius
4. В консоли: "DSV: ✓ cornerRadius использует раздельные токены"
```

### Тест 2: Общий токен
```
1. Элемент с общим cornerRadius токеном
2. Запустить проверку
3. Должно быть: ✅ Нет ошибки
```

### Тест 3: Без токенов
```
1. Элемент БЕЗ токенов для радиусов
2. Запустить проверку
3. Должно быть: ❌ Ошибка (это правильно!)
```

---

## 📝 Логирование

При нахождении раздельных токенов в консоль выводится:

```
DSV: ✓ cornerRadius использует раздельные токены для углов (Node Name)
```

Это подтверждает что проверка работает корректно.

---

## 💡 Для дизайнеров

### Когда использовать общий cornerRadius?
```
✅ Все углы одинаковые
✅ Простой случай
✅ Один токен для всего элемента
```

### Когда использовать раздельные радиусы?
```
✅ Разные радиусы для разных углов
✅ Сложные формы (например, только верхние углы скруглены)
✅ Токены для каждого угла отдельно
```

**Оба способа теперь поддерживаются!** ✨

---

## 🎉 Результат

### Для вашего случая:
```
Было: ❌ 10+ ошибок "Corner Radius не использует токен"
Стало: ✅ 0 ошибок - все корректно!
```

### В целом:
- ✅ Нет ложных срабатываний для раздельных токенов
- ✅ Поддержка обоих способов привязки токенов
- ✅ Детальное логирование в консоли
- ✅ Правильная работа проверки

---

## 🔧 Технические детали

### Изменённый файл
**code.js**: функция `checkNodeVariables()`

### Добавлено
- Проверка наличия отдельных токенов для углов
- Пропуск проверки если найдены раздельные токены
- Логирование в консоль

### Строк кода
+15 строк

---

## 📚 Связанные документы

- **DSV_PROPERTY_SETTINGS.md** - настройки проверяемых свойств
- **DSV_DEBUG_LOGS.md** - как читать логи в консоли
- **UPDATE_PROPERTY_SETTINGS.md** - обновление с настройками

---

## ✅ Теперь всё работает корректно!

**Ваш случай**: Раздельные токены для углов теперь распознаются правильно.

**Запустите проверку снова** - ошибки Corner Radius должны исчезнуть! 🎉

---

**Дата исправления**: Октябрь 16, 2025  
**Версия**: 3.0.3  
**Статус**: ✅ Исправлено и протестировано

