# Design System Validator - Быстрый старт с JSON токенами

## 🚀 За 3 шага

### Шаг 1: Экспортируйте токены из Figma

1. Откройте ваш Figma файл с токенами (обычно это библиотека дизайн-системы)
2. Запустите любой плагин (например, Orbita Icon Checker)
3. Откройте консоль разработчика:
   - **macOS**: `Cmd + Option + I`
   - **Windows**: `Ctrl + Shift + I`
4. Скопируйте и вставьте код из файла `export-tokens-script.js`
5. Нажмите `Enter`
6. JSON будет выведен в консоль и скопирован в буфер обмена
7. Сохраните JSON в файл (например, `my-design-tokens.json`)

### Шаг 2: Загрузите JSON в плагин

1. Откройте **Orbita Icon Checker** в режиме **Design System Validator**
2. Раскройте секцию **"Альтернатива: Загрузить JSON с токенами"**
3. Нажмите **"📁 Загрузить JSON"**
4. Выберите сохранённый JSON файл
5. Убедитесь, что появилось сообщение: `✅ Загружено токенов: XXX`

### Шаг 3: Запустите проверку

1. Выберите элементы для проверки (или оставьте пустым для проверки всей страницы)
2. Выберите режим:
   - **Local** - только локальные токены
   - **Remote** - только токены из библиотек
   - **All** - все токены
3. Нажмите **"🔍 Проверить Variables"**
4. Изучите результаты!

---

## 📝 Формат JSON (минимальный)

```json
{
  "tokens": [
    {
      "name": "color/primary/500",
      "isRemote": true
    },
    {
      "name": "spacing/md",
      "isRemote": true
    }
  ]
}
```

**Обязательное поле**: только `name`

**Рекомендуемые поля**: `name`, `type`, `isRemote`

---

## ❓ FAQ

### Q: Сколько токенов должно быть в JSON?

**A**: Столько, сколько у вас в дизайн-системе. Обычно 100-500+.

### Q: Что делать если Figma показывает 32 токена, а у меня их больше 100?

**A**: Именно для этого и создана эта функция! Figma API не всегда видит все remote токены. Используйте JSON экспорт.

### Q: Можно ли использовать токены из Tokens Studio или Style Dictionary?

**A**: Да! Главное, чтобы каждый токен имел поле `name`.

### Q: Токены сохраняются после перезапуска плагина?

**A**: Да, JSON сохраняется в localStorage браузера.

### Q: Как обновить токены?

**A**: Просто загрузите новый JSON файл - он заменит старый.

### Q: Как вернуться к использованию Figma API?

**A**: Нажмите кнопку **"🗑️ Очистить"** в секции загрузки JSON.

---

## 🔍 Как проверить что работает?

1. После загрузки JSON откройте консоль
2. Запустите проверку
3. В консоли должно быть сообщение:
   ```
   DSV: Используются токены из загруженного JSON файла: XXX
   ```
4. Также будут показаны первые 10 токенов для проверки

---

## 💡 Советы

✅ **Экспортируйте токены из главной библиотеки** - так вы получите все токены, включая remote

✅ **Сохраните JSON файл** - он может понадобиться для повторных проверок

✅ **Сравните результаты** - запустите проверку с Figma API и с JSON, сравните количество найденных проблем

✅ **Проверяйте консоль** - там подробная информация о том, какие токены используются

❌ **Не редактируйте JSON вручную** - используйте скрипт экспорта для получения актуальных данных

---

## 📚 Дополнительные материалы

- **DSV_JSON_TOKENS_GUIDE.md** - Подробная документация
- **tokens-example.json** - Пример файла с токенами
- **export-tokens-script.js** - Скрипт для экспорта токенов

---

## 🐛 Проблемы?

1. Откройте консоль разработчика
2. Запустите проверку
3. Скопируйте все логи начинающиеся с `DSV:`
4. Это поможет понять что пошло не так

---

**Версия**: 3.0.3  
**Дата обновления**: Октябрь 2025

