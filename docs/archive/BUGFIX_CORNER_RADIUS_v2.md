# Bugfix: Corner Radius - Проверка каждого угла отдельно

## 🔄 Обновление v2

Улучшена логика проверки Corner Radius - теперь **каждый угол проверяется отдельно**.

---

## 🎯 Новая проблема

После первого исправления:
```
✅ Если ВСЕ углы имеют токены → OK
❌ Если ОДИН угол без токена, остальные с токенами → не находилось!
```

### Пример проблемы:
```
topLeftRadius: 8 → токен ✅
topRightRadius: 8 → БЕЗ токена ❌
bottomLeftRadius: 8 → БЕЗ токена ❌
bottomRightRadius: 8 → токен ✅
```

**Было**: Проверка говорила "OK" (есть хотя бы один токен)  
**Нужно**: Найти что 2 угла без токенов!

---

## ✅ Решение v2

Теперь проверяется **каждый угол отдельно**:

```javascript
for (const corner of [topLeft, topRight, bottomLeft, bottomRight]) {
  if (угол имеет значение) {
    if (угол имеет токен) {
      ✅ Валидируем токен
    } else {
      ❌ Добавляем ошибку для этого конкретного угла
    }
  }
}
```

---

## 📊 Как работает теперь

### Сценарий 1: Общий cornerRadius токен
```
cornerRadius: 8 → токен
```
**Результат**: ✅ OK

---

### Сценарий 2: Все углы с токенами
```
topLeftRadius: 8 → токен ✅
topRightRadius: 8 → токен ✅
bottomLeftRadius: 8 → токен ✅
bottomRightRadius: 8 → токен ✅
```
**Результат**: ✅ OK (все углы проверены)

---

### Сценарий 3: Один угол без токена (ваш новый случай)
```
topLeftRadius: 8 → токен ✅
topRightRadius: 8 → БЕЗ токена ❌
bottomLeftRadius: 8 → токен ✅
bottomRightRadius: 8 → токен ✅
```
**Результат**: 
```
❌ Corner "Top-Right" не использует Design Token
```

---

### Сценарий 4: Два угла без токенов
```
topLeftRadius: 8 → токен ✅
topRightRadius: 8 → БЕЗ токена ❌
bottomLeftRadius: 0 (нет значения)
bottomRightRadius: 8 → БЕЗ токена ❌
```
**Результат**: 
```
❌ Corner "Top-Right" не использует Design Token
❌ Corner "Bottom-Right" не использует Design Token
```

---

### Сценарий 5: Разные значения углов
```
topLeftRadius: 16 → токен ✅
topRightRadius: 16 → токен ✅
bottomLeftRadius: 0 → нет значения (пропускается)
bottomRightRadius: 0 → нет значения (пропускается)
```
**Результат**: ✅ OK (только верхние углы проверены, нижние = 0)

---

## 🔍 Логирование

### При нахождении угла без токена:
```
DSV: 🔍 Угол без токена:
  Node: "Button/Primary"
  Corner: Top-Right (topRightRadius)
  Value: 8
  ---
```

### При успешной проверке всех углов:
```
DSV: ✓ Проверены раздельные углы для Button/Primary
```

---

## 📝 Детали реализации

### Что проверяется:

1. **Общий cornerRadius**:
   - Если привязан токен → проверяем его
   - Если нет → переходим к отдельным углам

2. **Отдельные углы** (если cornerRadius не привязан):
   - topLeftRadius
   - topRightRadius
   - bottomLeftRadius
   - bottomRightRadius

3. **Для каждого угла**:
   - Проверяем есть ли значение (не 0, не undefined)
   - Если есть значение:
     - ✅ Токен есть → валидируем
     - ❌ Токена нет → добавляем ошибку

4. **Если угол = 0 или undefined**:
   - Пропускаем проверку (нет значения = нет проблемы)

---

## 🎨 Примеры для дизайнеров

### ✅ Правильно: Все с токенами
```figma
Элемент:
  topLeftRadius: 16px → tokens/radius/lg
  topRightRadius: 16px → tokens/radius/lg
  bottomLeftRadius: 16px → tokens/radius/lg
  bottomRightRadius: 16px → tokens/radius/lg

✅ Результат: Нет ошибок
```

---

### ✅ Правильно: Только верхние углы
```figma
Элемент:
  topLeftRadius: 16px → tokens/radius/lg
  topRightRadius: 16px → tokens/radius/lg
  bottomLeftRadius: 0px
  bottomRightRadius: 0px

✅ Результат: Нет ошибок (нижние = 0, не проверяются)
```

---

### ❌ Неправильно: Один угол без токена
```figma
Элемент:
  topLeftRadius: 16px → tokens/radius/lg ✅
  topRightRadius: 16px → [хардкод 16px] ❌
  bottomLeftRadius: 16px → tokens/radius/lg ✅
  bottomRightRadius: 16px → tokens/radius/lg ✅

❌ Результат: "Corner Top-Right не использует токен"
```

---

### ⚠️ Смешанные значения
```figma
Элемент:
  topLeftRadius: 16px → tokens/radius/lg ✅
  topRightRadius: 8px → [хардкод 8px] ❌
  bottomLeftRadius: 4px → [хардкод 4px] ❌
  bottomRightRadius: 16px → tokens/radius/lg ✅

❌ Результат: 2 ошибки
  - "Corner Top-Right не использует токен"
  - "Corner Bottom-Left не использует токен"
```

---

## 🧪 Как протестировать

### Тест 1: Все углы с токенами
```
1. Элемент с токенами для всех 4 углов
2. Запустить проверку
3. Результат: ✅ Нет ошибок
4. Консоль: "✓ Проверены раздельные углы"
```

### Тест 2: Один угол без токена
```
1. Элемент:
   - topLeft: 8px с токеном
   - topRight: 8px БЕЗ токена
   - bottomLeft: 8px с токеном
   - bottomRight: 8px с токеном
2. Запустить проверку
3. Результат: ❌ 1 ошибка "Corner Top-Right"
4. Консоль: "🔍 Угол без токена: Top-Right"
```

### Тест 3: Два угла = 0
```
1. Элемент:
   - topLeft: 16px с токеном
   - topRight: 16px с токеном
   - bottomLeft: 0
   - bottomRight: 0
2. Запустить проверку
3. Результат: ✅ Нет ошибок (нижние не проверяются)
```

### Тест 4: Общий cornerRadius
```
1. Элемент с общим cornerRadius токеном
2. Запустить проверку
3. Результат: ✅ Нет ошибок
```

---

## 📊 Было → Стало

### Было (v1):
```
Проверка: Есть ли ХОТЯ БЫ ОДИН угол с токеном?
  ↓ ДА
✅ OK (даже если остальные без токенов)
```

### Стало (v2):
```
Проверка: Для КАЖДОГО угла с значением
  ↓
Угол имеет значение?
  ↓ ДА
Угол имеет токен?
  ↓ НЕТ
❌ Ошибка для этого угла
```

---

## 💡 Полезные советы

### Для команды дизайна

1. **Используйте общий cornerRadius** если все углы одинаковые
   ```
   ✅ Проще
   ✅ Один токен
   ✅ Легче поддерживать
   ```

2. **Используйте раздельные углы** только когда нужны разные радиусы
   ```
   ⚠️ Сложнее
   ⚠️ Нужно 4 токена
   ⚠️ Легко забыть привязать один угол
   ```

3. **Убедитесь что ВСЕ углы с значением имеют токены**
   ```
   Плагин найдёт если хотя бы один угол без токена!
   ```

---

## 🔧 Технические детали

### Изменения в code.js
- Добавлен цикл по всем 4 углам
- Проверка значения каждого угла (не 0, не undefined)
- Валидация токена для каждого угла отдельно
- Генерация ошибки для каждого угла без токена

### Строк кода
+50 строк (расширенная логика)

### Типы ошибок
- `Corner Radius Top-Left`
- `Corner Radius Top-Right`
- `Corner Radius Bottom-Left`
- `Corner Radius Bottom-Right`

---

## 🎉 Результат

### Для вашего случая:
```
Теперь найдёт если:
✅ Все углы с токенами → OK
✅ Один угол без токена → найдёт ❌
✅ Несколько углов без токенов → найдёт все ❌
✅ Углы = 0 → пропустит (не проверит)
```

### Общий результат:
- ✅ Точная проверка каждого угла
- ✅ Находит частичные проблемы
- ✅ Детальные ошибки (какой именно угол)
- ✅ Правильная работа со всеми случаями

---

## 📚 Связанные документы

- **BUGFIX_CORNER_RADIUS.md** - первое исправление (v1)
- **DSV_PROPERTY_SETTINGS.md** - настройки свойств
- **DSV_DEBUG_LOGS.md** - как читать логи

---

**Дата обновления**: Октябрь 16, 2025  
**Версия**: 3.0.3 (v2)  
**Статус**: ✅ Полностью исправлено

**Теперь проверка Corner Radius работает идеально!** 🎯

