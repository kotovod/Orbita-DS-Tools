# Design System Validator - Настройки проверяемых свойств

## ✨ Новая функция

Добавлены **настройки проверяемых свойств** с чекбоксами - теперь вы можете выбрать какие свойства проверять на наличие токенов.

---

## 🎯 Зачем это нужно?

### Проблема
При проверке могут возникать **ложные срабатывания** (false positives):
- Свойство использует токен, но плагин говорит что нет
- Instance компоненты имеют свои значения
- Некоторые свойства не должны использовать токены

### Решение
Гибкие настройки позволяют:
- ✅ Отключить проверку конкретных свойств
- ✅ Сосредоточиться на важных свойствах
- ✅ Легко отлаживать проблемы
- ✅ Настроить проверку под ваш workflow

---

## 📋 Доступные настройки

### 🎨 Цвета
- **Fill (Заливка)** - проверка цвета заливки
- **Stroke (Обводка)** - проверка цвета обводки

### 📏 Размеры и отступы
- **Corner Radius** - проверка радиуса скругления
- **Gap / Item Spacing** - проверка отступов между элементами
- **Padding** - проверка внутренних отступов (все 4 стороны)

### ✨ Эффекты и прочее
- **Effects** - проверка эффектов (тени, размытие)
- **Opacity** - проверка прозрачности
- **Width/Height** - проверка размеров элементов

---

## ⚙️ Настройки по умолчанию

### ✅ Включены (checked):
- Fill
- Stroke
- Corner Radius
- Gap / Spacing
- Padding

### ❌ Выключены (unchecked):
- Effects
- Opacity
- Width/Height

**Почему?** Эти свойства чаще всего должны использовать токены в дизайн-системах.

---

## 🚀 Как использовать

### 1. Открыть настройки
```
Плагин → DSV → Раскрыть "Проверяемые свойства"
```

### 2. Выбрать нужные свойства
Поставьте/уберите галочки на нужных свойствах

### 3. Быстрые действия
- **✓ Все** - включить все свойства
- **✗ Нет** - отключить все свойства
- **↺ По умолчанию** - вернуть настройки по умолчанию

### 4. Запустить проверку
```
Кнопка "🔍 Проверить Variables"
```

---

## 💡 Примеры использования

### Пример 1: Проверка только цветов
```
1. Нажать "✗ Нет" - отключить всё
2. Включить только:
   ✓ Fill
   ✓ Stroke
3. Запустить проверку
→ Проверятся только цвета
```

### Пример 2: Без Corner Radius
```
Если у вас много компонентов с разными радиусами:
1. Отключить: ☐ Corner Radius
2. Остальное оставить включённым
3. Запустить проверку
→ Радиусы не проверяются, нет ложных срабатываний
```

### Пример 3: Только spacing и padding
```
Для проверки отступов в Auto Layout:
1. Нажать "✗ Нет"
2. Включить:
   ✓ Gap / Spacing
   ✓ Padding
3. Запустить проверку
→ Проверяются только отступы
```

---

## 🔍 Отладка ложных срабатываний

### Шаг 1: Определите проблемное свойство
```
Смотрим в отчёте:
Property "Corner Radius" не использует Design Token
```

### Шаг 2: Проверьте консоль
```
Откройте консоль (Cmd+Option+I)
Найдите логи с "DSV: ❌ Токен не найден"
Там будет подробная информация:
- Имя ноды
- Имя свойства
- Имя токена
- isRemote (local/remote)
- Режим проверки
```

### Шаг 3: Проверьте настройки
```
1. Проверьте режим: Local / Remote / All
2. Проверьте загружены ли токены из JSON
3. Убедитесь что токен есть в JSON файле
```

### Шаг 4: Отключите проблемное свойство
```
Если Corner Radius даёт ложные срабатывания:
1. Отключите ☐ Corner Radius
2. Запустите проверку снова
→ Ошибка пропадёт
```

---

## 🧪 Как протестировать

### Тест 1: Все свойства
```
1. Нажать "✓ Все"
2. Проверить Variables
→ Должны проверяться все 8 типов свойств
```

### Тест 2: Ничего не проверяется
```
1. Нажать "✗ Нет"
2. Проверить Variables
→ Проверка должна вернуть 0 проблем (ничего не проверяется)
```

### Тест 3: По умолчанию
```
1. Нажать "↺ По умолчанию"
2. Проверить что включены: Fill, Stroke, Corner Radius, Spacing, Padding
3. Проверить что отключены: Effects, Opacity, Size
```

---

## 🔧 Технические детали

### Передача настроек

```javascript
// UI → Code
const propertiesToCheck = {
  fills: true,
  strokes: true,
  cornerRadius: true,
  spacing: true,
  padding: true,
  effects: false,
  opacity: false,
  size: false
};

parent.postMessage({
  pluginMessage: {
    type: 'dsv-validate',
    options: {
      propertiesToCheck: propertiesToCheck
    }
  }
}, '*');
```

### Фильтрация свойств в code.js

```javascript
const allProperties = [
  { key: 'fills', settingKey: 'fills' },
  { key: 'strokes', settingKey: 'strokes' },
  { key: 'cornerRadius', settingKey: 'cornerRadius' },
  { key: 'paddingLeft', settingKey: 'padding' },
  { key: 'paddingRight', settingKey: 'padding' },
  { key: 'paddingTop', settingKey: 'padding' },
  { key: 'paddingBottom', settingKey: 'padding' },
  { key: 'itemSpacing', settingKey: 'spacing' },
  { key: 'effects', settingKey: 'effects' },
  { key: 'opacity', settingKey: 'opacity' },
  { key: 'width', settingKey: 'size' },
  { key: 'height', settingKey: 'size' }
];

// Фильтрация
const propertiesToCheck = allProperties.filter(prop => {
  return propsSettings[prop.settingKey] !== false;
});
```

### Логирование

При ложных срабатываниях в консоль выводится:
```
DSV: ❌ Токен не найден. 
  Node: "Button/Primary", 
  Property: Corner Radius, 
  Token: "border-radius/md", 
  isRemote: true, 
  Mode: remote
DSV: Всего токенов в списке: 127
```

Это помогает понять:
- Какой именно токен не найден
- В каком элементе проблема
- Сколько токенов загружено всего

---

## 📊 Какие свойства чаще всего проверять?

### Обязательно (✅):
- **Fill** - основной цвет элементов
- **Stroke** - цвет обводок
- **Corner Radius** - радиусы скругления
- **Spacing** - отступы в Auto Layout
- **Padding** - внутренние отступы

### Опционально (❓):
- **Effects** - если используете токены для теней
- **Opacity** - если используете токены для прозрачности
- **Size** - редко используется через токены

---

## 💡 Советы

### ✅ Рекомендации
1. Начните с настроек по умолчанию
2. Отключайте свойства если видите много ложных срабатываний
3. Используйте консоль для отладки проблем
4. Проверьте что загружены правильные токены (JSON)

### ❌ Не рекомендуется
1. Отключать все свойства - проверка не имеет смысла
2. Игнорировать логи в консоли - они помогают найти реальные проблемы
3. Проверять без загруженных токенов - будет много ошибок

---

## 🐛 Устранение проблем

### "Много ложных срабатываний на Corner Radius"

**Причина**: Возможно токены из библиотеки не загружены  
**Решение**: 
1. Экспортируйте токены из библиотеки
2. Загрузите JSON в плагин
3. Запустите проверку снова

### "Все свойства показывают ошибки"

**Причина**: Токены не загружены или неправильный режим  
**Решение**:
1. Проверьте что токены загружены: "✅ Загружено токенов: XXX"
2. Проверьте режим: попробуйте "All" вместо "Remote"
3. Проверьте консоль на ошибки

### "Instance компоненты дают ошибки"

**Причина**: Instance могут иметь свои переопределения  
**Решение**:
1. Включите настройку "Пропускать вложенные Instance"
2. Или отключите проверку проблемных свойств

---

## 📚 Связанные документы

- **DSV_QUICK_START_JSON.md** - быстрый старт с токенами
- **DSV_JSON_TOKENS_GUIDE.md** - полное руководство по JSON
- **BUGFIX_LOCALSTORAGE.md** - исправление localStorage
- **НАЧНИТЕ_ЗДЕСЬ.md** - главная инструкция

---

## 🎉 Результат

Теперь вы можете:
- ✅ Выбирать какие свойства проверять
- ✅ Отключать проблемные свойства
- ✅ Быстро переключаться между настройками
- ✅ Видеть подробные логи в консоли
- ✅ Легко отлаживать ложные срабатывания

**Настройте проверку под ваши нужды!** 🚀

---

**Версия**: 3.0.3  
**Дата**: Октябрь 16, 2025  
**Статус**: ✅ Готово к использованию

