# DSV Auto-Bind Tokens Feature

**Версия:** 3.1  
**Дата:** 16 октября 2025

## Описание

Новая функциональность Design System Validator (DSV), которая автоматически определяет подходящие токены для элементов без привязки и позволяет автоматически привязывать их одним кликом.

## Что нового

### 1. Автоматическое определение токенов по значению

Когда DSV находит элемент без привязанного токена, система теперь:
- Анализирует текущее значение свойства (цвет, радиус, отступ и т.д.)
- Ищет подходящий токен в загруженной библиотеке токенов
- Сравнивает значения с высокой точностью
- Проверяет релевантность токена по имени (например, для `cornerRadius` ищет токены с "radius", "corner" в названии)

### 2. Визуальное отображение предложенного токена

Если подходящий токен найден, в карточке проблемы отображается:
- **💡 Найден подходящий токен** - зелёный заголовок
- Название токена и его значение
- Кнопка **🔗 Привязать токен** для автоматической привязки

### 3. Автоматическая привязка токена

При нажатии на кнопку "Привязать токен":
1. Токен автоматически привязывается к свойству элемента
2. Отображается уведомление об успехе/ошибке
3. Issue автоматически удаляется из списка проблем
4. Элемент обновляется в Figma

## Поддерживаемые свойства

Функциональность работает со всеми свойствами, которые проверяет DSV:

### Числовые значения
- **Corner Radius** - радиусы углов (общий и раздельные)
- **Padding** - все виды отступов (left, right, top, bottom)
- **Gap** (itemSpacing) - расстояние между элементами
- **Width/Height** - размеры элементов
- **Opacity** - прозрачность

### Цвета
- **Fill** - заливка
- **Stroke** - обводка

## Как это работает

### Алгоритм поиска токена

1. **Для числовых значений:**
   - Сравнение с точностью ±0.01
   - Фильтрация по типу токена (FLOAT, NUMBER)
   - Проверка релевантности по имени токена

2. **Для цветов:**
   - Сравнение RGB компонентов с точностью ±0.01
   - Сравнение альфа-канала
   - Фильтрация по типу токена (COLOR)

### Процесс привязки

```javascript
// 1. Получение элемента по ID
const node = await figma.getNodeByIdAsync(nodeId);

// 2. Получение токена (variable)
const variable = await figma.variables.getVariableByIdAsync(tokenId);
// или поиск по имени в локальных variables

// 3. Привязка токена к свойству
node.setBoundVariable(property, variable);
```

## Примеры использования

### Пример 1: Радиус углов

**До:**
- Element имеет `cornerRadius = 8`
- Токен не привязан
- DSV показывает issue "Property Corner Radius не использует Design Token"

**После добавления функции:**
- DSV находит токен `radius/small` со значением `8`
- Отображается предложение: "💡 Найден подходящий токен: radius/small = 8"
- Кнопка "🔗 Привязать токен"
- При клике - токен автоматически привязывается

### Пример 2: Цвет заливки

**До:**
- Element имеет fill `rgb(248, 96, 37)` (оранжевый)
- Токен не привязан

**После:**
- DSV находит токен `color/primary` с таким же значением
- Отображается предложение
- Автоматическая привязка по клику

## Технические детали

### Файлы изменены

1. **code.js:**
   - Модифицированы все места создания issues типа "No Variable"
   - Добавлена функция `bindTokenToProperty(nodeId, property, tokenId)`
   - Добавлен обработчик сообщения `dsv-bind-token`
   - Функция `findTokenByValue()` уже существовала, теперь активно используется

2. **ui.html:**
   - Обновлена функция `createDSVIssueCard()` - добавлен параметр `issueIndex`
   - Добавлена функция `formatTokenValue()` для форматирования значений
   - Добавлена функция `bindTokenToIssue()` для отправки запроса на привязку
   - Добавлена функция `mapPropertyDisplayToKey()` для маппинга имён свойств
   - Добавлен обработчик сообщения `dsv-bind-token-result`

### Структура данных

**Issue с предложенным токеном:**
```javascript
{
  type: 'No Variable',
  severity: 'medium',
  property: 'Corner Radius',
  description: 'Property "Corner Radius" не использует Design Token',
  nodeId: 'node-id-here',
  nodeName: 'Button',
  value: 8,
  suggestedToken: {
    id: 'json-token-radius-small',
    name: 'radius/small',
    value: 8
  }
}
```

## Ограничения

1. **Токен должен существовать в Figma:**
   - Если токен только в JSON, но не создан в Figma - привязка невозможна
   - Отображается ошибка с предложением создать токен

2. **Точность сравнения:**
   - Числовые значения сравниваются с точностью ±0.01
   - Очень близкие, но не идентичные значения могут не найти токен

3. **Релевантность по имени:**
   - Для cornerRadius токен должен содержать "radius", "corner" или "rounded" в имени
   - Это предотвращает привязку несоответствующих токенов с таким же значением

## Будущие улучшения

- [ ] Поддержка пакетной привязки токенов (привязать все найденные токены одной кнопкой)
- [ ] Создание токенов из JSON прямо в Figma
- [ ] Настройка точности сравнения значений
- [ ] История привязок с возможностью отмены
- [ ] Автоматическая привязка при импорте токенов

## Тестирование

### Сценарий 1: Радиус углов
1. Создать элемент с `cornerRadius = 8`
2. Загрузить JSON с токеном `radius/small = 8`
3. Запустить DSV проверку
4. Убедиться что показывается предложенный токен
5. Нажать "Привязать токен"
6. Проверить что токен привязался в Figma

### Сценарий 2: Нет подходящего токена
1. Создать элемент с `cornerRadius = 13`
2. Загрузить JSON с токенами 8, 12, 16
3. Запустить DSV проверку
4. Убедиться что НЕ показывается кнопка привязки

### Сценарий 3: Цвет заливки
1. Создать элемент с заливкой `rgb(248, 96, 37)`
2. Загрузить JSON с цветовым токеном
3. Запустить DSV проверку
4. Проверить автоматическую привязку

## Changelog

### Version 3.1 (16 октября 2025)
- ✅ Добавлено автоматическое определение токенов по значению
- ✅ Добавлена кнопка "Привязать токен" в UI
- ✅ Реализована функция автоматической привязки
- ✅ Добавлена обработка результатов привязки
- ✅ Улучшен UX с визуальными индикаторами

### Version 3.0 (16 октября 2025)
- Стабильная версия DSV до добавления auto-bind

## Авторы

Разработано с использованием AI ассистента для улучшения Design System Validator.

