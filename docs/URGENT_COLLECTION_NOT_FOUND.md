# 🔍 СРОЧНО: Проблема найдена - Коллекция не локальная

## ❗ Обнаруженная проблема

Из логов видно:
```
⚠️ Коллекция не найдена локально, используем savedVariables
✓ Найден режим из savedVariables: Light
✅ Режим темы: "Light"  ← Неправильно!
```

**Коллекция "1. Theme" не найдена в локальных коллекциях Figma!**

---

## 🔍 Возможные причины

### 1. Коллекция импортирована из библиотеки (НЕ локальная)
- Вы используете внешнюю библиотеку компонентов
- Коллекция Variables находится в другом файле
- `getLocalVariableCollectionsAsync()` не возвращает импортированные коллекции

### 2. Название коллекции отличается
- В Figma коллекция называется не "1. Theme"
- Возможно, есть пробелы или другие символы
- Возможно, есть эмодзи в названии

### 3. Коллекция не публикована
- Коллекция существует, но не опубликована из библиотеки
- Режимы не доступны для импортированных переменных

---

## 📋 Действия для диагностики

### Шаг 1: Проверьте название коллекции (2 мин)

1. **Откройте панель Variables** в Figma
2. **Посмотрите название коллекции** в списке слева
3. **Скопируйте ТОЧНОЕ название** (включая пробелы, точки, эмодзи)

Ожидается: `1. Theme`

Возможные варианты:
- `1.Theme` (без пробела после точки)
- `1 Theme` (пробел вместо точки)
- `Theme` (без префикса)
- `🎨 1. Theme` (с эмодзи)

### Шаг 2: Проверьте, локальная ли коллекция (2 мин)

В панели Variables:
- Если коллекция **локальная** → справа от названия нет иконки библиотеки
- Если коллекция **импортированная** → справа есть иконка библиотеки 📚

### Шаг 3: Перезапустите плагин с новыми логами (3 мин)

Я добавил новые логи, которые покажут:
- Сколько локальных коллекций найдено
- Названия всех локальных коллекций

1. **Перезагрузите плагин** в Figma
2. **Запустите проверку** снова
3. **Откройте консоль**: `Plugins → Development → Open Console`
4. **Найдите строки:**
   ```
   📚 Всего локальных коллекций: X
   Локальные коллекции: "название1", "название2"...
   ```

5. **Скопируйте эти строки** и отправьте мне

---

## 🔧 Возможные решения

### Решение 1: Если коллекция локальная, но название не совпадает

**Исправьте название коллекции** в строке 1627 файла `code.js`:

```javascript
// Было:
const themeCollectionName = Object.keys(savedVariables).find(name => 
  name.startsWith('1.') || name.startsWith('1 ')
);

// Станет (пример, если коллекция называется "Theme"):
const themeCollectionName = Object.keys(savedVariables).find(name => 
  name.startsWith('1.') || name.startsWith('1 ') || name === 'Theme'
);
```

### Решение 2: Если коллекция импортирована из библиотеки

**Вариант A: Сделать коллекцию локальной**
1. Откройте файл-библиотеку, где находится коллекция
2. Скопируйте все переменные
3. Создайте локальную коллекцию в текущем файле
4. Вставьте переменные
5. Переключите элементы на локальную коллекцию

**Вариант B: Добавить поддержку импортированных коллекций (требует доработки кода)**

Это сложнее и требует изменений в коде плагина. Нужно использовать:
- `figma.variables.getVariableByIdAsync()` для получения переменной
- `figma.variables.getVariableCollectionByIdAsync()` для получения коллекции
- Поиск коллекции через `boundVariables` на узлах

### Решение 3: Использовать режим из родительского фрейма (быстрое решение)

Если вы применили режим Dark к фрейму, но плагин не видит коллекцию:

1. **Примените переменную из коллекции** к родительскому фрейму
   - Например, примените `orb-background/primary` к заливке фрейма
   - Плагин сможет найти коллекцию через эту переменную

2. **Затем запустите проверку** на текстовом элементе внутри фрейма

---

## 📊 Ожидаемый результат после исправления

После того, как коллекция будет найдена, вы увидите:

```
🔍 getModeForCollection: Ищем режим для коллекции "1. Theme"
   Начальный узел: Test Text (TEXT)
   📚 Всего локальных коллекций: 3
   Локальные коллекции: "1. Theme", "2. Product", "3. Spacing"
   ✓ Коллекция найдена локально, ID: VariableCollectionId:...
   Доступные режимы: Light, Dark
   
   [0] Проверяем узел: Test Text (TEXT)
   ⚠️ Режим не найден на узле "Test Text"
   
   [1] Проверяем узел: Dark Container (FRAME)
   ✅ Найден режим "Dark" на узле "Dark Container"
   
✅ Режим темы: "Dark"  ← Правильно!
```

И плагин выберет правильный токен:
```
✅ Найдено совпадение в 1.Theme: orb-text/primary в режиме Dark
✅ Выбрана переменная: orb-text/primary  ← Правильно!
```

---

## 🚨 Следующие шаги

1. **Запустите плагин с новыми логами** (уже добавлены)
2. **Найдите строки с локальными коллекциями** в консоли
3. **Отправьте мне:**
   - Точное название коллекции из Figma
   - Список локальных коллекций из логов
   - Скриншот панели Variables (опционально)

После этого я смогу точно сказать, как исправить проблему!

---

*Срочная диагностика проблемы с режимом Light/Dark*
*Для плагина Orbita DS Tools v3.x*
*Декабрь 2024*

