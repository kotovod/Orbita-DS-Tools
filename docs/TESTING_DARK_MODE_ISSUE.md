# 🧪 Инструкция по тестированию и отладке проблемы с режимом Dark

## Проблема
Плагин находит `orb-text/inverse` вместо `orb-text/primary` для цвета `#F3F4F5` в Dark режиме.

## Что было сделано

### ✅ Добавлены детальные логи

В код добавлены подробные логи для отладки:

1. **В `getModeForCollection`:**
   - Показывает, какие узлы проверяются
   - Показывает, на каком узле найден режим
   - Показывает доступные режимы коллекции

2. **В `getThemeModeForNode`:**
   - Показывает найденный режим темы
   - Показывает, если режим не найден

3. **В `findColorVariable` (ШАГ 2):**
   - Показывает каждую проверяемую переменную
   - Показывает доступные режимы у переменной
   - Показывает цвет в нужном режиме
   - Показывает результат сравнения

---

## 📋 Как протестировать

### Шаг 1: Подготовка в Figma

1. **Создайте фрейм "Dark Container":**
   ```
   Инструмент: Frame (F)
   Размер: 400x300px
   Имя: "Dark Container"
   ```

2. **Примените режим Dark к фрейму:**
   - Выделите фрейм
   - Откройте панель Variables (справа, иконка Variables)
   - Найдите секцию "Applied variables"
   - Нажмите на поле рядом с коллекцией "1. Theme" (или как она называется)
   - Выберите режим: **Dark**

3. **Создайте текстовый элемент внутри фрейма:**
   ```
   Инструмент: Text (T)
   Текст: "Test Text Dark"
   Поместите внутрь "Dark Container"
   ```

4. **Установите hardcoded цвет:**
   - Выделите текст
   - В панели справа найдите Fill
   - Нажмите на цвет
   - Введите HEX: `F3F4F5`
   - **НЕ** нажимайте на иконку переменной!

### Шаг 2: Запуск плагина

1. Выделите фрейм "Dark Container" (или сам текст)
2. Запустите плагин
3. Перейдите в раздел "Design System Validator"
4. Нажмите "Check Selection"

### Шаг 3: Открыть консоль

1. В Figma: меню `Plugins → Development → Open Console`
2. Консоль откроется в отдельном окне

### Шаг 4: Анализ логов

Ищите в логах следующее:

#### 1. Определение режима:

```
🎨 getThemeModeForNode: Определяем режим темы для узла "Test Text Dark"
   ✓ Коллекция темы: "1. Theme"

🔍 getModeForCollection: Ищем режим для коллекции "1. Theme"
   Начальный узел: Test Text Dark (TEXT)
   ✓ Коллекция найдена локально, ID: VariableCollectionId:...
   Доступные режимы: Light, Dark
   
   [0] Проверяем узел: Test Text Dark (TEXT)
   ⚠️ Режим не найден на узле "Test Text Dark"
   
   [1] Проверяем узел: Dark Container (FRAME)
   ✅ Найден режим "Dark" на узле "Dark Container"   ← ВАЖНО!
   
   ✅ Режим темы: "Dark"   ← ВАЖНО!
```

**Если здесь "Dark" ✅** - режим определяется правильно!

#### 2. Поиск в 1.Theme:

```
📍 ШАГ 2 (ЗАПАСНОЙ): Ищем переменные напрямую в коллекции 1.Theme
   Ищем цвет: #F3F4F5 (opacity: 100%)
   Тип узла: TEXT, Имя узла: Test Text Dark
   ✓ Коллекция 1.Theme: 1. Theme
   ✓ Найдено X видимых переменных в 1.Theme
   ✓ После фильтрации по правилам: Y переменных

🔍 Начинаем поиск совпадений в режиме "Dark":   ← ВАЖНО!

   Проверяем переменную: orb-text/primary
      Доступные режимы: Light, Dark
      Цвет в режиме "Dark": #F3F4F5   ← ВАЖНО!
      Parsed HEX: #F3F4F5, opacity: 100%
      Искомый HEX: #F3F4F5, opacity: 100%
      ✅ СОВПАДЕНИЕ! Добавляем orb-text/primary в список   ← ВАЖНО!

   Проверяем переменную: orb-text/inverse
      Доступные режимы: Light, Dark
      Цвет в режиме "Dark": #34343C   ← Должно НЕ совпадать!
      Parsed HEX: #34343C, opacity: 100%
      Искомый HEX: #F3F4F5, opacity: 100%
      ❌ Не совпадает   ← ВАЖНО!
```

**Если здесь `primary` совпадает, а `inverse` нет ✅** - логика работает правильно!

#### 3. Выбор токена:

```
✅ ШАГ 2: Найдено 1 совпадений в 1.Theme
   1. orb-text/primary (priority: 1)
   
✅ Выбрана переменная из 1.Theme: orb-text/primary   ← ДОЛЖНО БЫТЬ ТАК!
```

**Если здесь выбран `primary` ✅** - всё работает правильно!

---

## ❓ Что делать, если проблема осталась

### Случай 1: Режим определяется как "Light"

```
✅ Режим темы: "Light"   ← ❌ Неправильно!
```

**Причина:** Режим не применён к фрейму или плагин не нашёл его.

**Решение:**
1. Проверьте, что режим Dark действительно применён к фрейму
2. В панели Variables убедитесь, что показано "Dark"
3. Попробуйте применить режим снова

### Случай 2: Режим "Dark", но выбирается "inverse"

```
✅ Режим темы: "Dark"   ← ✅ Правильно
...
✅ Выбрана переменная: orb-text/inverse   ← ❌ Неправильно!
```

**Причина:** Проблема в логике выбора токена.

**Решение:**
1. Проверьте логи - какие переменные нашлись в поиске
2. Возможно, `inverse` находится раньше в списке
3. Нужно исправить приоритет выбора

### Случай 3: Не находит ни одной переменной

```
⚠️ В коллекции 1.Theme не найдено совпадений
```

**Причина:** Возможно, токены не импортированы или структура данных неправильная.

**Решение:**
1. Проверьте, что токены импортированы: DSV → Import Tokens
2. Проверьте файл `variables.json`
3. Перезагрузите плагин

---

## 📄 Отправка логов

Если проблема не решается, скопируйте **все логи** из консоли и отправьте разработчику.

Для копирования логов:
1. Кликните правой кнопкой в консоли
2. Выберите "Save as..." или просто выделите и скопируйте текст
3. Сохраните в файл `debug-logs.txt`

---

## 🎯 Ожидаемый результат

После исправления логики:

```
Frame "Dark Container" [Режим: Dark]
  └─ Text "Test" [Цвет: #F3F4F5, hardcoded]
  
Запуск проверки:
  ✓ Режим определён: Dark
  ✓ Найден токен: orb-text/primary
  ✓ Токен применён: orb-text/primary (Dark = #F3F4F5)
  ✓ Визуально ничего не изменилось
```

---

*Инструкция по тестированию проблемы с режимом Dark*
*Для плагина Orbita DS Tools v3.x*
*Декабрь 2024*

