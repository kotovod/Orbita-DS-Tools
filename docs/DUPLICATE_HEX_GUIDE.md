# 🎨 Справка: Токены с одинаковыми HEX-значениями

## Проблема: Один HEX — несколько токенов

В дизайн-системе Orbita один и тот же HEX-код может соответствовать **нескольким токенам с разными scopes**.

Это нормально и правильно! Плагин автоматически выбирает правильный токен в зависимости от:
1. **Типа элемента** (Text, Frame, Shape)
2. **Scopes токена** (TEXT_FILL, FRAME_FILL, SHAPE_FILL)
3. **Контекста применения** (fills, strokes)

---

## 📋 Примеры одинаковых HEX

### Пример 1: #F86025 (Light Mode)

Этот HEX соответствует **двум разным токенам**:

#### Токен 1: `orb-text/brand`
```json
{
  "key": "34a413e23fb14077b19759864151d4bbdc13d088",
  "id": "VariableID:38:884",
  "scopes": ["SHAPE_FILL", "TEXT_FILL"],
  "Light": "#F86025",
  "Dark": "#FF9168"
}
```
**Применяется к:**
- ✅ Текстовые элементы (Text)
- ✅ Векторные shape (Rectangle, Ellipse, Polygon)
- ❌ Фреймы

---

#### Токен 2: `orb-surface/brand/brand-hovered`
```json
{
  "key": "4b0594cf3cd9cfb7b633f8b7ce44464d4181ed1c",
  "id": "VariableID:59:2797",
  "scopes": ["FRAME_FILL"],
  "Light": "#F86025",
  "Dark": "#FF7F4D"
}
```
**Применяется к:**
- ✅ Фреймы (Frame)
- ✅ Компоненты
- ❌ Текстовые элементы
- ❌ Векторные shape

---

### Как плагин выбирает токен?

#### Ситуация 1: Текстовый элемент с цветом #F86025
```
Элемент: Text "Hello"
Свойство: fills (цвет текста)
HEX: #F86025
Требуемые scopes: ['TEXT_FILL', 'ALL_SCOPES']

Проверка токенов:
✅ orb-text/brand → scopes = ['TEXT_FILL', 'SHAPE_FILL'] → Подходит!
❌ orb-surface/brand/brand-hovered → scopes = ['FRAME_FILL'] → Не подходит

РЕЗУЛЬТАТ: Применяется orb-text/brand
```

#### Ситуация 2: Фрейм с заливкой #F86025
```
Элемент: Frame "Container"
Свойство: fills (заливка)
HEX: #F86025
Требуемые scopes: ['FRAME_FILL', 'SHAPE_FILL', 'ALL_SCOPES']

Проверка токенов:
❌ orb-text/brand → scopes = ['TEXT_FILL', 'SHAPE_FILL'] → Не подходит
✅ orb-surface/brand/brand-hovered → scopes = ['FRAME_FILL'] → Подходит!

РЕЗУЛЬТАТ: Применяется orb-surface/brand/brand-hovered
```

#### Ситуация 3: Прямоугольник с заливкой #F86025
```
Элемент: Rectangle "Shape"
Свойство: fills (заливка)
HEX: #F86025
Требуемые scopes: ['SHAPE_FILL', 'ALL_SCOPES']

Проверка токенов:
✅ orb-text/brand → scopes = ['TEXT_FILL', 'SHAPE_FILL'] → Подходит!
❌ orb-surface/brand/brand-hovered → scopes = ['FRAME_FILL'] → Не подходит

РЕЗУЛЬТАТ: Применяется orb-text/brand
```

---

### Пример 2: #F3F4F5 (Проблема с inverse)

Этот HEX имеет **особенность** - один токен в Dark, другой в Light:

#### Токен 1: `orb-text/primary`
```json
{
  "Light": "#34343C",
  "Dark": "#F3F4F5",
  "scopes": ["SHAPE_FILL", "TEXT_FILL"]
}
```
**Применяется к:** Text, Shape
**Режим:** Dark

#### Токен 2: `orb-text/inverse`
```json
{
  "Light": "#F3F4F5",
  "Dark": "#34343C",
  "scopes": ["SHAPE_FILL", "TEXT_FILL"]
}
```
**Применяется к:** Text, Shape
**Режим:** Light

⚠️ **Важно:** Для правильной работы плагин должен определить режим объекта (Light/Dark). Если текст находится вне фрейма с привязанным режимом, плагин может выбрать неправильный токен!

**Решение:** Всегда помещайте элементы внутрь фрейма с примененным режимом из коллекции `1.Theme`.

---

### Пример 3: #FF9168 (Dark Mode)

Этот HEX также соответствует **нескольким токенам** в Dark режиме:

#### Токен 1: `orb-text/brand`
```json
{
  "Light": "#F86025",
  "Dark": "#FF9168",
  "scopes": ["SHAPE_FILL", "TEXT_FILL"]
}
```

#### Токен 2: `orb-surface/brand/brand`
```json
{
  "Light": "#FF7F4D",
  "Dark": "#FF9168",
  "scopes": ["FRAME_FILL"]
}
```

**Логика выбора такая же:** плагин проверяет scopes и выбирает подходящий токен.

---

## 🎯 Важные правила

### Правило 1: Scopes определяют применимость
**Токен может применяться к элементу ТОЛЬКО если:**
- Его scopes пересекаются с требуемыми scopes для свойства
- ИЛИ у токена нет scopes (универсальный)
- ИЛИ у токена есть `ALL_SCOPES`

### Правило 2: Один тип элемента — один токен
Для каждого типа элемента и каждого HEX плагин найдёт **только один подходящий токен**.

### Правило 3: Приоритет по коллекциям
Если найдено несколько подходящих токенов:
1. **1. Theme** (публичные) — высший приоритет
2. **2. Product** (скрытые) — средний приоритет
3. **2.X** (контекстные) — низший приоритет

---

## 🧪 Как тестировать

### Тест: Один HEX, разные элементы

**Создайте 3 элемента с одинаковым HEX #F86025:**

1. **Текстовый элемент:**
   ```
   Элемент: Text "Brand Text"
   Цвет: #F86025
   Ожидаемый токен: orb-text/brand
   ```

2. **Фрейм:**
   ```
   Элемент: Frame "Brand Surface"
   Заливка: #F86025
   Ожидаемый токен: orb-surface/brand/brand-hovered
   ```

3. **Прямоугольник:**
   ```
   Элемент: Rectangle "Brand Shape"
   Заливка: #F86025
   Ожидаемый токен: orb-text/brand
   ```

**Запустите проверку и убедитесь:**
- ✅ Каждый элемент получил правильный токен
- ✅ Токены соответствуют scopes элемента
- ✅ Визуально цвет не изменился

---

## 📊 Полная таблица токенов с #F86025

| HEX | Режим | Токен | Scopes | Применяется к |
|-----|-------|-------|--------|---------------|
| #F86025 | Light | orb-text/brand | TEXT_FILL, SHAPE_FILL | Text, Shape |
| #F86025 | Light | orb-surface/brand/brand-hovered | FRAME_FILL | Frame |
| #FF9168 | Dark | orb-text/brand | TEXT_FILL, SHAPE_FILL | Text, Shape |
| #FF9168 | Dark | orb-surface/brand/brand | FRAME_FILL | Frame |

---

## 💡 Рекомендации для дизайнеров

### При создании дизайна:

1. **Используйте правильные токены сразу:**
   - Для текста → токены с `TEXT_FILL`
   - Для фреймов → токены с `FRAME_FILL`
   - Для shape → токены с `SHAPE_FILL`

2. **Не полагайтесь только на HEX:**
   - Один HEX может означать разные токены
   - Проверяйте название токена в панели Variables

3. **Используйте плагин для проверки:**
   - Запускайте Design System Validator регулярно
   - Проверяйте, что hardcoded значения заменены на токены

---

## 🐛 Частые вопросы

### Q: Почему плагин применяет не тот токен?
**A:** Проверьте:
1. Тип элемента (Text/Frame/Shape)
2. Scopes токена
3. Режим объекта (Light/Dark)

### Q: Можно ли применить orb-text/brand к фрейму?
**A:** Нет, потому что:
- `orb-text/brand` имеет scopes: TEXT_FILL, SHAPE_FILL
- Фрейм требует FRAME_FILL
- Используйте `orb-surface/brand/...` для фреймов

### Q: Как узнать, какой токен правильный?
**A:**
1. Откройте панель Variables в Figma
2. Найдите токен по HEX
3. Посмотрите его scopes
4. Сравните с типом элемента

### Q: Что делать если плагин ничего не находит?
**A:**
1. Проверьте, что токены импортированы
2. Проверьте HEX (точное совпадение)
3. Проверьте режим (Light/Dark)
4. Посмотрите логи в консоли Figma

---

## 📚 См. также

- `SCOPES_GUIDE.md` - Подробное руководство по scopes
- `DESIGN_SYSTEM_STRUCTURE.md` - Структура дизайн-системы
- `TEST_ENVIRONMENT_SETUP.md` - Настройка тестовой среды
- `QUICK_TEST_CHECKLIST.md` - Чеклист тестирования

---

*Справочник по токенам с одинаковыми HEX для плагина Orbita DS Tools v3.x*
*Последнее обновление: Декабрь 2024*

