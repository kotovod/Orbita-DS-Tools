# 🔧 Диагностика: Проверка скрытых объектов

## Проблема решена!

Оказалось, что в плагине используются **ДВЕ разные системы валидации**:

1. **`validateDesignSystem()`** - новая версия (строка ~6828)
2. **`checkNumericVariables()`** - старая Token Guard версия (строка ~852)

Текущая версия использует **`checkNumericVariables()`**, и проверка скрытых объектов была добавлена только в `validateDesignSystem()`.

## Что исправлено

### 1. Добавлена проверка в `checkNumericVariables()` (строка ~914)

```javascript
// Теперь обрабатываем все ноды асинхронно
for (const node of nodesToCheck) {
  // Проверка скрытых объектов - ПЕРВАЯ проверка
  if (options.skipHiddenObjects !== false && isNodeOrParentHidden(node)) {
    console.log(`⏭️ DSV: Пропущен скрытый объект "${node.name}" (${node.type})`);
    continue;
  }
  
  // Остальные проверки...
}
```

### 2. Добавлено диагностическое логирование

**В UI (`ui.html`):**
- Логирует состояние чекбокса
- Логирует финальное значение при отправке

**В плагине (`code.js`):**
- `checkNumericVariables()` - логирует получение настройки
- `isNodeOrParentHidden()` - детально логирует проверку каждого уровня иерархии
- `checkNodeVariables()` - логирует процесс проверки

## Как проверить (с диагностикой)

### 1. Откройте консоль DevTools

В Figma: `Plugins → Development → Open Console`

### 2. Скройте тестовый объект

Создайте Frame с элементами, скройте через иконку глаза.

### 3. Запустите проверку

Вы увидите подробные логи:

```
🔧 DSV UI: Отправка настроек в плагин: {
  skipHiddenObjects: true,
  ...
}

🔍 DSV: Проверка видимости для "Frame 1" (FRAME)
  └─ [0] FRAME "Frame 1": visible=false
  ✓ СКРЫТ на уровне 0: FRAME "Frame 1"

⏭️ DSV: Пропущен скрытый объект "Frame 1" (FRAME)
```

### 4. Для вложенных объектов:

```
🔍 DSV: Проверка видимости для "Instance 1" (INSTANCE)
  └─ [0] INSTANCE "Instance 1": visible=true
    └─ [1] FRAME "Parent Frame": visible=false
  ✓ СКРЫТ на уровне 1: FRAME "Parent Frame"

⏭️ DSV: Пропущен скрытый объект "Instance 1" (INSTANCE)
```

## После тестирования

Можно **удалить** или **закомментировать** детальное логирование в `isNodeOrParentHidden()` для производительности. Базовое логирование можно оставить.

## Теперь работает в обеих системах

✅ `checkNumericVariables()` - текущая активная система  
✅ `validateDesignSystem()` - новая система (на будущее)  
✅ Рекурсивная проверка родителей  
✅ Детальное логирование для отладки  

## Важно: Перезагрузка плагина

После изменений кода **обязательно перезагрузите плагин**:
1. Закройте плагин
2. `Plugins → Development → Orbita DS Tools → Validator`
3. Проверьте снова

Изменения в `code.js` применяются только после перезагрузки!

