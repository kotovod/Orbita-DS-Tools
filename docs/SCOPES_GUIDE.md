# 📖 Руководство по Scopes в Figma Variables

## Что такое Scopes?

**Scopes** (области применения) определяют, к каким свойствам элементов можно привязать переменную в Figma.

Когда вы создаёте переменную в Figma, вы выбираете её scopes, которые ограничивают, где эта переменная может использоваться.

---

## 🎨 Scopes для цветовых переменных

### TEXT_FILL
**Назначение:** Заливка текста
**Применяется к:**
- Текстовые элементы (Text layers)
- Цвет текста в компонентах

**НЕ применяется к:**
- Фреймы
- Прямоугольники
- Векторные shape
- Обводки

**Пример токенов:**
- `orb-text/primary`
- `orb-text/secondary`
- `orb-text/brand`

**Как создать в Figma:**
1. Variables → Create Variable → Type: Color
2. В разделе Scopes выберите только: `Text fill`

---

### SHAPE_FILL
**Назначение:** Заливка векторных фигур
**Применяется к:**
- Прямоугольники (Rectangle)
- Эллипсы (Ellipse)
- Многоугольники (Polygon)
- Векторные shape
- Иконки

**НЕ применяется к:**
- Текстовые элементы
- Фреймы (используйте FRAME_FILL)

**Пример токенов:**
- `orb-icon/primary`
- Токены для иконок и векторной графики

**Как создать в Figma:**
1. Variables → Create Variable → Type: Color
2. В разделе Scopes выберите: `Shape fill`

---

### FRAME_FILL
**Назначение:** Заливка фреймов и компонентов
**Применяется к:**
- Frame
- Component
- Instance

**Пример токенов:**
- `orb-background/primary`
- `orb-background/secondary`
- `orb-surface/primary`

**Как создать в Figma:**
1. Variables → Create Variable → Type: Color
2. В разделе Scopes выберите: `Frame fill`

---

### STROKE_COLOR
**Назначение:** Цвет обводки (stroke)
**Применяется к:**
- Обводки фреймов
- Обводки shape
- Линии (Line)
- Borders

**Пример токенов:**
- `orb-border/primary`
- `orb-line/default`

**Как создать в Figma:**
1. Variables → Create Variable → Type: Color
2. В разделе Scopes выберите: `Stroke`

---

### ALL_SCOPES (множественные scopes)
**Назначение:** Универсальные переменные
**Применяется к:** Всему, что выбрано в scopes

**Пример:**
Токен с scopes: `TEXT_FILL`, `SHAPE_FILL`, `FRAME_FILL`
- Может применяться к текстам
- Может применяться к shape
- Может применяться к фреймам

**Как создать в Figma:**
1. Variables → Create Variable → Type: Color
2. В разделе Scopes выберите ВСЕ нужные области

---

## 📏 Scopes для числовых переменных

### GAP
**Назначение:** Отступы и промежутки
**Применяется к:**
- `paddingLeft`, `paddingRight`, `paddingTop`, `paddingBottom`
- `itemSpacing` (gap между элементами в Auto Layout)

**Пример токенов:**
- Все токены из коллекции `Spacing`
- `0m`, `1m`, `2m`, `4m`, `6m`, `8m`...

**Как создать в Figma:**
1. Variables → Create Variable → Type: Number
2. В разделе Scopes выберите: `Gap`

---

### CORNER_RADIUS
**Назначение:** Скругление углов
**Применяется к:**
- `cornerRadius` (все углы)
- `topLeftRadius`, `topRightRadius`, `bottomLeftRadius`, `bottomRightRadius`

**Пример токенов:**
- Все токены из коллекции `Rounding`
- `none`, `xs`, `sm`, `md`, `lg`, `xl`, `full`

**Как создать в Figma:**
1. Variables → Create Variable → Type: Number
2. В разделе Scopes выберите: `Corner radius`

---

### WIDTH_HEIGHT
**Назначение:** Размеры элементов
**Применяется к:**
- `width` (ширина)
- `height` (высота)

**Пример токенов:**
- Токены из коллекции `Sizing`

**Как создать в Figma:**
1. Variables → Create Variable → Type: Number
2. В разделе Scopes выберите: `Width and height`

---

### STROKE_FLOAT
**Назначение:** Толщина обводки
**Применяется к:**
- `strokeWeight` (толщина stroke)

**Пример токенов:**
- Токены из коллекции `Line`
- `1`, `2`, `3`, `4`...

**Как создать в Figma:**
1. Variables → Create Variable → Type: Number
2. В разделе Scopes выберите: `Stroke weight`

---

## 🔍 Как работает фильтрация по Scopes в плагине

### Алгоритм проверки:

```javascript
function isScopeCompatible(variableScopes, requiredScopes) {
  // Если у переменной нет scopes - она универсальная
  if (!variableScopes || variableScopes.length === 0) {
    return true;
  }
  
  // Если у переменной есть ALL_SCOPES - она универсальная
  if (variableScopes.includes('ALL_SCOPES')) {
    return true;
  }
  
  // Проверяем пересечение scopes
  return requiredScopes.some(reqScope => variableScopes.includes(reqScope));
}
```

### Примеры проверки:

#### Пример 1: Цвет текста
```
Элемент: Text "Hello"
Свойство: fills (цвет текста)
Требуемые scopes: ['TEXT_FILL', 'ALL_SCOPES']

Токен A: scopes = ['TEXT_FILL'] → ✅ Подходит (есть пересечение)
Токен B: scopes = ['SHAPE_FILL'] → ❌ Не подходит (нет пересечения)
Токен C: scopes = ['TEXT_FILL', 'SHAPE_FILL'] → ✅ Подходит (есть TEXT_FILL)
Токен D: scopes = [] → ✅ Подходит (универсальная)
Токен E: scopes = ['ALL_SCOPES'] → ✅ Подходит (универсальная)
```

#### Пример 2: Заливка фрейма
```
Элемент: Frame "Container"
Свойство: fills (цвет заливки)
Требуемые scopes: ['FRAME_FILL', 'ALL_SCOPES']

Токен A: scopes = ['FRAME_FILL'] → ✅ Подходит
Токен B: scopes = ['TEXT_FILL'] → ❌ Не подходит
Токен C: scopes = ['SHAPE_FILL', 'FRAME_FILL'] → ✅ Подходит
```

#### Пример 3: Padding
```
Элемент: Auto Layout Frame
Свойство: paddingLeft
Требуемые scopes: ['GAP', 'ALL_SCOPES']

Токен A: scopes = ['GAP'] → ✅ Подходит
Токен B: scopes = ['CORNER_RADIUS'] → ❌ Не подходит
Токен C: scopes = [] → ✅ Подходит (универсальная)
```

#### Пример 4: Corner Radius
```
Элемент: Frame
Свойство: cornerRadius
Требуемые scopes: ['CORNER_RADIUS', 'ALL_SCOPES']

Токен A: scopes = ['CORNER_RADIUS'] → ✅ Подходит
Токен B: scopes = ['GAP'] → ❌ Не подходит
```

---

## 💡 Рекомендации по настройке Scopes

### Для цветовых токенов:

**Семантические токены (1.Theme):**
```
orb-text/primary       → TEXT_FILL
orb-text/secondary     → TEXT_FILL
orb-background/primary → FRAME_FILL, SHAPE_FILL
orb-icon/primary       → SHAPE_FILL
orb-border/primary     → STROKE_COLOR
```

**Универсальные токены:**
```
orb-brand              → TEXT_FILL, SHAPE_FILL, FRAME_FILL
orb-feedback/warning   → TEXT_FILL, SHAPE_FILL, FRAME_FILL
```

### Для числовых токенов:

**Spacing:**
```
0m, 1m, 2m, 4m...      → GAP (или ALL_SCOPES)
```

**Rounding:**
```
none, xs, sm, md...    → CORNER_RADIUS (или ALL_SCOPES)
```

**Sizing:**
```
Токены размеров        → WIDTH_HEIGHT (или ALL_SCOPES)
```

**Line:**
```
Токены толщины линий   → STROKE_FLOAT (или ALL_SCOPES)
```

---

## ⚠️ Частые ошибки

### Ошибка 1: Неправильные scopes для текстовых токенов
```
❌ Плохо: orb-text/primary с scope SHAPE_FILL
✅ Хорошо: orb-text/primary с scope TEXT_FILL
```

### Ошибка 2: Слишком широкие scopes
```
❌ Плохо: Все токены с ALL_SCOPES
✅ Хорошо: Конкретные scopes для каждого типа токенов
```

### Ошибка 3: Применение токена не по назначению
```
❌ Плохо: Применить orb-text/primary к фрейму
✅ Хорошо: Применить orb-background/primary к фрейму
```

### Ошибка 4: Отсутствие scopes
```
❌ Плохо: Токен без scopes (хотя должны быть конкретные)
✅ Хорошо: Явно указать нужные scopes или ALL_SCOPES
```

---

## 🧪 Тестирование Scopes

### Чеклист проверки:

```
□ Текстовый токен (TEXT_FILL):
  ✅ Применяется к текстовым элементам
  ❌ Не применяется к фреймам

□ Фоновый токен (FRAME_FILL):
  ✅ Применяется к фреймам
  ❌ Не применяется к текстовым элементам

□ Токен для shape (SHAPE_FILL):
  ✅ Применяется к прямоугольникам и векторам
  ❌ Не применяется к текстовым элементам

□ Универсальный токен (ALL_SCOPES):
  ✅ Применяется ко всем типам элементов

□ Spacing токен (GAP):
  ✅ Применяется к padding и itemSpacing
  ❌ Не применяется к cornerRadius

□ Rounding токен (CORNER_RADIUS):
  ✅ Применяется к скруглениям углов
  ❌ Не применяется к padding
```

---

## 📚 Дополнительные ресурсы

### Документация Figma:
- [Variables in Figma](https://help.figma.com/hc/en-us/articles/15339657135383-Guide-to-variables-in-Figma)
- [Scopes for variables](https://help.figma.com/hc/en-us/articles/15343103619095-Apply-variables-to-designs)

### Внутренние документы:
- `DESIGN_SYSTEM_STRUCTURE.md` - структура дизайн-системы
- `TEST_ENVIRONMENT_SETUP.md` - настройка тестовой среды
- `QUICK_TEST_CHECKLIST.md` - чеклист тестирования

---

*Руководство по Scopes для плагина Orbita DS Tools v3.x*
*Последнее обновление: Декабрь 2024*

