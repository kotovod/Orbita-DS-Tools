# AI Design Lint — Улучшения

## ✅ Реализованные улучшения

### 1. **Исправлена навигация к проблемным элементам**

**Проблема:** Переход из результатов анализа на проблемный фрейм не работал.

**Решение:**
- Улучшен парсинг ответов AI для извлечения ID элементов в формате `[ID:element_id]`
- AI теперь обязательно указывает ID элемента для каждой найденной проблемы
- Добавлен fallback-поиск по имени элемента, если ID не указан
- Улучшена логика поиска элементов в `allNodes` карте

**Как это работает:**
1. AI анализирует компонент и для каждой проблемы указывает ID в формате: `[ID:123:456] Описание проблемы`
2. Парсер извлекает ID и сохраняет его в объекте issue
3. При клике на кнопку "→ Перейти" плагин находит элемент по ID и фокусируется на нём

### 2. **Глубокий анализ всех параметров компонента**

**Что собирается:**

#### Базовая информация:
- ID, имя, тип, видимость элемента
- Размеры (width, height) и позиция (x, y)
- Структура дерева элементов с дочерними элементами

#### Цвета и заливки:
- Все fills с их параметрами (type, color, opacity)
- **Привязка к переменным/токенам** — если цвет определён переменной, собирается информация о токене
- Флаг `isHardcoded: true` для жёстко закодированных цветов
- Информация о переменной: имя, ID, тип

#### Обводки (Strokes):
- Тип, цвет, прозрачность
- Толщина обводки (strokeWeight)

#### Текст:
- Содержимое (characters)
- Размер шрифта (fontSize)
- Шрифт (fontName)
- Выравнивание (textAlignHorizontal, textAlignVertical)
- Межстрочный интервал (lineHeight)
- Межбуквенный интервал (letterSpacing)
- **Привязка fontSize к переменной** — флаг `fontSizeHardcoded: true` если размер не использует токен

#### Auto Layout:
- Режим (mode: HORIZONTAL/VERTICAL)
- Sizing modes (primaryAxisSizingMode, counterAxisSizingMode)
- Отступы (paddingLeft, paddingRight, paddingTop, paddingBottom)
- Расстояние между элементами (itemSpacing)
- Выравнивание (primaryAxisAlignItems, counterAxisAlignItems)
- **Проверка нестандартных отступов**: `hasNonStandardSpacing: true` и массив `nonStandardValues` для значений, не соответствующих стандартной сетке 4px

#### Border Radius:
- Значение cornerRadius
- Флаг `nonStandardCornerRadius: true` если значение не соответствует стандартным

#### Эффекты:
- Тени, размытие и другие эффекты
- Видимость, радиус, смещение

#### Компоненты:
- Описание (description)
- Свойства вариантов (variantProperties) для Component
- Список всех вариантов (variantsList) для Component Set с их ID и properties
- Информация об overrides для Instance
- Ссылка на main component для Instance

### 3. **Добавлен контекст дизайн-системы Orbita**

**Что включено:**

#### Токены из всех коллекций переменных:
```javascript
{
  variables: {
    colors: [...],      // Все цветовые токены с их значениями
    spacing: [...],     // Токены отступов, gap, padding
    typography: [...],  // Токены шрифтов, размеров текста
    other: [...]        // Остальные переменные
  },
  collections: [...]    // Полная информация о всех коллекциях
}
```

#### Правила дизайн-системы:
- **Spacing**: Стандартные значения [0, 4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64]px
- **Corner Radius**: Стандартные значения [0, 2, 4, 6, 8, 12, 16, 20, 24, 32]px
- **Touch Targets**: Минимум 44px для интерактивных элементов
- **Contrast**: WCAG AA стандарты (4.5:1 для обычного текста, 3.0:1 для крупного)

### 4. **Улучшен AI-промпт**

**Новые возможности:**

1. **Конкретная информация о токенах:**
   - AI получает список всех доступных токенов дизайн-системы
   - При обнаружении hard-coded значения AI предлагает конкретный токен для замены
   - Примеры: "Используй токен spacing-2 (8px)", "Замени на color-primary-500"

2. **Проверка на соответствие правилам:**
   - AI проверяет отступы на кратность 4px
   - Проверяет borderRadius на соответствие стандартным значениям
   - Проверяет размеры touch targets (минимум 44px)
   - Проверяет контраст цветов по формуле WCAG

3. **Детальный технический анализ:**
   - Проверка всех fills на наличие привязки к переменным
   - Выявление `hasNonStandardSpacing: true` в autoLayout
   - Проверка `fontSizeHardcoded: true` для текстов
   - Анализ структуры компонента и именования слоёв

4. **Формат ответа с ID элементов:**
   - AI обязательно указывает ID проблемного элемента: `[ID:element_id]`
   - Это обеспечивает корректную навигацию к проблемным элементам

## 📊 Примеры анализа

### До улучшений:
```
❌ Проверьте размеры шрифта
❌ Возможно, есть проблемы с отступами
❌ Рекомендуется использовать токены
```

### После улучшений:
```
✅ [ID:123:456] Текст использует fontSize: 13px (hard-coded), 
   рекомендуется заменить на токен text-sm (14px)

✅ [ID:789:012] Отступ paddingLeft: 7px не соответствует 
   стандартным значениям сетки 4px, используй 8px (токен: spacing-2)

✅ [ID:345:678] Цвет fill: rgb(0.2, 0.3, 0.4) hard-coded, 
   используй токен color-primary-500 из коллекции Orbita Colors

✅ [ID:901:234] Corner radius: 15px не стандартный, 
   используй 16px (токен: radius-md)

✅ [ID:567:890] Контраст текста 3.2:1 не проходит WCAG AA (требуется 4.5:1), 
   измени цвет текста на более контрастный
```

## 🚀 Как использовать

1. **Выбери компонент(ы)** в Figma
2. **Запусти AI Design Lint** через меню плагина
3. **Введи Yandex OAuth Token** (сохраняется локально)
4. **Нажми "Анализировать дизайн"**
5. **Получи детальный отчёт** с конкретными проблемами и рекомендациями
6. **Кликни "→ Перейти"** чтобы перейти к проблемному элементу
7. **Исправь проблемы** следуя рекомендациям AI

## 🎯 Code Readiness Score

AI оценивает готовность компонента к передаче в разработку по шкале 0-100%:

- **80-100%** 🟢 — Готов к разработке
- **60-79%** 🟡 — Требуются небольшие доработки
- **0-59%** 🔴 — Требуется значительная доработка

## 📝 Что AI проверяет

### 1. Hard-coded значения vs Токены
- ✅ Цвета определены через variables
- ✅ Размеры шрифта используют токены
- ✅ Отступы соответствуют стандартной сетке

### 2. Accessibility
- ✅ Контраст цветов (WCAG AA)
- ✅ Размеры touch targets (44x44px минимум)
- ✅ Читаемость текста

### 3. Структура компонента
- ✅ Правильность Auto Layout
- ✅ Именование слоёв
- ✅ Соответствие дизайн-системе Orbita

### 4. Consistency
- ✅ Использование стандартных значений spacing
- ✅ Использование стандартных border-radius
- ✅ Наличие всех необходимых состояний (hover, disabled, etc)

## 💡 Советы

1. **Используй описательные имена слоёв** — AI лучше их распознаёт
2. **Применяй токены везде, где возможно** — это основа хорошей дизайн-системы
3. **Следуй сетке 4px** для всех отступов
4. **Проверяй контраст** важных текстов
5. **Документируй компоненты** через description

## 🔧 Технические детали

### Сбор информации о дизайн-системе
Функция `collectDesignSystemInfo()` автоматически:
- Находит все коллекции переменных в файле
- Извлекает значения всех переменных
- Группирует их по типам (colors, spacing, typography)
- Добавляет стандартные правила Orbita

### Глубокий анализ компонентов
Функция `extractNodeInfo()` рекурсивно:
- Обходит всё дерево элементов
- Собирает информацию о каждом свойстве
- Проверяет привязку к переменным
- Выявляет отклонения от стандартов

### Парсинг AI-ответов
Функция `parseAIAnalysis()`:
- Извлекает Code Readiness Score
- Парсит проблемы по категориям
- Извлекает ID элементов из формата `[ID:xxx]`
- Создаёт структурированный список проблем с навигацией

---

**Версия:** 2.0  
**Дата обновления:** 6 октября 2025  
**Дизайн-система:** Orbita

