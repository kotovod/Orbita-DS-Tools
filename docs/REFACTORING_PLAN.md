# План Рефакторинга Orbita DS Tools

## ✅ Выполнено

### Фаза 0: Очистка документации
- ✅ Перемещено 30+ устаревших MD файлов в `docs/archive/`
- ✅ Создана чистая структура документации в `docs/`
- ✅ Обновлён основной README.md

### Фаза 1: Создание структуры папок
- ✅ Создана папка `src/common/` для общих утилит
- ✅ Создана папка `src/features/` для модулей функций
- ✅ Создана папка `ui/components/` для UI компонентов
- ✅ Создана папка `ui/features/` для UI каждой функции

### Фаза 2: Извлечение общих утилит
- ✅ `src/common/debug.js` - Утилиты отладки (debugLog, debugWarn)
- ✅ `src/common/color-utils.js` - Работа с цветами (rgbToHex, hexToRgb)
- ✅ `src/common/node-utils.js` - Работа с нодами Figma
- ✅ `src/common/serialization.js` - Сериализация данных
- ✅ `src/common/constants.js` - Константы плагина
- ✅ `src/common/storage.js` - Работа с clientStorage
- ✅ `src/common/index.js` - Главный экспорт утилит

---

## 📋 План Дальнейших Фаз

### Фаза 3: Разделение code.js на модули (Следующая)
**Цель:** Разбить монолитный code.js (6596 строк) на модули по функциям

**Подзадачи:**
1. Извлечь логику Check Icons в `src/features/check-icons/`
   - `handler.js` - Обработчики сообщений
   - `validator.js` - Логика валидации иконок
   - `fixer.js` - Автоматическое исправление ошибок

2. Извлечь логику Design System Validator в `src/features/design-system-validator/`
   - `handler.js` - Обработчики сообщений
   - `validator.js` - Логика проверки токенов
   - `token-utils.js` - Работа с variables

3. Извлечь логику AI Design Lint в `src/features/ai-design-lint/`
   - `handler.js` - Обработчики сообщений
   - `analyzer.js` - Анализ компонентов
   - `api.js` - Работа с Yandex Eliza API

4. Извлечь остальные модули (SVG Export, Node Inspector, Component Properties, Page Text)

### Фаза 4: Рефакторинг системы обработки сообщений
**Цель:** Упростить 33 `else if` в систему маршрутизации

**Подходы:**
```javascript
// Вместо 33 else if создать роутер сообщений
const messageRouter = {
  'check-icons': checkIconsModule.handle,
  'dsv-validate': dsvModule.handle,
  'ai-design-lint': aiLintModule.handle,
  // ...
};

figma.ui.onmessage = async (msg) => {
  const handler = messageRouter[msg.type];
  if (handler) {
    await handler(msg);
  }
};
```

### Фаза 5: Разделение ui.html на компоненты
**Цель:** Разбить ui.html (6911 строк, 356 KB) на модули

**Структура:**
```
/ui
  /components
    - button.html
    - progress-bar.html
    - error-card.html
    - settings-panel.html
  /features
    - check-icons.html
    - design-system-validator.html
    - ai-design-lint.html
    - svg-export.html
    - node-inspector.html
    - component-properties.html
    - page-text.html
  /common
    - styles.css
    - utils.js
  index.html (загрузчик)
```

**Технология:** Использовать динамическую загрузку HTML через fetch() или template literals

### Фаза 6: Настройка системы сборки
**Цель:** Собирать модули в единые code.js и ui.html

**Варианты:**

#### Вариант A: Webpack (Рекомендуется)
```bash
npm install --save-dev webpack webpack-cli
```

**webpack.config.js:**
```javascript
module.exports = {
  entry: {
    code: './src/main.js',
    ui: './ui/index.js'
  },
  output: {
    filename: '[name].js'
  }
};
```

**Преимущества:**
- ✅ Tree shaking (удаление неиспользуемого кода)
- ✅ Минификация
- ✅ Source maps для отладки
- ✅ Популярный стандарт

#### Вариант B: Rollup (Легковесный)
```bash
npm install --save-dev rollup @rollup/plugin-node-resolve
```

**Преимущества:**
- ✅ Быстрее Webpack
- ✅ Лучше для библиотек
- ✅ Проще конфигурация

#### Вариант C: esbuild (Самый быстрый)
```bash
npm install --save-dev esbuild
```

**Преимущества:**
- ✅ Очень быстрая сборка (10-100x быстрее Webpack)
- ✅ Встроенная минификация
- ✅ TypeScript из коробки

**Рекомендация:** Начать с **esbuild** для скорости, при необходимости перейти на Webpack

---

## 🎯 Дополнительные Улучшения (После основных фаз)

### TypeScript
Добавить типизацию для безопасности:
```bash
npm install --save-dev typescript @figma/plugin-typings
```

### Тестирование
```bash
npm install --save-dev jest @testing-library/dom
```

### Линтинг
```bash
npm install --save-dev eslint prettier
```

### CI/CD
Настроить автоматическую сборку и тесты в GitHub Actions

---

## 📊 Ожидаемые Результаты

### До рефакторинга:
- `code.js`: 6596 строк, 280 KB
- `ui.html`: 6911 строк, 356 KB
- **Итого:** 13,507 строк, 636 KB

### После рефакторинга:
- 50+ модулей по 50-200 строк каждый
- Улучшенная читаемость
- Упрощённое тестирование
- Быстрая навигация по коду
- Возможность работы команды над разными модулями

### Производительность:
- ✅ Размер собранных файлов останется таким же (или меньше с минификацией)
- ✅ Производительность runtime не изменится
- ✅ Developer Experience улучшится на 300-500%

---

## 🚦 Текущий Статус

**Фаза 0:** ✅ Завершена  
**Фаза 1:** ✅ Завершена  
**Фаза 2:** ✅ Завершена  
**Фаза 3:** 🔄 Следующая  
**Фаза 4:** ⏳ Ожидание  
**Фаза 5:** ⏳ Ожидание  
**Фаза 6:** ⏳ Ожидание

---

## 💡 Примечания

1. **Обратная совместимость:** Все изменения должны быть обратно совместимы
2. **Тестирование:** После каждой фазы тестировать в Figma
3. **Git:** Коммитить после каждой завершённой фазы
4. **Документация:** Обновлять README и DEVELOPMENT.md

---

**Последнее обновление:** 30 ноября 2025

