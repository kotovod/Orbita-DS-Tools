# ⚠️ ВАЖНО: Прочитайте Перед Сборкой

## 📦 Два Манифеста - Важно!

Проект использует **два манифеста** для разных целей:

### `manifest.json` - Production (для Figma Community)
- ✅ Публичная версия плагина
- ✅ Только стабильные функции
- ❌ **AI Design Lint СКРЫТ** из меню
- ✅ Минимальный networkAccess

### `manifest.dev.json` - Development (для разработки)
- ✅ Локальная разработка и тестирование
- ✅ ВСЕ функции (включая экспериментальные)
- ✅ **AI Design Lint ДОСТУПЕН**
- ✅ Полный networkAccess (включая AI API)

**📖 Подробнее см. `docs/DEV_VS_PRODUCTION.md`**

---

## Что Мы Создали

Новую модульную архитектуру с системой маршрутизации сообщений.

**Проблема:** Сейчас у вас ДВА файла code.js:
1. **Старый code.js** (6,596 строк) - рабочий код плагина
2. **Новый src/main.js** - новая архитектура

## ⚠️ Критически Важно!

**НЕ ЗАПУСКАЙТЕ `npm run build` ПОКА НЕ ПРОЧИТАЕТЕ!**

### Что Произойдёт При Сборке

```bash
npm run build
```

Эта команда:
1. ✅ Соберёт все файлы из `src/` в один
2. ❌ **ПЕРЕЗАПИШЕТ code.js**
3. ❌ Старый code.js будет УТЕРЯН

## 🛡️ Безопасная Процедура

### Вариант A: Сохранить Старый Код (Рекомендуется)

```bash
# 1. Сделать бэкап старого code.js
cp code.js code.js.backup

# 2. Или переименовать
mv code.js code.legacy.js

# 3. Теперь можно собирать
npm install
npm run build
```

### Вариант B: Протестировать Без Риска

```bash
# 1. Создать ветку для эксперимента
git checkout -b test-new-architecture

# 2. Собрать
npm install
npm run build

# 3. Тестировать в Figma

# 4. Если не работает - откатить
git checkout main
git branch -D test-new-architecture
```

## 📊 Что Нужно Понимать

### Текущая Ситуация

Новая архитектура **НЕ РАБОТАЕТ** без старого кода, потому что:

```javascript
// src/features/check-icons/handlers.js
async function handleCheckIcons(msg) {
  // ❌ Вызывает функцию из СТАРОГО code.js
  await global.checkIcons(msg.settings);
}
```

### Решение

Нужно **объединить** старый и новый код:

#### Способ 1: Ручное Объединение
1. Скопировать функции из старого `code.js`
2. Вставить в новые модули (`src/features/*/`)
3. Собрать

#### Способ 2: Временная Интеграция
1. Создать `src/legacy.js` с функциями из старого code.js
2. Импортировать в handlers
3. Постепенно мигрировать

## 🎯 Реко мендация

### ДЛЯ БЕЗОПАСНОСТИ:

**НЕ СОБИРАЙТЕ** новую архитектуру прямо сейчас, потому что:
1. ❌ Она не содержит логику (только обёртки)
2. ❌ Нужны функции из старого code.js
3. ❌ Полная миграция займёт ещё 5-10 часов

### ЧТО ДЕЛАТЬ:

**Вариант 1: Остановиться Здесь**
- ✅ Фундамент готов
- ✅ Старый код работает
- ⏳ Продолжить миграцию позже

**Вариант 2: Продолжить Миграцию**
- Извлечь функции из code.js в модули
- Займёт ещё 5-10 часов
- Нужно быть внимательным

**Вариант 3: Использовать Гибрид**
- Создать `src/legacy.js` со всеми старыми функциями
- Импортировать в handlers
- Работает сразу, но код всё ещё монолит

## 📝 Итоги Рефакторинга

### ✅ Что Сделано (Фазы 0-4):

1. **Фаза 0:** Очищена документация
2. **Фаза 1:** Создана структура папок
3. **Фаза 2:** Извлечены утилиты (7 модулей)
4. **Фаза 4:** Создана система маршрутизации

### ⏳ Что Осталось:

1. **Фаза 3:** Извлечь логику функций (5-10 часов)
2. **Фаза 5:** Разделить UI (2-3 часа)
3. **Фаза 6:** Финальная сборка (1 час)

### 💡 Польза Уже Есть:

- ✅ Чистая структура проекта
- ✅ Переиспользуемые утилиты
- ✅ Система маршрутизации готова
- ✅ Фундамент для миграции
- ✅ Документация

## 🚀 Следующие Шаги

### Если Хотите Продолжить:

1. Решите какой вариант выбрать (A, B или C выше)
2. Создайте бэкап code.js
3. Начните миграцию функций
4. Тестируйте после каждого шага

### Если Хотите Остановиться:

1. Оставьте старый code.js как есть
2. Используйте проект как обычно
3. Продолжите миграцию когда будет время

---

## 📞 Готовы Продолжить?

Скажите:
- **"Давай Способ 3 - Гибрид"** - Быстрое решение (30 мин)
- **"Давай продолжим миграцию"** - Полная модуляризация (5-10 часов)
- **"Остановимся здесь"** - Зафиксируем прогресс

Что выбираете? 🎯

