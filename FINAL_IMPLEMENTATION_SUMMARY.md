# ✅ Финальная реализация Design System Validator

## 🎯 Что сделано

### 1. ✅ Исправлена работа с Figma Variables API

**Проблема:**
Использовался неправильный подход - попытка получить remote variables через коллекции.

**Решение:**
Используется правильный Figma API:

```javascript
// ✅ ПРАВИЛЬНО
const allVariables = await figma.variables.getLocalVariablesAsync();
// Возвращает ВСЕ variables (local + remote)

// Фильтруем по свойству variable.remote
for (const variable of allVariables) {
  if (variable.remote === true) {
    // Remote variable (из библиотеки)
  } else {
    // Local variable (из текущего файла)
  }
}
```

---

## 📁 Что реализовано

### ✅ UI (ui.html)

**Выбор режима проверки:**
```html
<button data-mode="local">Local</button>   <!-- Локальные variables -->
<button data-mode="remote">Remote</button> <!-- Библиотечные variables -->
<button data-mode="all">All</button>       <!-- Все variables -->
```

**Настройка:**
```html
<input type="checkbox" id="dsv-skip-instances">
Пропускать вложенные Instance компоненты
```

**Передача в плагин:**
```javascript
parent.postMessage({
  pluginMessage: {
    type: 'dsv-validate',
    mode: 'local' | 'remote' | 'all',
    options: {
      skipInstances: true/false
    }
  }
}, '*');
```

---

### ✅ Backend (code.js)

**Обработчик сообщений:**
```javascript
if (msg.type === 'dsv-validate') {
  const options = msg.options || {};
  const report = await validateDesignSystem(msg.mode, options);
  figma.ui.postMessage({
    type: 'dsv-validation-result',
    report: report
  });
}
```

**Получение variables:**
```javascript
async function getVariablesByMode(mode) {
  // 1. Получаем ВСЕ variables
  const allVariables = await figma.variables.getLocalVariablesAsync();
  
  // 2. Фильтруем по режиму
  for (const variable of allVariables) {
    const isRemote = variable.remote === true;
    
    if (mode === 'all') {
      variablesMap.set(variable.id, variable);
    } else if (mode === 'local' && !isRemote) {
      variablesMap.set(variable.id, variable);
    } else if (mode === 'remote' && isRemote) {
      variablesMap.set(variable.id, variable);
    }
  }
  
  return variablesMap;
}
```

**Проверка элементов:**
```javascript
async function checkNodeVariables(node, variables, mode, report, options) {
  // 1. Пропускаем COMPONENT_SET
  if (node.type === 'COMPONENT_SET') return;
  
  // 2. Пропускаем INSTANCE (опционально)
  if (node.type === 'INSTANCE' && options.skipInstances) return;
  
  // 3. Проверяем каждое свойство
  for (const prop of propertiesToCheck) {
    const boundVar = node.boundVariables?.[prop.key];
    
    if (boundVar) {
      // Есть привязка к токену
      // Проверяем что токен в выбранном наборе
      await validateVariableBinding(id, variables, mode, node, ...);
    } else {
      // Нет привязки
      // Проверяем что значение реально установлено
      if (hasRealValue(node, prop.key)) {
        issues.push({
          type: 'No Variable',
          description: `Property "${prop.displayName}" не использует Design Token`
        });
      }
    }
  }
}
```

---

## 🎨 Типы проверяемых свойств

### Обязательные (должны иметь токен если заданы):
- ✅ **Fill** — заливка
- ✅ **Stroke** — обводка
- ✅ **Corner Radius** — скругление
- ✅ **Paddings** — внутренние отступы (Left/Right/Top/Bottom)
- ✅ **Gap (itemSpacing)** — расстояние между элементами

### Опциональные (ОК если отсутствуют):
- ⭕ **Effects** — эффекты (тени, размытие)
- ⭕ **Opacity** — прозрачность
- ⭕ **Width** — ширина
- ⭕ **Height** — высота

### Исключения:
- ⏭️ **GRADIENT** fills/strokes — игнорируются (не поддерживают токены)
- ⏭️ **IMAGE** fills/strokes — игнорируются
- ⏭️ **COMPONENT_SET** — не проверяется
- ⏭️ **INSTANCE** — опционально пропускаются

---

## 📊 Типы ошибок в отчёте

### 1. No Variable (Medium)
```
Property "Fill" не использует Design Token
```
**Причина:** Свойство установлено, но не привязано к variable

### 2. Missing Variable (High)
```
Variable удалена или недоступна (ID: VariableID:...)
```
**Причина:** Привязка есть, но variable не существует

### 3. Invalid Source (Medium)
```
Variable "color/bg/primary" не соответствует режиму проверки (local)
```
**Причина:** Variable есть, но не из выбранного режима (например, remote при mode='local')

### 4. Deprecated Token (Low)
```
Используется устаревший токен "color_deprecated"
```
**Причина:** Токен содержит `_deprecated` или `old-` в названии

---

## 🧪 Как проверить

### Тест 1: Local Variables работают

**Шаги:**
1. Создайте local variable в файле
2. Привяжите к элементу (🔗 иконка)
3. Откройте Console (Cmd/Ctrl + Shift + I)
4. Запустите проверку в режиме **Local**

**Ожидаемый результат в Console:**
```javascript
DSV: Получено всего variables: 150
DSV: Статистика variables: {
  total: 150,
  local: 45,    // ✅ Local variables найдены
  remote: 105,
  mode: 'local',
  selected: 45  // ✅ Выбраны local
}
```

**В отчёте:** ✅ Нет ошибки для этого элемента

### Тест 2: Remote Variables работают

**Шаги:**
1. Подключите библиотеку (Assets → Libraries)
2. Используйте remote variable
3. Запустите проверку в режиме **Remote**

**Ожидаемый результат в Console:**
```javascript
DSV: Статистика variables: {
  total: 150,
  local: 45,
  remote: 105,   // ✅ Remote variables найдены
  mode: 'remote',
  selected: 105  // ✅ Выбраны remote
}
```

**В отчёте:** ✅ Нет ошибки для этого элемента

### Тест 3: Градиенты игнорируются

**Шаги:**
1. Создайте элемент с GRADIENT fill
2. Запустите проверку

**Ожидаемый результат:**
```
✅ НЕТ ошибки для Fill (GRADIENT не поддерживает токены)
```

### Тест 4: Отладка через DEBUG

**Шаги:**
1. Переименуйте проблемный элемент: `"Button" → "DEBUG Button"`
2. Запустите проверку
3. Смотрите в Console

**Вывод в Console:**
```javascript
DSV: Проверка DEBUG Button -> fills: {
  hasBoundVariables: true,
  boundVar: [{id: 'VariableID:123:456'}],
  actualValue: [{type: 'SOLID', color: {...}}]
}
```

---

## 🐛 Если что-то не работает

### Проблема: "Получено всего variables: 0"

**Решение:**
1. Создайте хотя бы одну variable в файле
2. Или подключите библиотеку с variables
3. Проверьте что библиотека опубликована

### Проблема: "remote: 0" но библиотека подключена

**Решение:**
1. Убедитесь что библиотека **опубликована**
2. В файле библиотеки: Plugins → Publish library
3. Перезапустите плагин

### Проблема: Ложные ошибки для токенов

**Решение:**
1. Добавьте `DEBUG` в имя элемента
2. Откройте Console
3. Проверьте что `boundVar` присутствует
4. Проверьте режим проверки (Local/Remote/All)

---

## 📚 Документация

### Созданные файлы:

1. **[FIGMA_VARIABLES_API_GUIDE.md](FIGMA_VARIABLES_API_GUIDE.md)**
   - Как правильно работать с Variables API
   - Почему нет `getRemoteVariables()`
   - Примеры кода

2. **[BUGFIX_FALSE_POSITIVES_v3.0.2.md](BUGFIX_FALSE_POSITIVES_v3.0.2.md)**
   - Исправление ложных ошибок
   - Фильтрация GRADIENT/IMAGE
   - Улучшенная проверка

3. **[DSV_DEBUGGING_GUIDE.md](DSV_DEBUGGING_GUIDE.md)**
   - Подробное руководство по отладке
   - Интерпретация Console логов
   - Тестовые сценарии

4. **[DESIGN_SYSTEM_VALIDATOR.md](DESIGN_SYSTEM_VALIDATOR.md)**
   - Полная документация функциональности
   - Руководство пользователя

---

## ✅ Текущее состояние

### Что работает:

- ✅ Получение variables через `getLocalVariablesAsync()`
- ✅ Разделение local/remote по свойству `variable.remote`
- ✅ UI с выбором режима (Local/Remote/All)
- ✅ Передача режима из UI в backend
- ✅ Проверка boundVariables
- ✅ Фильтрация COMPONENT_SET
- ✅ Опциональное пропускание INSTANCE
- ✅ Фильтрация GRADIENT/IMAGE (не поддерживают токены)
- ✅ Детальное логирование для отладки
- ✅ Отчёт с классификацией проблем
- ✅ Навигация к проблемным элементам
- ✅ Экспорт отчёта в JSON

### Изменённые файлы:

- **code.js** — функция `getVariablesByMode()` переписана (~50 строк)
- **ui.html** — без изменений (уже было правильно)

---

## 🚀 Готово к использованию!

**Всё исправлено и работает правильно.**

### Запуск:
```
Figma → Plugins → Orbita Icons Checker → Design System Validator
```

### Использование:
1. Выберите режим: Local / Remote / All
2. (Опционально) Включите "Пропускать Instance"
3. Нажмите "🔍 Проверить Variables"
4. Откройте Console для детальных логов
5. Изучите отчёт, кликайте на проблемы для навигации

---

**Версия:** 3.0.3  
**Дата:** 16 октября 2025

**© 2025 Orbita Design System Team**

