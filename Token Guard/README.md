# Token Guard 🛡️

Figma плагин для управления токенами дизайн-системы.

## Возможности

- **Импорт дизайн-системы**: Загрузка переменных дизайн-системы из текущего файла Figma
  - Импорт всех локальных переменных (числовых и цветовых)
  - Автоматическое разрешение алиасов
  - Поддержка вложенных структур переменных
- **Сохранение в память**: Данные сохраняются в clientStorage плагина
  - Сохраняются `value`, `key`, `id` и `scopes` переменных
  - Автоматическая загрузка при следующем запуске
  - Отображение даты и времени импорта
- **Проверка числовых значений**: Автоматическая проверка gap, padding, corner radius, stroke weight
  - Обнаружение значений без привязки к переменным
  - Автоматическое исправление с применением ближайшей переменной
  - Группировка ошибок по элементам
- **Проверка цветов**: Проверка fills и strokes на наличие переменных
  - Определение примененных режимов (modes)
  - Автоматическое исправление с учетом режимов
  - Поддержка приоритизации коллекций
- **Работа со скрытыми переменными**: Поддержка переменных, скрытых от публикации
  - Автоматическое определение локальных переменных
  - Применение как опубликованных, так и скрытых переменных

## Запуск в Figma (DEV mode)

1. Откройте Figma Desktop приложение
2. Перейдите в меню: **Plugins → Development → Import plugin from manifest...**
3. Выберите файл `manifest.json` из этой папки
4. Плагин появится в меню: **Plugins → Development → Token Guard**

## Использование

### 1. Импорт дизайн-системы

1. Откройте файл с вашей дизайн-системой (файл, где определены переменные)
2. Запустите плагин через меню Plugins
3. Перейдите в раздел "Импорт дизайн-системы"
4. Нажмите кнопку "📥 Импортировать дизайн-систему"
5. Все локальные переменные будут импортированы и сохранены в памяти плагина

**Примечание:** Плагин автоматически импортирует все числовые и цветовые переменные из текущего файла, включая:
- Переменные, скрытые от публикации
- Вложенные структуры переменных
- Разрешенные алиасы

### 2. Проверка числовых значений

1. Выберите фрейм или элемент для проверки
2. Перейдите в раздел "Числовые значения"
3. Нажмите кнопку "🔍 Проверить выбранный фрейм"
4. Плагин покажет все найденные ошибки (значения без переменных)
5. Нажмите "Исправить" для автоматического применения переменных

Плагин проверяет:
- Gap (itemSpacing) в автолейауте
- Padding (все стороны)
- Corner radius (общий и индивидуальные углы)
- Stroke weight

### 3. Проверка цветов

1. Выберите фрейм или элемент для проверки
2. Перейдите в раздел "Цвета"
3. Нажмите кнопку "🎨 Проверить цвета"
4. Плагин покажет:
   - Примененные режимы (modes)
   - Цвета без переменных (fills и strokes)
5. Нажмите "Исправить" для автоматического применения цветовых переменных

**Примечание о режимах:**
- Плагин определяет режимы, установленные вручную (explicit modes)
- Автоматически выбирает подходящую переменную с учетом текущих режимов
- Использует систему приоритизации коллекций (2.1, 2.2 > 2. Product > 1. Theme)

### 4. Работа со скрытыми переменными

Начиная с версии от 16.11.2025, плагин поддерживает применение переменных, скрытых от публикации:

1. При импорте дизайн-системы плагин сохраняет ID всех локальных переменных
2. При применении переменной плагин сначала ищет её локально (в текущем файле)
3. Если переменная не найдена локально, плагин пытается импортировать её из библиотеки
4. Скрытые переменные работают только если вы находитесь в файле, где они определены

**Ограничение:** Переменные, скрытые от публикации, нельзя импортировать в другие файлы. Они работают только в исходном файле библиотеки.

## Структура проекта

- `manifest.json` - Конфигурация плагина
- `code.js` - Основной код плагина
- `ui.html` - Пользовательский интерфейс

## Технологии

- JavaScript
- Figma Plugin API
- Client Storage API (для сохранения данных)

## Известные ограничения

1. **Скрытые переменные:** Переменные, скрытые от публикации (`hiddenFromPublishing: true`), можно применять только в файле, где они определены. В других файлах, использующих библиотеку, эти переменные недоступны.

2. **Режимы (Modes):** При проверке цветов плагин показывает только режимы, установленные вручную (explicit modes). Наследуемые режимы не отображаются для упрощения интерфейса.

3. **Приоритизация коллекций:** Система приоритизации коллекций (2.1 > 2. > 1.) является хардкодом. В будущем планируется сделать её настраиваемой.

## Будущие улучшения

- [ ] Настраиваемая приоритизация коллекций
- [ ] Поддержка batch-обработки множества фреймов
- [ ] Экспорт отчетов о найденных ошибках
- [ ] Интеграция с CI/CD для автоматической проверки
- [ ] Поддержка других типов переменных (boolean, string)
