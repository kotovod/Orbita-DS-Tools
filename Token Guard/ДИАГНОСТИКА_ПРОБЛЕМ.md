# 🔍 Диагностика проблем с применением переменных

## Проблема: "Не найдена переменная с цветом"

### Что означает эта ошибка?

Когда вы видите в консоли:
```
❌ Не найдена переменная с цветом #ff6666 в применённых режимах
```

Это означает, что плагин:
1. ✅ Нашел цвет без переменной
2. ✅ Определил применённые режимы (modes)
3. ✅ Нашел совместимые цветовые переменные в вашей дизайн-системе
4. ❌ Но НЕ нашел переменную с этим конкретным цветом в этих конкретных режимах

**Это нормально!** Плагин не может применить переменную, которой нет в системе.

## 🔍 Как диагностировать?

### Шаг 1: Проверьте расширенное логирование

После обновления плагин показывает дополнительную диагностику:

```
🔍 Ищем среди 496 совместимых переменных...
📌 Применённые режимы для поиска: Light, Dark
📋 Первые 5 доступных переменных (для диагностики):
   - Colors/surface-brand: Light: #3B82F6, Dark: #1E40AF
   - Colors/text-primary: Light: #000000, Dark: #FFFFFF
   ...
💡 Найдена переменная "Colors/error" с этим цветом в режиме "Warning" (но не в применённых режимах!)
```

Это поможет понять:
- Какие режимы применены к объекту
- Какие переменные вообще есть в системе
- Есть ли нужный цвет в других режимах

### Шаг 2: Проверьте режимы объекта

Убедитесь, что к объекту применены правильные режимы:

1. Выберите объект в Figma
2. Откройте правую панель (Design)
3. Найдите секцию "Applied modes" или "Variable modes"
4. Проверьте, какие режимы установлены

**Важно:** Плагин ищет переменную только в тех режимах, которые применены к объекту!

### Шаг 3: Проверьте наличие переменной

Откройте панель Variables в Figma и проверьте:

1. Есть ли переменная с нужным цветом?
2. В каких режимах у неё это значение?
3. Совпадают ли эти режимы с применёнными к объекту?

## 💡 Частые причины и решения

### Причина 1: Переменной нет в системе

**Проблема:** Цвет `#ff6666` просто отсутствует в дизайн-системе.

**Решение:**
- Создайте новую переменную с этим цветом
- Или измените цвет объекта на существующий из системы

### Причина 2: Переменная есть, но в другом режиме

**Проблема:** У объекта режим "Light", а переменная с цветом `#ff6666` есть только в режиме "Dark".

**Что показывает плагин:**
```
💡 Найдена переменная "Colors/error" с этим цветом в режиме "Dark" (но не в применённых режимах!)
```

**Решение:**
- Вариант 1: Добавьте это значение в режим "Light"
- Вариант 2: Переключите объект на режим "Dark"
- Вариант 3: Используйте другой цвет из режима "Light"

### Причина 3: Неправильные scopes

**Проблема:** Переменная есть, но у неё неправильные scopes (области применения).

**Что показывает плагин:**
```
✓ Требуемые scopes: [FRAME_FILL, SHAPE_FILL, TEXT_FILL]
❌ Проверка scopes: переменная [STROKE_COLOR] vs требуемые [...]
```

**Решение:**
- Откройте переменную в Figma
- Измените её scopes, добавив нужные (например, Shape fill, Frame fill)

### Причина 4: Переменная скрыта от публикации и вы в другом файле

**Проблема:** Переменная скрыта (`hiddenFromPublishing: true`) и вы работаете не в файле библиотеки.

**Решение:**
- Вариант 1: Откройте файл библиотеки и работайте там
- Вариант 2: Сделайте переменную видимой для публикации
- Вариант 3: Используйте другую переменную

## 🛠️ Пошаговая диагностика

1. **Запустите проверку цветов**
   - Выберите объект
   - Нажмите "🎨 Проверить цвета"

2. **Откройте консоль браузера**
   - В Figma Desktop: Developer → Developer Console
   - Или: View → Developer Console (Cmd+Option+I / Ctrl+Shift+I)

3. **Нажмите "Исправить" на ошибке**

4. **Читайте логи:**
   ```
   🎨 Ищем цветовую переменную для fills: #ff6666
   📋 Применённые режимы: ...
   ✓ Найдено X цветовых переменных
   ✓ После фильтрации по scopes: Y совместимых переменных
   🔍 Ищем среди Y совместимых переменных...
   📌 Применённые режимы для поиска: Light, Dark
   📋 Первые 5 доступных переменных (для диагностики):
   ...
   ```

5. **Анализируйте результат:**
   - Если показаны примеры переменных → система работает, просто нет подходящей
   - Если показано "Найдена переменная в другом режиме" → проблема в режимах
   - Если ничего не найдено → проверьте импорт дизайн-системы

## ✅ Чеклист проверки

- [ ] Дизайн-система импортирована в плагин
- [ ] К объекту применены режимы (modes)
- [ ] В дизайн-системе есть переменная с нужным цветом
- [ ] Цвет есть в применённых режимах (не в других)
- [ ] У переменной правильные scopes
- [ ] Если переменная скрыта - вы в файле библиотеки

## 📊 Пример хорошего лога

Когда всё работает правильно:

```
🎨 Ищем цветовую переменную для fills: #3b82f6
📋 Применённые режимы: Light, Dark
✓ Требуемые scopes: [ALL_SCOPES, FRAME_FILL, SHAPE_FILL, TEXT_FILL]
✓ Найдено 499 цветовых переменных
✓ После фильтрации по scopes: 496 совместимых переменных
🔍 Ищем среди 496 совместимых переменных...
📌 Применённые режимы для поиска: Light, Dark
✅ Найдено совпадение: Colors/surface-brand в режиме Light (коллекция: 2.4, приоритет: 1)
✅ Выбрана переменная: Colors/surface-brand из коллекции 2.4 (приоритет 1)
✓ Найдена локальная переменная: Colors/surface-brand
✅ Переменная применена к fills[0]
✅ Исправлено: Frame 34 → Fill
```

## 🆘 Нужна помощь?

Если проблема не решается:

1. Скопируйте полный лог из консоли
2. Сделайте скриншот панели Variables с вашими переменными
3. Укажите, какой цвет пытаетесь применить
4. Укажите, какие режимы применены к объекту
5. Создайте issue в репозитории проекта


