# Работа со скрытыми переменными

## Что изменилось?

Теперь плагин **Token Guard** поддерживает работу с переменными, которые скрыты от публикации в Figma!

## Как это работает?

### Проблема раньше

Раньше, когда вы пытались применить переменную, скрытую от публикации (`hiddenFromPublishing: true`), плагин выдавал ошибку. Это происходило потому, что Figma не позволяет импортировать такие переменные из библиотек в другие файлы.

### Решение сейчас

Теперь плагин:

1. **Сохраняет ID переменных** при импорте дизайн-системы
2. **Сначала ищет переменную локально** (в текущем файле) по ID
3. **Только потом пытается импортировать** из библиотеки по ключу

## Как использовать?

### Шаг 1: Импорт дизайн-системы

1. Откройте файл с вашей дизайн-системой (где определены переменные)
2. Запустите плагин Token Guard
3. Перейдите в "Импорт дизайн-системы"
4. Нажмите "📥 Импортировать дизайн-систему"

✅ Плагин сохранит все переменные, включая скрытые от публикации

### Шаг 2: Применение переменных

1. Выберите элемент для проверки
2. Используйте проверку "Числовые значения" или "Цвета"
3. Нажмите "Исправить" на нужной ошибке

✅ Плагин применит переменную, даже если она скрыта от публикации!

## Важные моменты

### ✅ Работает

- Применение скрытых переменных **в файле библиотеки** (где они определены)
- Применение обычных переменных в любом файле
- Автоматическое определение локальных переменных

### ❌ Не работает

- Применение скрытых переменных **в других файлах** (использующих библиотеку)
- Это ограничение Figma API, не плагина

## Почему это полезно?

### Сценарий использования:

У вас есть переменные, которые:
- Используются только внутри дизайн-системы
- Не должны быть доступны дизайнерам в других файлах
- Например: промежуточные значения, служебные токены, экспериментальные переменные

Теперь вы можете:
- Скрыть их от публикации (чтобы не засорять список переменных в других файлах)
- Но при этом продолжать применять их в файле библиотеки через плагин

## Технические детали

### Что сохраняется при импорте?

Для каждой переменной:
```javascript
{
  "value": 8,           // Значение
  "key": "abc123...",   // Ключ для импорта из библиотеки
  "id": "VariableID:...", // ID для локального поиска (НОВОЕ!)
  "scopes": ["GAP"],    // Области применения
  "hiddenFromPublishing": false // Скрыта ли от публикации
}
```

### Логика применения переменной:

1. Ищем переменную по `id` среди локальных (`figma.variables.getVariableById()`)
   - ✅ Найдена → применяем
   - ❌ Не найдена → переходим к шагу 2

2. Пытаемся импортировать по `key` из библиотеки (`figma.variables.importVariableByKeyAsync()`)
   - ✅ Импортирована → применяем
   - ❌ Не импортирована → показываем ошибку

## Пример

### До исправления:

```
❌ Не удалось применить переменную "spacing/internal/4m"
   Ошибка: Переменная скрыта от публикации
```

### После исправления:

```
✅ Найдена локальная переменная "spacing/internal/4m"
✅ Переменная применена к padding
```

## Часто задаваемые вопросы

**Q: Нужно ли мне переимпортировать дизайн-систему?**
A: Да, для использования новой функции нужно переимпортировать переменные, чтобы плагин сохранил их ID.

**Q: Будут ли работать старые данные?**
A: Да, плагин обратно совместим. Если нет ID, он попытается импортировать по ключу (старое поведение).

**Q: Можно ли использовать скрытые переменные в других файлах?**
A: Нет, это ограничение Figma. Скрытые переменные работают только в файле, где они определены.

**Q: Как понять, что переменная скрыта от публикации?**
A: В Figma, в панели Variables, у такой переменной будет значок замочка или она будет помечена как "Hidden from publishing".

## Обратная связь

Если у вас возникли проблемы или вопросы, создайте issue в репозитории проекта.


