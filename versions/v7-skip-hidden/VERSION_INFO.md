# Версия 7 - Skip Hidden Objects

**Дата:** 2025-12-25
**Статус:** ✅ Стабильно

## 🎯 Что нового

### Пропуск скрытых объектов
- ✅ Добавлена настройка "Пропускать скрытые объекты" (включена по умолчанию)
- ✅ Рекурсивная проверка иерархии - если родитель скрыт, дети тоже пропускаются
- ✅ Работает с Instance внутри скрытых Frame
- ✅ Проверка всего пути от объекта до PAGE

### Технические улучшения
- Добавлена функция `isNodeOrParentHidden()` для проверки видимости
- Проверка добавлена в обе системы валидации:
  - `checkNumericVariables()` (текущая активная)
  - `validateDesignSystem()` (для будущего)
- Детальное диагностическое логирование

### UI изменения
- Новый чекбокс в блоке "Настройки"
- Tooltip с объяснением функции
- Передача настройки `skipHiddenObjects` в плагин

## 🔧 Как работает

Функция проверяет не только сам объект, но и всех его родителей:

```
Frame (visible = false) ← скрыт через иконку глаза
  └─ Instance (visible = true) ← ПРОПУСКАЕТСЯ ✅
       └─ Rectangle (visible = true) ← ПРОПУСКАЕТСЯ ✅
```

Если хотя бы один родитель скрыт - весь объект считается скрытым.

## 📚 Документация

Созданы документы:
- `docs/DSV_SKIP_HIDDEN_OBJECTS.md` - полное описание функции
- `docs/FIX_HIDDEN_OBJECTS_HIERARCHY.md` - описание рекурсивной проверки
- `docs/DEBUG_HIDDEN_OBJECTS.md` - инструкция по диагностике

## 🔄 Откат на эту версию

```bash
cd "/Users/kotovod/Documents/GitHub/Orbita DS Tools"
cp versions/v7-skip-hidden/* ./
```

Затем перезагрузите плагин в Figma.

## ⚙️ Настройка в UI

**Design System Validator → Настройки:**
- ☐ Пропускать Instance
- ☐ Только orb-* компоненты
- ☑ Пропускать значения = 0
- ☑ **Пропускать скрытые объекты** ← НОВОЕ!

## 🐛 Исправленные проблемы

- Скрытые Instance внутри скрытых Frame теперь корректно пропускаются
- Все дочерние элементы скрытых родителей пропускаются
- Настройка работает во всех режимах проверки (local, remote, all)

