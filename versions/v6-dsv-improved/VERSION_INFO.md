# Версия 6 - DSV Improvements

**Дата:** 2025-12-03

## Что нового

### Настройки проверяемых свойств
- Добавлены чекбоксы для выбора проверяемых свойств:
  - Fill (Заливка)
  - Stroke (Обводка)
  - Corner Radius
  - Gap / Item Spacing
  - Padding
  - Effects (по умолчанию выключено)
  - Opacity (по умолчанию выключено)
  - Width/Height (по умолчанию выключено)

### Управление токенами
- Кнопка "Импортировать из библиотеки" для загрузки токенов
- Кнопка "Сбросить токены" для очистки
- Кнопки взаимоисключающие (показывается только одна)
- Отображение даты и времени загрузки токенов
- Подсказка (tooltip) с инструкцией
- Скрытие "Просмотр JSON токенов" когда токенов нет

### Сообщение об успехе
- Зелёное сообщение "Все свойства привязаны к токенам! 🎉" когда ошибок нет

### Исправления багов
- **Ошибки больше не пропадают после исправления одной** - убран автоматический перезапуск проверки
- Исправленные ошибки удаляются только из UI, остальные остаются

### UI улучшения
- Header фиксирован, контент скроллится отдельно
- Панель настроек имеет свой скролл (344px высота)
- Убран горизонтальный скролл
- Убран скролл на уровне всего UI плагина

## Файлы
- `code.js` - логика плагина
- `ui.html` - интерфейс
- `manifest.json` - манифест плагина

