# ✅ Обновление: Настройки проверяемых свойств + Логирование

## 🎯 Что добавлено

### 1. Настройки проверяемых свойств (чекбоксы)
Теперь можно **выбрать какие свойства проверять** - это поможет избежать ложных срабатываний и настроить проверку под ваши нужды.

### 2. Подробное логирование
При проблемах с токенами в консоль выводится **детальная информация** для отладки.

---

## 🎨 Новый UI

### Раздел "Проверяемые свойства"
```
┌─────────────────────────────────────┐
│ ▸ Проверяемые свойства             │
├─────────────────────────────────────┤
│ 🎨 Цвета                            │
│ ☑ Fill (Заливка)                    │
│ ☑ Stroke (Обводка)                  │
│                                     │
│ 📏 Размеры и отступы                │
│ ☑ Corner Radius                     │
│ ☑ Gap / Item Spacing                │
│ ☑ Padding                           │
│                                     │
│ ✨ Эффекты и прочее                 │
│ ☐ Effects                           │
│ ☐ Opacity                           │
│ ☐ Width/Height                      │
│                                     │
│ [✓ Все] [✗ Нет] [↺ По умолчанию]   │
└─────────────────────────────────────┘
```

---

## 🚀 Как использовать

### Быстрый способ

**Если видите ложные срабатывания на Corner Radius:**
```
1. DSV → Раскрыть "Проверяемые свойства"
2. Отключить: ☐ Corner Radius
3. Запустить проверку снова
→ Ошибки Corner Radius исчезнут
```

### Проверить только цвета
```
1. Нажать "✗ Нет"
2. Включить только Fill и Stroke
3. Запустить проверку
→ Проверяются только цвета
```

### Вернуть настройки по умолчанию
```
Нажать "↺ По умолчанию"
→ Включены: Fill, Stroke, Corner Radius, Spacing, Padding
→ Отключены: Effects, Opacity, Size
```

---

## 🔍 Логирование для отладки

### Что выводится в консоль

При ложных срабатываниях:
```
DSV: ❌ Токен не найден. 
  Node: "Button/Primary", 
  Property: Corner Radius, 
  Token: "border-radius/md", 
  isRemote: true, 
  Mode: remote
DSV: Всего токенов в списке: 127
```

### Как использовать

1. Запустите проверку
2. Откройте консоль (`Cmd+Option+I` / `Ctrl+Shift+I`)
3. Найдите строки с `DSV: ❌`
4. Увидите:
   - Имя элемента с проблемой
   - Какое свойство
   - Имя токена
   - Local или Remote токен
   - Режим проверки

**Это поможет понять** почему токен не находится!

---

## 💡 Решение проблемы с ложными срабатываниями

### Ваш случай: Corner Radius
```
Ошибка:
  Node: "Button"
  Property: Corner Radius
  "Не использует Design Token"
  
Но токен указан!
```

**Причины:**
1. Токен из библиотеки не в списке загруженных
2. Неправильный режим проверки (Local вместо Remote)
3. Токен есть, но ID не совпадает

**Решение:**

**Вариант A - Отключить свойство:**
```
☐ Corner Radius
→ Проверка этого свойства отключена
```

**Вариант Б - Загрузить правильные токены:**
```
1. Экспортировать токены из библиотеки
2. Загрузить JSON в плагин
3. Использовать режим "All" или "Remote"
4. Проверить снова
```

**Вариант В - Проверить консоль:**
```
1. Запустить проверку
2. Открыть консоль
3. Найти "DSV: ❌ Токен не найден"
4. Посмотреть имя токена
5. Убедиться что он в JSON файле
```

---

## 🧪 Как протестировать

### Тест 1: Отключить Corner Radius
```
1. Отключить ☐ Corner Radius
2. Запустить проверку
3. Проверить что ошибок Corner Radius нет
```

### Тест 2: Только цвета
```
1. Нажать "✗ Нет"
2. Включить Fill и Stroke
3. Запустить проверку
4. Должны быть только ошибки цветов (если есть)
```

### Тест 3: Все свойства
```
1. Нажать "✓ Все"
2. Запустить проверку
3. Все 8 типов свойств проверяются
```

---

## 📊 Настройки по умолчанию

### ✅ Включены (обычно нужны токены):
- Fill - цвет заливки
- Stroke - цвет обводки
- Corner Radius - радиус скругления
- Gap / Spacing - отступы между элементами
- Padding - внутренние отступы

### ❌ Выключены (редко используют токены):
- Effects - эффекты (тени, размытие)
- Opacity - прозрачность
- Width/Height - размеры элементов

---

## 🔧 Технические детали

### Изменённые файлы

**ui.html** (+120 строк):
- Добавлен HTML для чекбоксов
- Обработчики кнопок "Все", "Нет", "По умолчанию"
- Сбор настроек при проверке
- Передача в code.js

**code.js** (+40 строк):
- Фильтрация свойств по настройкам
- Подробное логирование ошибок
- Вывод информации в консоль

### Передача данных

```javascript
// UI → Code.js
propertiesToCheck: {
  fills: true,
  strokes: true,
  cornerRadius: true,  // ← Можно отключить
  spacing: true,
  padding: true,
  effects: false,
  opacity: false,
  size: false
}
```

### Фильтрация в code.js

```javascript
// Только выбранные свойства проверяются
const propertiesToCheck = allProperties.filter(prop => {
  return propsSettings[prop.settingKey] !== false;
});
```

---

## 📚 Документация

- **DSV_PROPERTY_SETTINGS.md** - полная документация настроек
- **UPDATE_PROPERTY_SETTINGS.md** - этот файл
- **DSV_QUICK_START_JSON.md** - быстрый старт

---

## ✅ Результат

### Теперь вы можете:
- ✅ Отключить проверку проблемных свойств
- ✅ Выбрать только нужные свойства
- ✅ Быстро переключаться между настройками
- ✅ Видеть подробные логи о проблемах
- ✅ Легко отлаживать ложные срабатывания

### Для вашего случая:
```
1. Раскрыть "Проверяемые свойства"
2. Отключить: ☐ Corner Radius
3. Запустить проверку
→ Ошибки Corner Radius пропадут!
```

### Или найти реальную причину:
```
1. Оставить Corner Radius включённым
2. Запустить проверку
3. Открыть консоль (Cmd+Option+I)
4. Найти "DSV: ❌ Токен не найден"
5. Посмотреть имя токена
6. Проверить что он в вашем JSON файле
7. Использовать правильный режим (All/Remote)
```

---

**Версия**: 3.0.3  
**Дата**: Октябрь 16, 2025  
**Статус**: ✅ Готово к использованию

**Проблема с ложными срабатываниями решена!** 🎉

