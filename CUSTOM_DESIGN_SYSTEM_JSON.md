# Кастомная дизайн-система для AI Design Lint

## Как использовать JSON с переменными дизайн-системы

### 1. Создайте JSON файл

Создайте файл `design-system.json` со структурой вашей дизайн-системы:

```json
{
  "name": "Orbita Design System",
  "version": "2.0",
  "tokens": {
    "colors": {
      "primary": {
        "100": "#FFE5D9",
        "200": "#FFCCB3",
        "300": "#FFB38C",
        "400": "#FF9966",
        "500": "#F86025",
        "600": "#E54D0F",
        "700": "#C23E0C",
        "800": "#9F3009",
        "900": "#7C2207"
      },
      "neutral": {
        "0": "#FFFFFF",
        "50": "#F5F5F5",
        "100": "#E5E5E5",
        "200": "#D4D4D4",
        "300": "#A3A3A3",
        "400": "#737373",
        "500": "#525252",
        "600": "#404040",
        "700": "#262626",
        "800": "#171717",
        "900": "#0A0A0A"
      },
      "semantic": {
        "success": "#4CAF50",
        "warning": "#FF9800",
        "error": "#F02D2D",
        "info": "#2196F3"
      }
    },
    "spacing": {
      "0": "0px",
      "1": "4px",
      "2": "8px",
      "3": "12px",
      "4": "16px",
      "5": "20px",
      "6": "24px",
      "8": "32px",
      "10": "40px",
      "12": "48px",
      "14": "56px",
      "16": "64px"
    },
    "typography": {
      "fontSize": {
        "xs": "12px",
        "sm": "14px",
        "base": "16px",
        "lg": "18px",
        "xl": "20px",
        "2xl": "24px",
        "3xl": "30px",
        "4xl": "36px"
      },
      "fontWeight": {
        "regular": 400,
        "medium": 500,
        "semibold": 600,
        "bold": 700
      },
      "lineHeight": {
        "tight": 1.25,
        "normal": 1.5,
        "relaxed": 1.75
      }
    },
    "borderRadius": {
      "none": "0px",
      "sm": "2px",
      "base": "4px",
      "md": "6px",
      "lg": "8px",
      "xl": "12px",
      "2xl": "16px",
      "3xl": "24px",
      "full": "9999px"
    },
    "shadows": {
      "sm": "0 1px 2px 0 rgba(0, 0, 0, 0.05)",
      "base": "0 1px 3px 0 rgba(0, 0, 0, 0.1)",
      "md": "0 4px 6px -1px rgba(0, 0, 0, 0.1)",
      "lg": "0 10px 15px -3px rgba(0, 0, 0, 0.1)",
      "xl": "0 20px 25px -5px rgba(0, 0, 0, 0.1)"
    }
  },
  "rules": {
    "spacing": {
      "description": "Все отступы должны быть кратны 4px",
      "standard": [0, 4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64],
      "exceptions": ["Допускаются 2px для мелких элементов"]
    },
    "borderRadius": {
      "description": "Стандартные значения border-radius",
      "standard": [0, 2, 4, 6, 8, 12, 16, 20, 24, 32],
      "usage": {
        "buttons": [4, 6, 8],
        "cards": [8, 12, 16],
        "modals": [12, 16, 24]
      }
    },
    "touchTargets": {
      "minimum": 44,
      "recommended": 48,
      "description": "Минимальный размер для интерактивных элементов"
    },
    "contrast": {
      "normalText": 4.5,
      "largeText": 3.0,
      "description": "Минимальный контраст по WCAG AA",
      "enhanced": {
        "normalText": 7.0,
        "largeText": 4.5,
        "description": "WCAG AAA стандарт"
      }
    },
    "typography": {
      "minFontSize": 12,
      "bodyText": [14, 16],
      "headings": {
        "h1": [30, 36],
        "h2": [24, 30],
        "h3": [20, 24],
        "h4": [18, 20],
        "h5": [16, 18],
        "h6": [14, 16]
      }
    }
  },
  "components": {
    "button": {
      "sizes": {
        "sm": { "height": 32, "padding": "8px 12px", "fontSize": 14 },
        "md": { "height": 40, "padding": "12px 16px", "fontSize": 16 },
        "lg": { "height": 48, "padding": "16px 24px", "fontSize": 18 }
      },
      "variants": ["primary", "secondary", "outline", "ghost"],
      "states": ["default", "hover", "active", "disabled", "loading"]
    },
    "input": {
      "sizes": {
        "sm": { "height": 32, "padding": "8px 12px", "fontSize": 14 },
        "md": { "height": 40, "padding": "12px 16px", "fontSize": 16 },
        "lg": { "height": 48, "padding": "16px 20px", "fontSize": 18 }
      },
      "states": ["default", "hover", "focus", "disabled", "error", "success"]
    },
    "card": {
      "padding": [16, 20, 24],
      "borderRadius": [8, 12, 16],
      "shadow": ["sm", "base", "md"]
    }
  },
  "naming": {
    "components": {
      "pattern": "ComponentName",
      "examples": ["Button", "Input", "Card", "Modal"]
    },
    "variants": {
      "pattern": "variant-name",
      "examples": ["primary", "secondary", "outline", "ghost"]
    },
    "states": {
      "pattern": "state-name",
      "examples": ["hover", "active", "disabled", "focus"]
    }
  }
}
```

### 2. Загрузите JSON в плагин

1. Откройте **AI Design Lint**
2. Нажмите на **⚙️ Настройки**
3. В разделе **"JSON дизайн-системы"** нажмите **📁 Загрузить JSON**
4. Выберите ваш `design-system.json` файл
5. Нажмите **Сохранить настройки**

### 3. Как AI использует JSON

AI будет использовать ваш JSON для:

#### ✅ Проверки токенов
```
❌ Плохо: padding: 15px (нестандартное значение)
✅ Хорошо: padding: 16px (токен: spacing-4)
```

#### ✅ Проверки цветов
```
❌ Плохо: color: #FF6633 (не из палитры)
✅ Хорошо: color: #F86025 (токен: primary-500)
```

#### ✅ Проверки компонентов
```
❌ Плохо: Button height: 38px (нестандартный)
✅ Хорошо: Button height: 40px (размер: md)
```

#### ✅ Проверки состояний
```
❌ Плохо: Отсутствует состояние hover
✅ Хорошо: Все состояния присутствуют (default, hover, active, disabled)
```

### 4. Структура JSON

#### Обязательные поля:
- `tokens` - токены дизайн-системы
  - `colors` - цветовая палитра
  - `spacing` - отступы
  - `typography` - типографика
  - `borderRadius` - скругления
  - `shadows` - тени

#### Опциональные поля:
- `rules` - правила дизайн-системы
- `components` - спецификации компонентов
- `naming` - правила именования

### 5. Примеры использования

#### Минимальный JSON (только токены):
```json
{
  "tokens": {
    "colors": {
      "primary": "#F86025",
      "secondary": "#2196F3"
    },
    "spacing": {
      "sm": "8px",
      "md": "16px",
      "lg": "24px"
    }
  }
}
```

#### Расширенный JSON (с правилами):
```json
{
  "tokens": { ... },
  "rules": {
    "spacing": {
      "standard": [0, 4, 8, 12, 16, 20, 24, 32]
    },
    "contrast": {
      "normalText": 4.5
    }
  }
}
```

#### Полный JSON (с компонентами):
```json
{
  "tokens": { ... },
  "rules": { ... },
  "components": {
    "button": {
      "sizes": { ... },
      "variants": [...],
      "states": [...]
    }
  }
}
```

### 6. Преимущества использования JSON

✨ **Точность** - AI знает точные значения токенов вашей системы
✨ **Консистентность** - проверка соответствия всем правилам
✨ **Автоматизация** - не нужно вручную указывать каждый токен
✨ **Масштабируемость** - легко обновить JSON при изменении системы
✨ **Переиспользование** - один JSON для всей команды

### 7. Просмотр загруженного JSON

После загрузки вы можете:
- Нажать **"Просмотр"** чтобы увидеть содержимое
- Нажать **🗑️ Очистить** чтобы удалить JSON
- Загрузить новый JSON (старый будет заменен)

### 8. Приоритет данных

AI использует данные в следующем порядке:
1. **Кастомный JSON** (если загружен) - наивысший приоритет
2. **Автоматически собранные токены** из Figma переменных
3. **Стандартные правила Orbita** - по умолчанию

---

**Совет:** Храните `design-system.json` в репозитории вашего проекта и обновляйте его вместе с дизайн-системой!

**Версия:** 2.0  
**Дата:** 6 октября 2025
