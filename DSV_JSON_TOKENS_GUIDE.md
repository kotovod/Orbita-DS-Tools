# Design System Validator - Загрузка токенов из JSON

## Проблема

Figma Plugin API метод `getLocalVariablesAsync()` не всегда корректно возвращает все remote токены из подключённых библиотек. Это может привести к неполной проверке компонентов на использование дизайн-токенов.

## Решение

Загрузка полного списка токенов из JSON файла, который можно экспортировать из вашей системы дизайна.

---

## Формат JSON файла

JSON файл должен содержать массив `tokens` с объектами токенов:

```json
{
  "tokens": [
    {
      "name": "color/primary/500",
      "type": "COLOR",
      "value": "#F86025",
      "isRemote": true,
      "description": "Основной оранжевый цвет",
      "scopes": ["ALL_FILLS", "ALL_STROKES"]
    },
    {
      "name": "spacing/md",
      "type": "FLOAT",
      "value": 16,
      "isRemote": true,
      "description": "Medium отступ",
      "scopes": ["GAP", "ALL_PADDING"]
    }
  ]
}
```

---

## Обязательные поля

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `name` | string | Имя токена (обязательно) | `"color/primary/500"` |

## Опциональные поля

| Поле | Тип | Описание | Значение по умолчанию |
|------|-----|----------|----------------------|
| `type` | string | Тип токена | `"UNKNOWN"` |
| `value` | any | Значение токена | - |
| `isRemote` | boolean | Токен из библиотеки? | `false` |
| `remote` | boolean | Альтернатива `isRemote` | `false` |
| `description` | string | Описание токена | `""` |
| `scopes` | array | Области применения | `[]` |
| `id` | string | ID токена (генерируется автоматически если не указан) | - |

---

## Типы токенов (type)

- `COLOR` - цвет
- `FLOAT` - числовое значение (отступы, размеры, радиусы)
- `STRING` - текстовое значение
- `BOOLEAN` - булево значение
- `UNKNOWN` - неизвестный тип

---

## Режимы проверки

После загрузки JSON вы можете выбрать режим проверки:

1. **Local** - проверка только локальных токенов (`isRemote: false`)
2. **Remote** - проверка только токенов из библиотек (`isRemote: true`)
3. **All** - проверка всех токенов

---

## Как использовать

### Шаг 1: Экспорт токенов из вашей системы

Экспортируйте токены из вашей системы дизайна (Figma, Tokens Studio, Style Dictionary и т.д.) в JSON формат.

**Пример скрипта для экспорта из Figma Variables:**

```javascript
// Запустите в консоли Figma Plugin
const allVariables = await figma.variables.getLocalVariablesAsync();
const tokens = allVariables.map(v => ({
  name: v.name,
  type: v.resolvedType,
  id: v.id,
  isRemote: v.remote || false,
  description: v.description || '',
  scopes: v.scopes || []
}));

const jsonData = {
  tokens: tokens
};

console.log(JSON.stringify(jsonData, null, 2));
```

### Шаг 2: Сохраните JSON файл

Скопируйте JSON из консоли и сохраните в файл, например `design-tokens.json`.

### Шаг 3: Загрузите в плагин

1. Откройте плагин **Orbita Icon Checker** в режиме **Design System Validator**
2. Раскройте секцию **"Альтернатива: Загрузить JSON с токенами"**
3. Нажмите **"📁 Загрузить JSON"**
4. Выберите ваш JSON файл
5. Убедитесь, что отображается сообщение с количеством загруженных токенов

### Шаг 4: Запустите проверку

1. Выберите режим проверки (Local / Remote / All)
2. Настройте опции при необходимости
3. Нажмите **"🔍 Проверить Variables"**

---

## Пример полного JSON файла

См. файл `tokens-example.json` в корне плагина.

---

## Преимущества использования JSON

✅ **Полный контроль** - вы точно знаете какие токены используются для проверки  
✅ **Независимость от Figma API** - не зависит от ограничений API  
✅ **Поддержка любых систем** - можно использовать токены из любой системы дизайна  
✅ **Версионность** - можно хранить разные версии токенов  
✅ **Офлайн работа** - токены сохраняются в localStorage браузера  

---

## Очистка данных

Чтобы очистить загруженные токены и вернуться к использованию Figma API:

1. Раскройте секцию **"Альтернатива: Загрузить JSON с токенами"**
2. Нажмите **"🗑️ Очистить"**

---

## Отладка

При загрузке JSON плагин выводит подробную информацию в консоль:

- Количество загруженных токенов
- Статистику local/remote токенов
- Первые 10 токенов для проверки

Откройте консоль разработчика (DevTools) для просмотра логов:
- **macOS**: `Cmd + Option + I`
- **Windows**: `Ctrl + Shift + I`

---

## Устранение проблем

### Ошибка "JSON должен содержать массив tokens"

Убедитесь, что ваш JSON файл имеет структуру:
```json
{
  "tokens": [ ... ]
}
```

### Показывает 0 токенов после загрузки

Проверьте:
1. Все токены имеют поле `name`
2. Формат JSON корректный (валидный JSON)
3. Поле `isRemote` или `remote` установлено правильно
4. Выбранный режим (Local/Remote/All) соответствует типу ваших токенов

### Токены загружены, но проверка не находит проблем

Это может означать, что:
1. Ваши компоненты действительно используют токены правильно ✅
2. Проверка работает с токенами из JSON, а элементы используют реальные Figma Variables
3. Нужно убедиться, что имена токенов в JSON совпадают с именами в Figma

---

## Комбинированный подход

Вы можете:
1. Сначала запустить проверку **без JSON** (используя Figma API)
2. Затем загрузить **JSON с токенами** и повторить проверку
3. Сравнить результаты - это покажет какие токены Figma API не видит

---

## Контакт

Если у вас возникли вопросы или проблемы, проверьте консоль разработчика для подробных логов.

