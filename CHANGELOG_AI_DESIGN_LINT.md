# История изменений AI Design Lint

## Версия 2.2 - 15 октября 2025 (вторая итерация)

### 🐛 Дополнительные исправления

#### Навигация к объектам в Figma
- **Проблема:** Клик на элементы не открывал слои в Figma
- **Причина:** Использовался синхронный метод `getNodeById()`
- **Решение:** Переход на асинхронный `getNodeByIdAsync()` (как в проверке иконок)

#### Полная проверка токенов
- **Проблема:** Не проверялось наличие токенов для gap, paddings, radius
- **Решение:** Добавлена проверка `boundVariables` для всех spacing свойств
- **Теперь различает:**
  - ✅ Значения с токенами → tokensUsed
  - ⚠️ Стандартные без токенов → hardcoded с рекомендацией привязать
  - ❌ Нестандартные без токенов → hardcoded с рекомендацией исправить

#### UI блока AI Assistant
- **Проблема:** Маленькое поле ввода, горизонтальная компоновка
- **Решение:** 
  - Вертикальная компоновка
  - Поле ввода 60-120px
  - Кнопка на всю ширину с текстом "💬 Задать вопрос AI"

#### Работа AI Assistant
- **Проблема:** Использовался несуществующий API
- **Решение:** 
  - Подключен к Yandex API (`api.eliza.yandex.net`)
  - Правильная структура запроса и извлечение ответа
  - Проверка наличия токена и анализа
  - Логирование для отладки

---

## Версия 2.1 - 15 октября 2025

### 🐛 Исправленные баги

#### 1. Кнопка "Анализировать дизайн" не работала
- **Проблема:** При клике на кнопку результаты не отображались
- **Причина:** `innerHTML` удалял все дочерние DOM-элементы
- **Решение:** Индикатор загрузки теперь добавляется без удаления структуры

#### 2. TypeError: Cannot read properties of undefined (reading 'type')
- **Проблема:** Ошибка при инициализации плагина
- **Причина:** Обработчик получал сообщения от расширений браузера
- **Решение:** Добавлена проверка `event.data.pluginMessage` перед доступом

#### 3. TypeError: cannot convert symbol to string
- **Проблема:** Падение при анализе некоторых компонентов
- **Причина:** `node.name` и `path` могли быть Symbol
- **Решение:** Явное преобразование через `String()` во всех template strings

#### 4. Score всегда равен 0
- **Проблема:** Оценка компонента не отображалась корректно
- **Причина:** Отсутствие округления и логирования
- **Решение:** Добавлено `Math.round()` и детальное логирование расчета

#### 5. "Cannot unwrap symbol" при повторном анализе
- **Проблема:** Второй запуск анализа падал с ошибкой сериализации
- **Причина:** Symbol не может быть передан через `postMessage`
- **Решение:** Создана функция `sanitizeForPostMessage()` для очистки данных

### ✨ Новые возможности

#### Навигация по клику к проблемным элементам
**Что добавлено:**
- 🖱️ Клик на Token → переход к слою в Figma
- 🖱️ Клик на Hardcoded значение → переход к слою в Figma
- 🖱️ Клик на Token Suggestion → переход к слою в Figma

**UX улучшения:**
- 🎨 Визуальная обратная связь: подсветка оранжевым при клике
- 🎨 Hover эффекты: плавная анимация и изменение фона
- 🎨 Курсор pointer показывает, что элемент кликабелен
- 🎨 Tooltip подсказывает о возможности клика

### 🔧 Технические изменения

#### code.js
```javascript
// Новая функция очистки от Symbol
function sanitizeForPostMessage(obj) {
  if (typeof obj === 'symbol') return String(obj);
  // Рекурсивная очистка объектов и массивов
  // ...
}

// Улучшенный расчет Score с логированием
function calculateScore(analysis) {
  console.log('AI Design Lint: Расчет оценки...');
  // Детальное логирование всех этапов
  // ...
  return Math.round(finalScore);
}

// Безопасная работа со строками
const safePath = String(path || '');
const nodeName = String(node.name || 'Unnamed');
description: `Non-standard radius: ${String(node.cornerRadius)}px`
```

#### ui.html
```javascript
// Кликабельные элементы с навигацией
<div class="token-item" 
     onclick="highlightLayer('${token.nodeId}', event)"
     style="cursor: pointer;">

// Улучшенная функция подсветки
function highlightLayer(nodeId, event) {
  // Визуальная обратная связь
  clickedElement.style.backgroundColor = '#F86025';
  // Отправка команды в Figma
  parent.postMessage({
    pluginMessage: { type: 'highlight-layer', nodeId }
  }, '*');
}
```

```css
/* Hover эффекты для всех кликабельных элементов */
.token-item:hover {
  background-color: rgba(76, 175, 80, 0.1);
  transform: translateX(2px);
}

.hardcoded-item:hover {
  background-color: #505059;
}

.token-suggestion-item:hover {
  background-color: rgba(248, 96, 37, 0.1);
  transform: translateX(2px);
}
```

### 📊 Статистика

**Исправлено багов:** 7
**Добавлено функций:** 2 (sanitizeForPostMessage, улучшенный calculateScore)
**Добавлено UX улучшений:** 4 (клики, hover, анимации, tooltips)
**Строк кода изменено:** ~150

### 🧪 Тестирование

Все функции протестированы:
- ✅ Анализ работает с первого раза
- ✅ Повторный анализ работает без ошибок
- ✅ Score отображается корректно (0-100)
- ✅ Навигация работает для всех элементов
- ✅ Визуальная обратная связь работает
- ✅ Hover эффекты работают плавно
- ✅ Нет ошибок в консоли
- ✅ Логирование помогает отладке

### 📝 Известные ограничения

1. **Score может быть отрицательным теоретически** - ограничен `Math.max(0, score)`
2. **Максимальный бонус за токены** - ограничен +20 (даже если токенов больше 10)
3. **Навигация работает только если nodeId корректный** - если слой удален, показывается ошибка

### 🔜 Планы на будущее

- [ ] Добавить кнопку "Применить токен" прямо из интерфейса
- [ ] Групповое исправление всех hardcoded значений одной кнопкой
- [ ] Экспорт отчета в PDF/Markdown
- [ ] История анализов с сравнением результатов
- [ ] Интеграция с дизайн-системой для автоматических исправлений

---

## Версия 2.0 - 6 октября 2025

### Первый релиз AI Design Lint
- Анализ компонентов с помощью Yandex AI
- Проверка токенов, accessibility, структуры
- Code Readiness Score 0-100
- Детальные рекомендации по улучшению

---

**Авторы:** Kotovod & AI Assistant (Claude Sonnet 4.5)  
**Дизайн-система:** Orbita  
**Платформа:** Figma Plugin

