# Исправление навигации и улучшение проверки токенов

## Дата: 15 октября 2025 (вторая итерация)

## 🎯 Решенные проблемы

### 1. ❌ Не работал переход по клику к объекту в Figma
**Проблема:** При клике на элементы в Token Usage и Hard-coded Values переход к слою не происходил

**Причина:** Использовался **синхронный** метод `figma.getNodeById()` вместо **асинхронного** `figma.getNodeByIdAsync()`

**Решение:**
```javascript
// БЫЛО (не работало):
const node = figma.getNodeById(nodeId);

// СТАЛО (работает):
const node = await figma.getNodeByIdAsync(nodeId);
```

Теперь используется тот же подход, что и в функции проверки иконок - асинхронный метод с await.

**Файл:** `code.js`, строки 307-346

---

### 2. ❌ Не проверялось наличие токенов для gap, paddings, radius
**Проблема:** Анализ не различал:
- Значения с токенами ✅
- Стандартные значения без токенов ⚠️  
- Нестандартные значения без токенов ❌

**Решение:** Добавлена полная проверка `boundVariables` для:
- ✅ **itemSpacing (gap)** - проверяется `boundVariables.itemSpacing`
- ✅ **paddingLeft/Right/Top/Bottom** - проверяется каждый отдельно
- ✅ **cornerRadius** - проверяется `boundVariables.cornerRadius`

#### Что теперь проверяется:

**Для gap (itemSpacing):**
```javascript
if (node.boundVariables && node.boundVariables.itemSpacing) {
  // ✅ Есть токен → добавляем в tokensUsed
  analysis.tokensUsed.push({ type: 'spacing', name: variable.name });
} else {
  // ❌ Нет токена → добавляем в hardcodedValues
  analysis.hardcodedValues.push({
    description: `Hardcoded gap: ${value}px - используй токен spacing`
  });
}
```

**Для paddings:**
```javascript
for (const prop of paddingProps) {
  const boundVar = node.boundVariables && node.boundVariables[prop.key];
  
  if (boundVar) {
    // ✅ Есть токен → добавляем в tokensUsed
  } else if (!localRules.standardSpacings.includes(prop.value)) {
    // ❌ Нестандартное значение без токена
    description: `Hardcoded ${prop.name}: ${value}px - используй токен`
  } else {
    // ⚠️ Стандартное значение, но без токена
    description: `${prop.name}: ${value}px без токена - привяжи к переменной`
  }
}
```

**Для corner radius:**
```javascript
if (node.boundVariables && node.boundVariables.cornerRadius) {
  // ✅ Есть токен → добавляем в tokensUsed
} else if (!localRules.standardRadii.includes(node.cornerRadius)) {
  // ❌ Нестандартное значение без токена
  description: `Hardcoded non-standard radius: ${value}px`
} else {
  // ⚠️ Стандартное значение, но без токена
  description: `Corner radius: ${value}px без токена - привяжи к переменной`
}
```

**Файл:** `code.js`, строки 2627-2747

---

### 3. ❌ Неудобный UI блока AI Assistant
**Проблема:** 
- Поле ввода было слишком маленьким (36px)
- Горизонтальная компоновка с маленькой кнопкой
- Непонятно, что это за кнопка (просто эмодзи 💬)

**Решение:**
- ✅ Вертикальная компоновка (`flex-direction: column`)
- ✅ Увеличено поле ввода (min-height: 60px, max-height: 120px)
- ✅ Кнопка на всю ширину с понятным текстом
- ✅ Улучшены отступы и размеры

**БЫЛО:**
```html
<div style="display: flex; gap: 6px; align-items: flex-end;">
  <textarea style="min-height: 36px; font-size: 11px;"></textarea>
  <button style="height: 36px; min-width: 50px;">💬</button>
</div>
```

**СТАЛО:**
```html
<div style="display: flex; flex-direction: column; gap: 8px;">
  <textarea style="min-height: 60px; max-height: 120px; font-size: 12px;"></textarea>
  <button style="width: 100%; padding: 10px;">💬 Задать вопрос AI</button>
</div>
```

**Файл:** `ui.html`, строки 1353-1358

---

### 4. ❌ AI Assistant не работал
**Проблема:** 
- Использовался несуществующий API endpoint (`https://api.eliza.ai/v1/chat`)
- Неправильная структура запроса
- Не проверялось наличие анализа

**Решение:**
Переписана функция `sendToElizaAPI()` с использованием правильного Yandex API:

```javascript
// Правильный endpoint
const response = await fetch('https://api.eliza.yandex.net/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'yandexgpt-lite',
    messages: [
      { role: 'system', content: systemPrompt },
      { role: 'user', content: message }
    ],
    temperature: 0.6,
    maxTokens: 500
  })
});

// Правильное извлечение ответа
const aiResponse = data.result?.alternatives?.[0]?.message?.text || 
                  data.choices?.[0]?.message?.content ||
                  'Извините, не удалось получить ответ от AI.';
```

**Добавлены проверки:**
- ✅ Проверка наличия токена
- ✅ Проверка наличия анализа (сначала нужно анализировать компонент)
- ✅ Логирование для отладки
- ✅ Правильная обработка ошибок

**Текст кнопки:**
- Обычное состояние: `💬 Задать вопрос AI`
- Во время загрузки: `⏳ AI думает...`
- После ответа: `💬 Задать вопрос AI`

**Файл:** `ui.html`, строки 4614-4695

---

## 📊 Результаты улучшений

### Навигация
**До:** ❌ Клик не работал  
**После:** ✅ Клик открывает слой в Figma, viewport прокручивается к элементу

**Логирование:**
```
AI Design Lint: Переход к узлу: 123:456
AI Design Lint: Узел найден: Button FRAME
AI Design Lint: Переход выполнен успешно
```

### Проверка токенов
**До:** 
- Проверялись только нестандартные значения
- Не различались токены и hardcoded

**После:**
- ✅ Проверяется наличие `boundVariables`
- ✅ Различаются: токены / стандартные без токенов / нестандартные без токенов
- ✅ Конкретные сообщения: "используй токен spacing", "привяжи к переменной"

**Примеры сообщений:**
```
✅ Token Usage: spacing-2 (gap: 8px)
⚠️ Hard-coded: paddingLeft: 8px без токена - привяжи к переменной
❌ Hard-coded: paddingTop: 7px - используй токен spacing (8px)
```

### AI Assistant
**До:**
- ❌ API не работал
- ❌ Маленькое поле ввода
- ❌ Горизонтальная компоновка

**После:**
- ✅ Подключен к Yandex API
- ✅ Удобное большое поле ввода
- ✅ Вертикальная компоновка
- ✅ Понятная кнопка с текстом

**Пример использования:**
1. Выполните анализ компонента
2. Задайте вопрос: "Как исправить проблему с отступами?"
3. AI ответит с учетом контекста анализа

---

## 🧪 Как протестировать

### 1. Тест навигации
1. Запустите AI Design Lint
2. Выберите компонент и нажмите "Анализировать дизайн"
3. Кликните на любой элемент в секциях:
   - Token Usage
   - Hard-coded Values
   - Token Suggestions
4. **Ожидаемо:** В Figma выделится нужный слой и viewport прокрутится к нему
5. В консоли плагина: `AI Design Lint: Переход выполнен успешно`

### 2. Тест проверки токенов
1. Создайте компонент с Auto Layout:
   - itemSpacing: 8px (с токеном spacing-2)
   - paddingLeft: 8px (БЕЗ токена)
   - paddingTop: 7px (БЕЗ токена, нестандартное)
   - cornerRadius: 8px (с токеном radius-2)
   
2. Запустите анализ

3. **Ожидаемо:**
   - Token Usage: spacing-2, radius-2
   - Hard-coded Values:
     - `paddingLeft: 8px без токена - привяжи к переменной`
     - `Hardcoded paddingTop: 7px - используй токен spacing`

### 3. Тест AI Assistant
1. Выполните анализ компонента
2. Откройте секцию "💬 AI Assistant"
3. Введите вопрос: "Какие токены использовать для отступов?"
4. Нажмите "💬 Задать вопрос AI"
5. **Ожидаемо:**
   - Кнопка меняется на `⏳ AI думает...`
   - Через несколько секунд появляется ответ от AI
   - В консоли: `AI Assistant: Ответ получен`

**Если токен не настроен:**
```
❌ Ошибка: Не настроен Yandex OAuth токен. Перейдите в настройки.
```

**Если анализ не выполнен:**
```
❌ Сначала выполните анализ компонента, нажав "Анализировать дизайн"
```

---

## 📝 Технические детали

### Изменения в code.js

#### 1. Асинхронный метод для навигации (строки 307-346)
```javascript
} else if (msg.type === 'highlight-layer') {
  try {
    console.log('AI Design Lint: Переход к узлу:', msg.nodeId);
    const node = await figma.getNodeByIdAsync(nodeId); // Используем async!
    
    if (node) {
      figma.currentPage.selection = [node];
      figma.viewport.scrollAndZoomIntoView([node]);
      console.log('AI Design Lint: Переход выполнен успешно');
    }
  } catch (error) {
    console.error('AI Design Lint: Ошибка:', error);
  }
}
```

#### 2. Полная проверка токенов (строки 2627-2747)
**Добавлено:**
- Проверка `boundVariables.itemSpacing` для gap
- Проверка `boundVariables[paddingKey]` для каждого padding
- Проверка `boundVariables.cornerRadius` для radius
- Добавление в `tokensUsed` если токен найден
- Добавление в `hardcodedValues` если токен НЕ найден
- Разные сообщения для стандартных и нестандартных значений

### Изменения в ui.html

#### 1. UI блока AI Assistant (строки 1353-1358)
```html
<div style="display: flex; flex-direction: column; gap: 8px;">
  <textarea 
    style="width: 100%; min-height: 60px; max-height: 120px; 
           padding: 8px; font-size: 12px; line-height: 1.4;">
  </textarea>
  <button style="width: 100%; padding: 10px; font-size: 12px;">
    💬 Задать вопрос AI
  </button>
</div>
```

#### 2. Функция sendToElizaAPI (строки 4614-4695)
**Исправлено:**
- ✅ Правильный API endpoint: `https://api.eliza.yandex.net/v1/chat/completions`
- ✅ Правильная модель: `yandexgpt-lite`
- ✅ Правильное извлечение ответа: `data.result?.alternatives?.[0]?.message?.text`
- ✅ Проверка наличия токена
- ✅ Проверка наличия анализа
- ✅ Логирование для отладки

---

## ✅ Статус: Полностью исправлено

Все функции AI Design Lint теперь работают корректно:
- ✅ Навигация по клику к объектам в Figma
- ✅ Полная проверка токенов для gap, paddings, radius
- ✅ Удобный UI блока AI Assistant
- ✅ AI Assistant работает с Yandex API
- ✅ Подробное логирование для отладки
- ✅ Информативные сообщения об ошибках

---

## 🔜 Дополнительные улучшения

Можно добавить в будущем:
- [ ] Кнопка "Применить токен" прямо из интерфейса
- [ ] Групповое исправление всех hardcoded значений
- [ ] История чата AI Assistant между анализами
- [ ] Сохранение истории чата в localStorage

