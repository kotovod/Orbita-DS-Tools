# Bugfix: Разрешение алиасов токенов

**Дата:** 16 октября 2025  
**Версия:** 3.2.1

## 🐛 Проблема

Кнопка "🔗 Привязать токен" не появлялась для токенов, которые были алиасами.

### Причина:

При экспорте токенов из Figma, токены-алиасы экспортировались в формате:
```json
{
  "name": "orb-text/primary",
  "type": "COLOR",
  "value": "{alias: VariableID:8c8e4c0fe213039be780a6ef12f2875bf9b76be2/2007:0}",
  "isAlias": true
}
```

Система пыталась сравнить цвет элемента со строкой `"{alias: ...}"` вместо реального RGB значения цвета.

## ✅ Решение

### 1. Обновлён `export-tokens-script.js`

**Добавлено:**
- Функция `resolveVariableAlias()` для рекурсивного разрешения алиасов
- Защита от циклических алиасов
- Экспорт как оригинального значения, так и разрешённого

**Теперь экспортируется:**
```json
{
  "name": "orb-text/primary",
  "type": "COLOR",
  "value": "{alias: VariableID:...}",
  "isAlias": true,
  "resolvedValue": {
    "r": 0.972549,
    "g": 0.376471,
    "b": 0.145098,
    "a": 1
  }
}
```

### 2. Обновлён `code.js`

**Изменено:**
- При загрузке токенов из JSON используется `resolvedValue` если токен является алиасом
- Добавлены логи для отслеживания разрешения алиасов
- Сохраняется как оригинальное значение (алиас), так и разрешённое

## 📋 Как использовать

### Шаг 1: Переэкспортируйте токены

1. Откройте Figma файл с токенами
2. Запустите плагин Orbita Icon Checker (любой режим)
3. Откройте консоль разработчика (`Cmd+Option+I` / `Ctrl+Shift+I`)
4. Скопируйте и вставьте **новый** `export-tokens-script.js` в консоль
5. Скопируйте полученный JSON

### Шаг 2: Загрузите новые токены

1. Откройте Design System Validator
2. Нажмите "📁 Загрузить JSON" (или "🔄 Заменить" если токены уже загружены)
3. Выберите файл с новым JSON

### Шаг 3: Проверьте работу

1. Запустите проверку DSV
2. Откройте консоль и проверьте логи:
   ```
   DSV: Алиас разрешён для "orb-text/primary": {r: 0.97, g: 0.38, b: 0.15, a: 1}
   DSV findTokenByValue: цвет RGB(0.97, 0.38, 0.15)
   DSV findTokenByValue: ✅ Найден цветовой токен: orb-text/primary
   ```
3. Увидите кнопки "🔗 Привязать токен" для элементов с подходящими цветами!

## 🔍 Отладочные логи

Добавлены подробные логи для диагностики:

### При загрузке токенов:
```
DSV: Алиас разрешён для "color/primary": {r: 0.97, g: 0.38, b: 0.15, a: 1}
```

### При поиске токена:
```
DSV findTokenByValue: поиск токена. Type: fills, Value: {r: 0.97, ...}, Variables count: 150
DSV findTokenByValue: цвет RGB(0.97, 0.38, 0.15)
DSV findTokenByValue: ✅ Найден цветовой токен: color/primary
```

### Если токен не найден:
```
DSV findTokenByValue: ❌ Цветовой токен не найден
DSV: Поиск токена для Fill, значение: {r: 0.5, ...}, найден: нет
```

## 📊 Изменённые файлы

### export-tokens-script.js
- **Добавлено:** Функция `resolveVariableAlias()` (строки 13-50)
- **Изменено:** Цикл экспорта токенов - теперь `for...of` вместо `map` (строки 73-128)
- **Добавлено:** Разрешение алиасов при экспорте (строки 104-116)

### code.js
- **Изменено:** Логика загрузки токенов - использование `resolvedValue` (строки 3809-3816)
- **Добавлено:** Логирование разрешения алиасов (строки 3813-3816)
- **Добавлено:** Множество отладочных логов в `findTokenByValue()` (строки 4348-4451)

## 🎯 Примеры

### Пример 1: Цветовой алиас

**До исправления:**
- Токен: `color/primary` → алиас на `base/orange`
- Система искала: строку `"{alias: ...}"`
- Результат: ❌ Токен не найден

**После исправления:**
- Токен: `color/primary` → разрешается в `RGB(0.97, 0.38, 0.15)`
- Система искала: `RGB(0.97, 0.38, 0.15)`
- Результат: ✅ Токен найден! Кнопка появляется

### Пример 2: Числовой алиас

**Токен:**
```json
{
  "name": "spacing/md",
  "type": "FLOAT",
  "isAlias": true,
  "value": "{alias: spacing/base}",
  "resolvedValue": 16
}
```

**Поведение:**
- Элемент с gap = 16
- Система находит `spacing/md`
- Кнопка "🔗 Привязать токен" появляется

## ⚠️ Важно

### Циклические алиасы
Если в вашей системе есть циклические алиасы (A → B → A), они будут обнаружены и пропущены:
```
⚠️ Обнаружен циклический алиас: VariableID:...
```

### Несуществующие алиасы
Если алиас указывает на несуществующую переменную:
```
⚠️ Переменная-алиас не найдена: VariableID:...
```

## 🧪 Тестирование

### Тест 1: Экспорт с алиасами
1. Создайте базовый цвет: `base/orange = RGB(248, 96, 37)`
2. Создайте алиас: `color/primary = alias → base/orange`
3. Экспортируйте токены новым скриптом
4. Проверьте JSON: должен быть `resolvedValue` с RGB

### Тест 2: Поиск токена
1. Создайте элемент с заливкой `RGB(248, 96, 37)`
2. Загрузите JSON с токенами
3. Запустите DSV
4. ✅ Должна появиться кнопка "🔗 Привязать токен"

### Тест 3: Привязка токена
1. Нажмите "🔗 Привязать токен"
2. ✅ Токен должен привязаться
3. ✅ Issue должна исчезнуть из списка

## 📚 Техническая информация

### Формат resolvedValue

**Для цветов (COLOR):**
```json
{
  "r": 0.972549,    // Red (0-1)
  "g": 0.376471,    // Green (0-1)
  "b": 0.145098,    // Blue (0-1)
  "a": 1            // Alpha (0-1)
}
```

**Для чисел (FLOAT/NUMBER):**
```json
16  // Простое число
```

**Для строк (STRING):**
```json
"some-value"  // Строка
```

### Алгоритм разрешения алиасов

```javascript
1. Проверить, является ли value алиасом (type === 'VARIABLE_ALIAS')
2. Если да:
   a. Найти переменную по aliasId
   b. Получить её значение
   c. Если это снова алиас → рекурсивно повторить
   d. Если нет → вернуть значение
3. Защита от циклов: Set посещённых ID
```

## 🎉 Результат

✅ Кнопка "🔗 Привязать токен" теперь работает с алиасами!  
✅ Автоматическое разрешение цепочек алиасов  
✅ Защита от циклических ссылок  
✅ Подробные логи для отладки  

---

**Статус:** ✅ Исправлено  
**Версия:** 3.2.1  
**Требуется:** Переэкспорт токенов новым скриптом

