# Changelog - Design System Validator: JSON Tokens Support

## Версия 3.0.3 - Поддержка загрузки токенов из JSON

**Дата**: Октябрь 16, 2025

---

## 🎯 Проблема

Figma Plugin API метод `getLocalVariablesAsync()` не всегда корректно возвращает все remote токены из подключённых библиотек. Это приводит к неполной проверке компонентов.

**Пример**:
- У пользователя в дизайн-системе **100+ токенов**
- Figma API возвращает только **32 токена**
- В примерах показывается только **5 токенов**

---

## ✨ Решение

Добавлена возможность **загрузки полного списка токенов из JSON файла**, который можно экспортировать напрямую из Figma или любой другой системы дизайна.

---

## 🚀 Новые возможности

### 1. Загрузка JSON файла с токенами

**UI изменения:**
- Новая секция "Альтернатива: Загрузить JSON с токенами"
- Кнопка "📁 Загрузить JSON"
- Кнопка "🗑️ Очистить"
- Индикатор статуса загрузки
- Ссылка на формат JSON с примером

**Функциональность:**
- Загрузка JSON файла через input[type="file"]
- Валидация формата JSON
- Сохранение токенов в localStorage
- Автоматическое восстановление при перезапуске плагина
- Подсчёт и отображение количества загруженных токенов

### 2. Улучшенная логика проверки токенов

**code.js изменения:**

#### Функция `getVariablesByMode(mode, tokensFromJson)`
- Новый параметр `tokensFromJson` - массив токенов из JSON
- Автоматическое определение источника токенов (Figma API или JSON)
- Создание совместимых объектов токенов из JSON
- Поддержка полей: `name`, `type`, `value`, `isRemote`, `description`, `scopes`
- Автогенерация ID для JSON токенов
- Детальное логирование для отладки

#### Функция `validateVariableBinding()`
- Улучшена логика проверки токенов
- **Двойная проверка**: по ID и по имени
- Поддержка JSON токенов с другими ID
- Новая функция `findTokenByName()` для поиска по имени

#### Функция `validateDesignSystem(mode, options)`
- Передача `tokensFromJson` из опций
- Логирование источника токенов (Figma API или JSON)

### 3. UI улучшения

**ui.html изменения:**
- Обработчики загрузки JSON файла
- Валидация структуры JSON
- Сообщения об ошибках
- Отображение количества токенов
- Автоматическое восстановление из localStorage
- Alert с форматом JSON и примером

---

## 📦 Новые файлы

### 1. `tokens-example.json`
Пример JSON файла с токенами (12 токенов):
- Цвета (color)
- Отступы (spacing)
- Радиусы (border-radius)
- Local и Remote токены

### 2. `DSV_JSON_TOKENS_GUIDE.md`
Подробная документация:
- Формат JSON
- Обязательные и опциональные поля
- Типы токенов
- Инструкции по экспорту
- Примеры использования
- Отладка и устранение проблем

### 3. `export-tokens-script.js`
Готовый скрипт для экспорта токенов из Figma:
- Запускается в консоли Figma Plugin
- Экспортирует все Variables в JSON
- Автоматически копирует в буфер обмена
- Показывает статистику (local/remote)

### 4. `DSV_QUICK_START_JSON.md`
Краткое руководство:
- Быстрый старт за 3 шага
- FAQ
- Советы по использованию

### 5. `CHANGELOG_DSV_JSON_SUPPORT.md`
Этот файл - подробное описание изменений

---

## 🔧 Технические детали

### Формат JSON

```json
{
  "tokens": [
    {
      "name": "color/primary/500",      // Обязательно
      "type": "COLOR",                  // Опционально
      "value": "#F86025",               // Опционально
      "isRemote": true,                 // Опционально (по умолчанию false)
      "description": "Описание",        // Опционально
      "scopes": ["ALL_FILLS"],          // Опционально
      "id": "VariableID:123:456"        // Опционально (генерируется автоматически)
    }
  ]
}
```

### Поддерживаемые типы токенов
- `COLOR` - цвета
- `FLOAT` - числовые значения (отступы, размеры, радиусы)
- `STRING` - текстовые значения
- `BOOLEAN` - булевы значения
- `UNKNOWN` - неизвестный тип (по умолчанию)

### Логика проверки

1. **Если JSON не загружен** → используется Figma API (`getLocalVariablesAsync()`)
2. **Если JSON загружен** → используются токены из JSON
3. **Проверка токенов**:
   - Сначала проверка по ID (прямое совпадение)
   - Если не найдено → получение реального токена из Figma API
   - Если существует → проверка по имени в JSON токенах
   - Если найдено по имени → токен валиден ✅
   - Если не найдено → токен не валиден ❌

---

## 📊 Преимущества

✅ **Полный контроль** - точно знаете какие токены используются  
✅ **Независимость от Figma API** - не зависит от ограничений API  
✅ **Поддержка любых систем** - токены из Tokens Studio, Style Dictionary и др.  
✅ **Версионность** - можно хранить разные версии токенов  
✅ **Офлайн работа** - токены сохраняются в localStorage  
✅ **Совместимость** - работает с существующей логикой проверки  

---

## 🔄 Обратная совместимость

✅ **Без JSON** - плагин работает как раньше (Figma API)  
✅ **С JSON** - использует токены из JSON  
✅ **Переключение** - можно легко переключаться между режимами  
✅ **Существующие проверки** - вся логика проверки осталась без изменений  

---

## 🧪 Тестирование

### Сценарий 1: Без JSON (Figma API)
1. Запустить проверку без загрузки JSON
2. Проверить консоль: `DSV: Используются токены из Figma API`
3. Посмотреть количество токенов

### Сценарий 2: С JSON
1. Загрузить JSON файл
2. Запустить проверку
3. Проверить консоль: `DSV: Используются токены из загруженного JSON файла: XXX`
4. Сравнить результаты с Figma API

### Сценарий 3: Очистка JSON
1. Очистить JSON
2. Запустить проверку
3. Должны использоваться токены из Figma API

---

## 📝 Изменённые файлы

### `code.js`
- Функция `getVariablesByMode()` - добавлен параметр `tokensFromJson`
- Функция `validateDesignSystem()` - передача `tokensFromJson`
- Функция `validateVariableBinding()` - улучшенная логика проверки
- Новая функция `findTokenByName()` - поиск токена по имени

**Строки изменений**: ~120 строк кода

### `ui.html`
- Секция загрузки JSON (HTML)
- Обработчики загрузки/очистки JSON (JavaScript)
- Валидация и сохранение в localStorage
- Передача токенов в код плагина

**Строки изменений**: ~90 строк кода

---

## 🐛 Известные ограничения

1. **Размер JSON** - localStorage ограничен ~5-10MB (обычно достаточно для тысяч токенов)
2. **Проверка по имени** - требует точного совпадения имён токенов
3. **Значения токенов** - не всегда доступны для проверки (зависит от Figma API)

---

## 🎯 Что дальше?

### Возможные улучшения:
- [ ] Поддержка множественных JSON файлов
- [ ] Экспорт отчёта с информацией об источнике токенов
- [ ] Визуализация токенов в UI
- [ ] Автоматический экспорт токенов (без скрипта)
- [ ] Поддержка токенов из URL
- [ ] Интеграция с GitHub/GitLab для загрузки токенов

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль разработчика
2. Убедитесь, что JSON файл валиден
3. Проверьте формат токенов
4. Посмотрите логи начинающиеся с `DSV:`

---

## 🙏 Благодарности

Спасибо за фидбек о проблеме с remote токенами!

---

**Автор**: AI Assistant  
**Версия плагина**: 3.0.3  
**Дата**: Октябрь 16, 2025

